<?xml version='1.0' encoding='UTF-8'?>
<feed xmlns="http://www.w3.org/2005/Atom"><id>https://github.com/lihe/MyLeetcode</id><title>RSS feed of lihe's MyLeetcode</title><updated>2026-03-11T13:58:17.613249+00:00</updated><link href="https://github.com/lihe/MyLeetcode"/><link href="https://raw.githubusercontent.com/lihe/MyLeetcode/master/feed.xml" rel="self"/><generator uri="https://lkiesow.github.io/python-feedgen" version="1.0.0">python-feedgen</generator><entry><id>https://github.com/lihe/MyLeetcode/issues/31</id><title>LeetCode 226：翻转二叉树（Invert Binary Tree）</title><updated>2026-03-11T13:58:17.998726+00:00</updated><content type="html"><![CDATA[<p><a href="https://leetcode.cn/problems/invert-binary-tree/">226. 翻转二叉树</a></p>
<p>题目的意思是：</p>
<blockquote>
<p><strong>把每个节点的左右子树交换。</strong></p>
</blockquote>
<hr />
<h1><strong>一、题目直观理解</strong></h1>
<p>原树：</p>
<pre><code>      4
     / \
    2   7
   / \ / \
  1  3 6  9
</code></pre>
<p>翻转后：</p>
<pre><code>      4
     / \
    7   2
   / \ / \
  9  6 3  1
</code></pre>
<p>规律：</p>
<pre><code>每个节点：
left ↔ right
</code></pre>
<hr />
<h1><strong>二、核心思路</strong></h1>
<p>对于每个节点：</p>
<pre><code>交换它的左右孩子
</code></pre>
<p>然后继续处理：</p>
<pre><code>左子树
右子树
</code></pre>
<p>这非常适合 <strong>递归</strong>。</p>
<hr />
<h1><strong>三、递归解法（最经典）</strong></h1>
<pre><code class="language-python">class Solution:
    def invertTree(self, root: Optional[TreeNode]) -&gt; Optional[TreeNode]:
        if root is None:
            return None
        
        # 交换左右子树
        root.left, root.right = root.right, root.left
        
        # 递归处理左右子树
        self.invertTree(root.left)
        self.invertTree(root.right)
        
        return root
</code></pre>
<hr />
<h1><strong>四、代码逐行解释</strong></h1>
<h2><strong>1 递归终止条件</strong></h2>
<pre><code>if root is None:
    return None
</code></pre>
<p>如果节点为空：</p>
<pre><code>不用翻转
</code></pre>
<hr />
<h2><strong>2 交换左右子树</strong></h2>
<pre><code>root.left, root.right = root.right, root.left
</code></pre>
<p>例如：</p>
<pre><code>    4
   / \
  2   7
</code></pre>
<p>变成：</p>
<pre><code>    4
   / \
  7   2
</code></pre>
<hr />
<h2><strong>3 递归翻转子树</strong></h2>
<pre><code>self.invertTree(root.left)
self.invertTree(root.right)
</code></pre>
<p>让左右子树也做同样操作。</p>
<hr />
<h1><strong>五、递归执行过程</strong></h1>
<p>例如：</p>
<pre><code>      4
     / \
    2   7
</code></pre>
<p>步骤：</p>
<h3><strong>① 翻转 4</strong></h3>
<pre><code>      4
     / \
    7   2
</code></pre>
<hr />
<h3><strong>② 翻转 7</strong></h3>
<pre><code>    7
   / \
  9   6
</code></pre>
<p>变成</p>
<pre><code>    7
   / \
  6   9
</code></pre>
<hr />
<h3><strong>③ 翻转 2</strong></h3>
<pre><code>    2
   / \
  1   3
</code></pre>
<p>变成</p>
<pre><code>    2
   / \
  3   1
</code></pre>
<hr />
<p>最终：</p>
<pre><code>      4
     / \
    7   2
   / \ / \
  9  6 3  1
</code></pre>
<hr />
<h1><strong>六、时间复杂度</strong></h1>
<pre><code>O(n)
</code></pre>
<p>因为：</p>
<pre><code>每个节点访问一次
</code></pre>
<hr />
<h1><strong>七、空间复杂度</strong></h1>
<pre><code>O(h)
</code></pre>
<p>h 是树高度。</p>
<p>最坏情况：</p>
<pre><code>O(n)
</code></pre>
<hr />
<h1><strong>八、迭代解法（面试加分）</strong></h1>
<p>可以用 <strong>BFS（队列）</strong>：</p>
<pre><code class="language-python">from collections import deque

class Solution:
    def invertTree(self, root):
        if not root:
            return None
        
        queue = deque([root])
        
        while queue:
            node = queue.popleft()
            
            node.left, node.right = node.right, node.left
            
            if node.left:
                queue.append(node.left)
            if node.right:
                queue.append(node.right)
        
        return root
</code></pre>
<hr />
<h1><strong>九、面试回答模板</strong></h1>
<p>可以这样说：</p>
<blockquote>
<p>这题可以用递归解决。对于每个节点交换左右子树，然后递归处理左右孩子。时间复杂度 O(n)，因为每个节点访问一次。</p>
</blockquote>
<hr />
<h1><strong>十、为什么这题这么经典？</strong></h1>
<p>这题考察：</p>
<pre><code>二叉树递归模板
</code></pre>
<p>很多题都能套这个结构：</p>
<pre><code>1 递归终止条件
2 处理当前节点
3 递归左右子树
</code></pre>
<hr />
<h1><strong>十一、这题有个很有名的故事</strong></h1>
<p>这题是 Google 面试题。</p>
<p>有人说：</p>
<blockquote>
<p>如果你无法在白板上写出这题，</p>
</blockquote>
<blockquote>
<p>那你不应该写递归代码。</p>
</blockquote>
<p>所以它成了 <strong>递归入门经典题</strong>。</p>
<hr />
<p>如果你愿意，我可以顺便给你讲：</p>
<p><strong>二叉树面试最重要的 5 个模板</strong></p>
<p>学会以后：</p>
<pre><code>90% 二叉树题
都能秒解
</code></pre>
]]></content><link href="https://github.com/lihe/MyLeetcode/issues/31"/><published>2026-03-11T13:57:47+00:00</published></entry><entry><id>https://github.com/lihe/MyLeetcode/issues/30</id><title>LeetCode 236：二叉树的最近公共祖先</title><updated>2026-03-11T13:58:18.130631+00:00</updated><content type="html"><![CDATA[<p><a href="https://leetcode.cn/problems/lowest-common-ancestor-of-a-binary-tree/">236. 二叉树的最近公共祖先</a></p>
<p>这是 <strong>LeetCode 236：二叉树的最近公共祖先</strong>，属于<strong>二叉树递归题的代表作</strong>。</p>
<p>这题真正考的不是“找祖先”，而是：</p>
<blockquote>
<p><strong>你能不能让递归函数返回“这棵子树里是否找到了 p 或 q”</strong></p>
</blockquote>
<hr />
<h1><strong>一、先给结论（面试可背）</strong></h1>
<blockquote>
<p>对当前节点 root：</p>
</blockquote>
<ul>
<li>
<blockquote>
<p>如果 root 是 p 或 q，直接返回 root</p>
</blockquote>
</li>
<li>
<blockquote>
<p>分别去左子树、右子树找</p>
</blockquote>
</li>
<li>
<blockquote>
<p>如果左右都找到了，说明当前节点就是最近公共祖先</p>
</blockquote>
</li>
<li>
<blockquote>
<p>如果只找到一边，就把那一边的结果返回</p>
</blockquote>
</li>
</ul>
<hr />
<h1><strong>二、为什么这样做？</strong></h1>
<p>最近公共祖先的含义是：</p>
<ul>
<li>
<p>p 和 q 分别在某个节点的左右两边</p>
<p><strong>或者</strong></p>
</li>
<li>
<p>其中一个节点本身就是另一个节点的祖先</p>
</li>
</ul>
<p>所以递归到某个节点时，只需要知道三件事：</p>
<ol>
<li>左子树有没有找到 p 或 q</li>
<li>右子树有没有找到 p 或 q</li>
<li>当前节点是不是 p 或 q</li>
</ol>
<hr />
<h1><strong>三、标准递归解法</strong></h1>
<pre><code class="language-python">class Solution:
    def lowestCommonAncestor(self, root: &#x27;TreeNode&#x27;, p: &#x27;TreeNode&#x27;, q: &#x27;TreeNode&#x27;) -&gt; &#x27;TreeNode&#x27;:
        if root is None:
            return None
        
        if root == p or root == q:
            return root

        left = self.lowestCommonAncestor(root.left, p, q)
        right = self.lowestCommonAncestor(root.right, p, q)

        if left and right:
            return root

        return left if left else right
</code></pre>
<hr />
<h1><strong>四、逐行解释</strong></h1>
<h2><strong>1. 递归终止条件</strong></h2>
<pre><code>if root is None:
    return None
</code></pre>
<p>说明这棵子树没找到。</p>
<hr />
<h2><strong>2. 当前节点就是 p 或 q</strong></h2>
<pre><code>if root == p or root == q:
    return root
</code></pre>
<p>意思是：</p>
<blockquote>
<p>找到了其中一个目标节点，就把它返回给上层</p>
</blockquote>
<p>注意：</p>
<ul>
<li>这里不能继续往下找了</li>
<li>因为一个节点也可以是它自己的祖先</li>
</ul>
<hr />
<h2><strong>3. 递归左右子树</strong></h2>
<pre><code>left = self.lowestCommonAncestor(root.left, p, q)
right = self.lowestCommonAncestor(root.right, p, q)
</code></pre>
<p>left 和 right 表示：</p>
<ul>
<li>左子树返回了什么</li>
<li>右子树返回了什么</li>
</ul>
<p>返回值可能是：</p>
<ul>
<li>None：没找到</li>
<li>p / q</li>
<li>最近公共祖先</li>
</ul>
<hr />
<h2><strong>4. 左右都找到了</strong></h2>
<pre><code>if left and right:
    return root
</code></pre>
<p>说明：</p>
<ul>
<li>p 在一边</li>
<li>q 在另一边</li>
</ul>
<p>所以当前节点 root 就是最近公共祖先。</p>
<hr />
<h2><strong>5. 只有一边找到</strong></h2>
<pre><code>return left if left else right
</code></pre>
<p>把找到的那一边往上传。</p>
<hr />
<h1><strong>五、用例子走一遍</strong></h1>
<p>示例：</p>
<pre><code>        3
      /   \
     5     1
    / \   / \
   6   2 0   8
      / \
     7   4
</code></pre>
<p>找：</p>
<pre><code>p = 5, q = 1
</code></pre>
<hr />
<h2><strong>递归过程</strong></h2>
<ul>
<li>
<p>到节点 5：命中，返回 5</p>
</li>
<li>
<p>到节点 1：命中，返回 1</p>
</li>
<li>
<p>回到节点 3：</p>
<ul>
<li>left = 5</li>
<li>right = 1</li>
</ul>
</li>
</ul>
<p>满足：</p>
<pre><code>if left and right:
    return root
</code></pre>
<p>所以返回：</p>
<pre><code>3
</code></pre>
<hr />
<h1><strong>六、再看示例 2</strong></h1>
<p>找：</p>
<pre><code>p = 5, q = 4
</code></pre>
<p>过程：</p>
<ul>
<li>到节点 5：命中，返回 5</li>
<li>在 5 的右子树里还能找到 4，但因为 5 已经先返回了</li>
<li>最后整个递归会把 5 传上去</li>
</ul>
<p>所以答案：</p>
<pre><code>5
</code></pre>
<p>这也符合：</p>
<blockquote>
<p>一个节点可以是它自己的祖先</p>
</blockquote>
<hr />
<h1><strong>七、为什么这题适合递归？</strong></h1>
<p>因为它天然符合“后序遍历”逻辑：</p>
<ul>
<li>先问左边找到了什么</li>
<li>再问右边找到了什么</li>
<li>最后决定当前节点返回什么</li>
</ul>
<p>这就是典型的：</p>
<blockquote>
<p><strong>从子问题结果推导父问题结果</strong></p>
</blockquote>
<hr />
<h1><strong>八、时间复杂度</strong></h1>
<ul>
<li>
<p>时间复杂度：O(n)</p>
<ul>
<li>每个节点最多访问一次</li>
</ul>
</li>
<li>
<p>空间复杂度：O(h)</p>
</li>
</ul>
<hr />
<h1><strong>九、面试一句话总结</strong></h1>
<blockquote>
<p>这题可以用递归解决。递归函数返回当前子树中找到的 p、q 或最近公共祖先。如果左右子树分别返回非空，说明当前节点就是最近公共祖先；否则返回非空的一侧。</p>
</blockquote>
<hr />
<h1><strong>十、你要形成的模板思维</strong></h1>
<p>这题的递归函数本质上是在问：</p>
<blockquote>
<p><strong>“当前子树里，我能找到什么？”</strong></p>
</blockquote>
<p>很多树题都能套这个思路：</p>
<ul>
<li>最近公共祖先</li>
<li>树的直径</li>
<li>平衡二叉树</li>
<li>路径和</li>
</ul>
<hr />
<p>如果你愿意，我下一步可以继续帮你讲：</p>
<ol>
<li><strong>最近公共祖先为什么一定是后序遍历</strong></li>
<li><strong>二叉搜索树版本的最近公共祖先（更简单）</strong></li>
<li><strong>把这题画成递归返回值流程图</strong></li>
</ol>
<p>你选一个，我继续。</p>
]]></content><link href="https://github.com/lihe/MyLeetcode/issues/30"/><category term="树"/><published>2026-03-11T11:56:08+00:00</published></entry><entry><id>https://github.com/lihe/MyLeetcode/issues/29</id><title>大数加法</title><updated>2026-03-11T13:58:18.283532+00:00</updated><content type="html"><![CDATA[<p>这题本质上就是：<strong>手动模拟竖式加法</strong>。</p>
<p>因为题目不让你：</p>
<ul>
<li>直接 int(num1) + int(num2)</li>
<li>用大整数库</li>
</ul>
<p>所以你就要像小学算加法一样，从<strong>最低位往高位</strong>一位一位加。</p>
<hr />
<h1><strong>一、核心思路（一句话）</strong></h1>
<blockquote>
<p><strong>从两个字符串的末尾开始，逐位相加，维护进位 carry。</strong></p>
</blockquote>
<hr />
<h1><strong>二、为什么要从后往前？</strong></h1>
<p>因为十进制加法是：</p>
<ul>
<li>先算个位</li>
<li>再算十位</li>
<li>再算百位</li>
</ul>
<p>例如：</p>
<pre><code>  123
+ 789
-----
  912
</code></pre>
<p>是从右往左算的。</p>
<p>字符串里：</p>
<ul>
<li>最右边是最低位</li>
<li>最左边是最高位</li>
</ul>
<p>所以要从末尾开始遍历。</p>
<hr />
<h1><strong>三、算法流程</strong></h1>
<p>设两个指针：</p>
<ul>
<li>i 指向 num1 最后一个字符</li>
<li>j 指向 num2 最后一个字符</li>
</ul>
<p>再维护：</p>
<ul>
<li>carry：进位</li>
<li>res：结果字符列表</li>
</ul>
<p>每一步做：</p>
<ol>
<li>取当前位数字</li>
<li>加上进位</li>
<li>结果的个位加入答案</li>
<li>十位变成新的进位</li>
<li>指针左移</li>
</ol>
<p>最后如果还有进位，再补一个。</p>
<hr />
<h1><strong>四、代码实现</strong></h1>
<pre><code class="language-python">class Solution:
    def addStrings(self, num1: str, num2: str) -&gt; str:
        i = len(num1) - 1
        j = len(num2) - 1
        carry = 0
        res = []

        while i &gt;= 0 or j &gt;= 0 or carry:
            x = ord(num1[i]) - ord(&#x27;0&#x27;) if i &gt;= 0 else 0
            y = ord(num2[j]) - ord(&#x27;0&#x27;) if j &gt;= 0 else 0

            total = x + y + carry
            res.append(str(total % 10))
            carry = total // 10

            i -= 1
            j -= 1

        return &#x27;&#x27;.join(res[::-1])
</code></pre>
<hr />
<h1><strong>五、代码逐行解释</strong></h1>
<h2><strong>1. 初始化指针</strong></h2>
<pre><code>i = len(num1) - 1
j = len(num2) - 1
</code></pre>
<p>指向两个字符串最后一位。</p>
<hr />
<h2><strong>2. 进位和结果</strong></h2>
<pre><code>carry = 0
res = []
</code></pre>
<ul>
<li>carry 表示上一位传来的进位</li>
<li>res 用来存结果</li>
</ul>
<hr />
<h2><strong>3. 循环条件</strong></h2>
<pre><code>while i &gt;= 0 or j &gt;= 0 or carry:
</code></pre>
<p>只要满足一个条件就继续：</p>
<ul>
<li>num1 还有没处理的位</li>
<li>num2 还有没处理的位</li>
<li>还有进位没处理完</li>
</ul>
<hr />
<h2><strong>4. 取当前位</strong></h2>
<pre><code>x = ord(num1[i]) - ord(&#x27;0&#x27;) if i &gt;= 0 else 0
y = ord(num2[j]) - ord(&#x27;0&#x27;) if j &gt;= 0 else 0
</code></pre>
<p>这里的意思是：</p>
<ul>
<li>如果指针没越界，就取当前字符对应的数字</li>
<li>如果越界了，就当作 0</li>
</ul>
<p>比如：</p>
<pre><code>ord(&#x27;7&#x27;) - ord(&#x27;0&#x27;)  # 7
</code></pre>
<hr />
<h2><strong>5. 当前位求和</strong></h2>
<pre><code>total = x + y + carry
</code></pre>
<hr />
<h2><strong>6. 当前位结果</strong></h2>
<pre><code>res.append(str(total % 10))
</code></pre>
<p>例如：</p>
<ul>
<li>17 % 10 = 7</li>
<li>当前位写 7</li>
</ul>
<hr />
<h2><strong>7. 更新进位</strong></h2>
<pre><code>carry = total // 10
</code></pre>
<p>例如：</p>
<ul>
<li>17 // 10 = 1</li>
<li>进位变成 1</li>
</ul>
<hr />
<h2><strong>8. 指针左移</strong></h2>
<pre><code>i -= 1
j -= 1
</code></pre>
<hr />
<h2><strong>9. 最后翻转结果</strong></h2>
<p>因为我们是从低位往高位加，所以结果是倒着放进 res 的。</p>
<pre><code>return &#x27;&#x27;.join(res[::-1])
</code></pre>
<hr />
<h1><strong>六、举个例子</strong></h1>
<h2><strong>输入</strong></h2>
<pre><code>num1 = &quot;456&quot;
num2 = &quot;77&quot;
</code></pre>
<hr />
<h2><strong>过程</strong></h2>
<h3><strong>第一步</strong></h3>
<ul>
<li>6 + 7 + 0 = 13</li>
<li>当前位 3</li>
<li>进位 1</li>
</ul>
<p>res = ['3']</p>
<hr />
<h3><strong>第二步</strong></h3>
<ul>
<li>5 + 7 + 1 = 13</li>
<li>当前位 3</li>
<li>进位 1</li>
</ul>
<p>res = ['3', '3']</p>
<hr />
<h3><strong>第三步</strong></h3>
<ul>
<li>4 + 0 + 1 = 5</li>
<li>当前位 5</li>
<li>进位 0</li>
</ul>
<p>res = ['3', '3', '5']</p>
<hr />
<h3><strong>最后翻转</strong></h3>
<pre><code>&quot;533&quot;[::-1] = &quot;533&quot;
</code></pre>
<p>结果：</p>
<pre><code>&quot;533&quot;
</code></pre>
<hr />
<h1><strong>七、时间复杂度</strong></h1>
<p>设：</p>
<ul>
<li>n = len(num1)</li>
<li>m = len(num2)</li>
</ul>
<p>每一位只处理一次，所以：</p>
<ul>
<li>时间复杂度：O(max(n, m))</li>
<li>空间复杂度：O(max(n, m))</li>
</ul>
<hr />
<h1><strong>八、这题为什么叫“大数相加”？</strong></h1>
<p>因为如果字符串非常长，比如几千位：</p>
<pre><code>999999999999999999999999999999999999999...
</code></pre>
<p>已经超出了普通整数类型能表示的范围。</p>
<p>所以不能直接转 int，只能按字符串手动处理。</p>
<hr />
<h1><strong>九、面试回答模板</strong></h1>
<p>你可以这样说：</p>
<blockquote>
<p>这题可以模拟十进制竖式加法。从两个字符串末尾开始逐位相加，用 carry 保存进位。每次把当前位结果加入答案数组，最后再反转得到最终结果。这样可以避免使用大整数库，时间复杂度是 O(max(n,m))。</p>
</blockquote>
<hr />
<h1><strong>十、补充：更简洁版本</strong></h1>
<p>如果你想写得更短一点：</p>
<pre><code class="language-python">class Solution:
    def addStrings(self, num1: str, num2: str) -&gt; str:
        i, j = len(num1) - 1, len(num2) - 1
        carry = 0
        ans = []

        while i &gt;= 0 or j &gt;= 0 or carry:
            a = int(num1[i]) if i &gt;= 0 else 0
            b = int(num2[j]) if j &gt;= 0 else 0
            s = a + b + carry
            ans.append(str(s % 10))
            carry = s // 10
            i -= 1
            j -= 1

        return &#x27;&#x27;.join(reversed(ans))
</code></pre>
<hr />
<p>如果你愿意，我还可以顺着这题继续给你讲：</p>
<p><strong>大数减法 / 大数乘法 / 字符串乘法</strong>，这几个经常是一组一起考。</p>
]]></content><link href="https://github.com/lihe/MyLeetcode/issues/29"/><published>2026-03-10T14:27:02+00:00</published></entry><entry><id>https://github.com/lihe/MyLeetcode/issues/28</id><title>LeetCode 470：用 Rand7() 实现 Rand10()</title><updated>2026-03-11T13:58:18.454730+00:00</updated><content type="html"><![CDATA[<p><a href="https://leetcode.cn/problems/implement-rand10-using-rand7/">470. 用 Rand7() 实现 Rand10()</a></p>
<h1><strong>一、先说结论：标准做法是「拒绝采样」</strong></h1>
<h2><strong>思路</strong></h2>
<p>rand7() 能均匀生成 1~7。</p>
<p>如果我们调用两次：</p>
<ul>
<li>第一次决定“行”</li>
<li>第二次决定“列”</li>
</ul>
<p>就能构造一个 <strong>7 × 7 = 49</strong> 个格子的均匀空间。</p>
<p>也就是：</p>
<pre><code>1, 2, 3, ..., 49
</code></pre>
<p>每个数出现概率都一样。</p>
<hr />
<h2><strong>为什么 49 有用？</strong></h2>
<p>因为：</p>
<pre><code>40 = 10 × 4
</code></pre>
<p>前 40 个数可以平均分成 10 组：</p>
<ul>
<li>1,11,21,31 → 1</li>
<li>2,12,22,32 → 2</li>
<li>…</li>
<li>10,20,30,40 → 10</li>
</ul>
<p>所以：</p>
<ul>
<li>如果生成的是 1<del>40，就可以均匀映射到 1</del>10</li>
<li>如果生成的是 41~49，就丢弃重来</li>
</ul>
<p>这就叫 <strong>拒绝采样</strong></p>
<hr />
<h1><strong>二、标准代码</strong></h1>
<pre><code class="language-python"># The rand7() API is already defined for you.
# def rand7():
# @return a random integer in the range 1 to 7

class Solution:
    def rand10(self):
        while True:
            # 生成 1~49 的均匀随机数
            row = rand7()
            col = rand7()
            num = (row - 1) * 7 + col   # 1~49

            # 只取前 40 个
            if num &lt;= 40:
                return (num - 1) % 10 + 1
</code></pre>
<hr />
<h1><strong>三、为什么这样是均匀的？</strong></h1>
<p>这是面试最爱问的点。</p>
<h2><strong>第一步：1~49为什么均匀？</strong></h2>
<p>因为：</p>
<ul>
<li>row 有 7 种可能，等概率</li>
<li>col 有 7 种可能，等概率</li>
<li>两次独立</li>
</ul>
<p>所以 49 种组合：</p>
<pre><code>(row, col)
</code></pre>
<p>每种概率都是：</p>
<pre><code>1/7 × 1/7 = 1/49
</code></pre>
<p>因此映射出来的 1~49 是均匀的。</p>
<hr />
<h2><strong>第二步：为什么1<del>40 -&gt; 1</del>10也是均匀的？</strong></h2>
<p>因为前 40 个数里，每个余数类恰好出现 4 次：</p>
<pre><code>(num - 1) % 10 + 1
</code></pre>
<p>例如：</p>
<ul>
<li>1,11,21,31 都映射到 1</li>
<li>2,12,22,32 都映射到 2</li>
</ul>
<p>每个结果都来自 4 个等概率数字，</p>
<p>所以 1~10 也是均匀的。</p>
<hr />
<h1><strong>四、为什么不能直接% 10用在 49 上？</strong></h1>
<p>比如你直接写：</p>
<pre><code>num = (rand7()-1) * 7 + rand7()   # 1~49
return (num - 1) % 10 + 1
</code></pre>
<p>这会有问题，因为：</p>
<pre><code>49 不是 10 的倍数
</code></pre>
<ul>
<li>有些数会出现 5 次</li>
<li>有些数只会出现 4 次</li>
</ul>
<p>概率不相等，<strong>不均匀</strong></p>
<p>所以必须只取 1~40。</p>
<hr />
<h1><strong>五、期望调用次数是多少？</strong></h1>
<p>每轮调用 rand7() 两次。</p>
<p>一轮成功的概率是：</p>
<pre><code>40 / 49
</code></pre>
<p>所以期望轮数：</p>
<pre><code>1 / (40/49) = 49/40
</code></pre>
<p>每轮 2 次 rand7()，所以期望调用次数：</p>
<pre><code>2 × 49/40 = 98/40 = 2.45
</code></pre>
<hr />
<h1><strong>六、进阶：还能更省吗？</strong></h1>
<p>可以。</p>
<p>标准优化思路是：</p>
<blockquote>
<p>被拒绝的 41~49 不要完全浪费，继续“回收利用”。</p>
</blockquote>
<p>比如：</p>
<ul>
<li>41~49 一共 9 个数</li>
<li>可以把它们重新映射成 1~9</li>
<li>再结合一次 rand7() 变成 1~63</li>
<li>再取前 60 个</li>
</ul>
<p>这样能进一步降低 rand7() 的期望调用次数。</p>
<p>但在一般面试里，<strong>先写出 49→40 的拒绝采样已经够了</strong>。</p>
<p>如果面试官追问“还能优化吗”，你再讲“回收剩余样本空间”。</p>
<hr />
<h1><strong>七、面试标准回答模板</strong></h1>
<p>你可以这样说：</p>
<blockquote>
<p>我会调用两次 rand7，构造一个 1 到 49 的均匀随机数空间。因为 40 是 10 的倍数，所以取前 40 个数并通过取模映射到 1 到 10，这样可以保证均匀性；而 41 到 49 的部分通过拒绝采样丢弃重试。</p>
</blockquote>
<hr />
<h1><strong>八、一句话总结</strong></h1>
<blockquote>
<p><strong>rand7 → 49 个均匀状态 → 取前 40 个 → 均匀映射到 1~10</strong></p>
</blockquote>
]]></content><link href="https://github.com/lihe/MyLeetcode/issues/28"/><published>2026-03-07T04:21:16+00:00</published></entry><entry><id>https://github.com/lihe/MyLeetcode/issues/27</id><title>Leetcode 33：搜索旋转排序数组</title><updated>2026-03-11T13:58:18.578966+00:00</updated><content type="html"><![CDATA[<p><a href="https://leetcode.cn/problems/search-in-rotated-sorted-array/">33. 搜索旋转排序数组</a></p>
<h1><strong>一、先理解什么是旋转数组</strong></h1>
<p>原始数组是升序：</p>
<pre><code>[0,1,2,4,5,6,7]
</code></pre>
<p>在某个位置 k 左旋：</p>
<pre><code>[4,5,6,7,0,1,2]
</code></pre>
<p>特点：</p>
<pre><code>左边一段有序
右边一段有序
中间断开
</code></pre>
<p>图示：</p>
<pre><code>4 5 6 7 | 0 1 2
</code></pre>
<hr />
<h1><strong>二、为什么还能用二分查找？</strong></h1>
<p>关键结论：</p>
<blockquote>
<p><strong>每次二分时，一定有一半是有序的</strong></p>
</blockquote>
<p>因为旋转只发生一次。</p>
<p>所以我们可以：</p>
<p>1️⃣ 判断哪一半有序</p>
<p>2️⃣ 判断 target 是否在该有序区间</p>
<p>3️⃣ 决定往哪边继续二分</p>
<hr />
<h1><strong>三、核心算法步骤</strong></h1>
<p>初始化：</p>
<pre><code>left = 0
right = n-1
</code></pre>
<p>循环：</p>
<pre><code>while left &lt;= right
</code></pre>
<hr />
<h2><strong>第一步：找到中点</strong></h2>
<pre><code>mid = (left + right) // 2
</code></pre>
<p>如果：</p>
<pre><code>nums[mid] == target
</code></pre>
<p>直接返回。</p>
<hr />
<h2><strong>第二步：判断哪一半有序</strong></h2>
<p>关键判断：</p>
<pre><code>nums[left] &lt;= nums[mid]
</code></pre>
<p>成立说明：</p>
<pre><code>左半部分有序
</code></pre>
<p>否则：</p>
<pre><code>右半部分有序
</code></pre>
<hr />
<h1><strong>四、情况分析</strong></h1>
<h2><strong>情况1：左半部分有序</strong></h2>
<pre><code>nums[left] &lt;= nums[mid]
</code></pre>
<p>数组结构：</p>
<pre><code>left ------ mid | 右半部分
</code></pre>
<p>判断 target 是否在左半区：</p>
<pre><code>nums[left] &lt;= target &lt; nums[mid]
</code></pre>
<p>如果在：</p>
<pre><code>right = mid - 1
</code></pre>
<p>否则：</p>
<pre><code>left = mid + 1
</code></pre>
<hr />
<h2><strong>情况2：右半部分有序</strong></h2>
<pre><code>nums[left] &gt; nums[mid]
</code></pre>
<p>结构：</p>
<pre><code>左半部分 | mid ----- right
</code></pre>
<p>判断 target 是否在右区：</p>
<pre><code>nums[mid] &lt; target &lt;= nums[right]
</code></pre>
<p>如果在：</p>
<pre><code>left = mid + 1
</code></pre>
<p>否则：</p>
<pre><code>right = mid - 1
</code></pre>
<hr />
<h1><strong>五、完整代码（面试标准版）</strong></h1>
<pre><code class="language-python">class Solution:
    def search(self, nums, target):
        left, right = 0, len(nums) - 1

        while left &lt;= right:
            mid = (left + right) // 2

            if nums[mid] == target:
                return mid

            # 左半部分有序
            if nums[left] &lt;= nums[mid]:
                if nums[left] &lt;= target &lt; nums[mid]:
                    right = mid - 1
                else:
                    left = mid + 1

            # 右半部分有序
            else:
                if nums[mid] &lt; target &lt;= nums[right]:
                    left = mid + 1
                else:
                    right = mid - 1

        return -1
</code></pre>
<hr />
<h1><strong>六、举例走一遍</strong></h1>
<pre><code>nums = [4,5,6,7,0,1,2]
target = 0
</code></pre>
<p>第一次：</p>
<pre><code>left=0
right=6
mid=3
nums[mid]=7
</code></pre>
<p>左半有序：</p>
<pre><code>4 ≤ 7
</code></pre>
<p>target 是否在左区？</p>
<pre><code>4 ≤ 0 &lt; 7 ❌
</code></pre>
<p>去右边：</p>
<pre><code>left = 4
</code></pre>
<hr />
<p>第二次：</p>
<pre><code>left=4
right=6
mid=5
nums[mid]=1
</code></pre>
<p>左半有序：</p>
<pre><code>0 ≤ 1
</code></pre>
<p>target 在左：</p>
<pre><code>0 ≤ 0 &lt; 1
right = 4
</code></pre>
<hr />
<p>第三次：</p>
<pre><code>mid=4
nums[mid]=0
</code></pre>
<p>找到。</p>
<hr />
<h1><strong>七、时间复杂度</strong></h1>
<p>二分查找：</p>
<pre><code>O(log n)
</code></pre>
<p>空间：</p>
<pre><code>O(1)
</code></pre>
<hr />
<h1><strong>八、面试回答模板</strong></h1>
<p>面试官问：思路？</p>
<p>你可以这样说：</p>
<blockquote>
<p>虽然数组经过旋转，但每次二分时一定有一半是有序的。我们先判断哪一半有序，再判断目标值是否在该有序区间，如果在就继续在该区间二分，否则搜索另一半，从而保持 O(log n) 的时间复杂度。</p>
</blockquote>
<hr />
<h1><strong>九、这一题在算法体系里的位置</strong></h1>
<p>这是一个 <strong>旋转数组二分模板题</strong>，同一套路可以解决：</p>
<table>
<thead>
<tr>
<th><strong>题目</strong></th>
<th><strong>类型</strong></th>
</tr>
</thead>
<tbody>
<tr>
<td>搜索旋转数组</td>
<td>LeetCode 33</td>
</tr>
<tr>
<td>搜索旋转数组 II（有重复）</td>
<td>LeetCode 81</td>
</tr>
<tr>
<td>旋转数组最小值</td>
<td>LeetCode 153</td>
</tr>
<tr>
<td>旋转数组最小值 II</td>
<td>LeetCode 154</td>
</tr>
</tbody></table><p>其实都是同一个思想。</p>
]]></content><link href="https://github.com/lihe/MyLeetcode/issues/27"/><published>2026-03-04T13:38:11+00:00</published></entry><entry><id>https://github.com/lihe/MyLeetcode/issues/26</id><title>Leetcode 56：合并区间</title><updated>2026-03-11T13:58:18.714864+00:00</updated><content type="html"><![CDATA[<p><a href="https://leetcode.cn/problems/merge-intervals/">56. 合并区间</a></p>
<h1><strong>一、核心思路（一句话）</strong></h1>
<blockquote>
<p><strong>先按区间起点排序，再从左到右合并</strong></p>
</blockquote>
<hr />
<h1><strong>二、为什么一定要排序？</strong></h1>
<p>假设输入：</p>
<pre><code>[[4,7],[1,4]]
</code></pre>
<p>如果不排序，你无法判断哪个在前。</p>
<p>排序后：</p>
<pre><code>[[1,4],[4,7]]
</code></pre>
<p>区间关系变得“线性可判断”。</p>
<hr />
<h1><strong>三、排序后发生了什么？</strong></h1>
<p>排序后，区间一定满足：</p>
<pre><code>intervals[i][0] &gt;= intervals[i-1][0]
</code></pre>
<p>我们只需要判断：</p>
<pre><code>当前区间 start &lt;= 上一个区间 end
</code></pre>
<p>如果满足 → 重叠</p>
<p>否则 → 不重叠</p>
<hr />
<h1><strong>四、合并规则（核心）</strong></h1>
<p>设当前合并区间为：</p>
<pre><code>[cur_start, cur_end]
</code></pre>
<p>新来区间：</p>
<pre><code>[start, end]
</code></pre>
<hr />
<h2><strong>情况 1：重叠</strong></h2>
<pre><code>start &lt;= cur_end
</code></pre>
<p>合并：</p>
<pre><code>cur_end = max(cur_end, end)
</code></pre>
<hr />
<h2><strong>情况 2：不重叠</strong></h2>
<pre><code>start &gt; cur_end
</code></pre>
<ul>
<li>把当前区间加入结果</li>
<li>更新当前区间</li>
</ul>
<hr />
<h1><strong>五、完整代码（面试标准写法）</strong></h1>
<pre><code class="language-python">class Solution:
    def merge(self, intervals: list[list[int]]) -&gt; list[list[int]]:
        if not intervals:
            return []

        # 1️⃣ 先按起点排序
        intervals.sort(key=lambda x: x[0])

        res = []
        cur_start, cur_end = intervals[0]

        # 2️⃣ 线性扫描
        for start, end in intervals[1:]:
            if start &lt;= cur_end:
                # 重叠 → 扩展右边界
                cur_end = max(cur_end, end)
            else:
                # 不重叠 → 保存当前区间
                res.append([cur_start, cur_end])
                cur_start, cur_end = start, end

        # 3️⃣ 最后一个别忘记加入
        res.append([cur_start, cur_end])

        return res
</code></pre>
<hr />
<h1><strong>六、用示例走一遍</strong></h1>
<p>输入：</p>
<pre><code>[[1,3],[2,6],[8,10],[15,18]]
</code></pre>
<p>排序后：</p>
<pre><code>[[1,3],[2,6],[8,10],[15,18]]
</code></pre>
<p>过程：</p>
<pre><code>[1,3] + [2,6] → [1,6]
[1,6] + [8,10] → 不重叠
[8,10] + [15,18] → 不重叠
</code></pre>
<p>输出：</p>
<pre><code>[[1,6],[8,10],[15,18]]
</code></pre>
<hr />
<h1><strong>七、时间复杂度</strong></h1>
<ul>
<li>排序：O(n log n)</li>
<li>扫描：O(n)</li>
</ul>
<p>👉 总复杂度：<strong>O(n log n)</strong></p>
<p>这是这题的最优解。</p>
<hr />
<h1><strong>八、区间题统一模型（非常重要）</strong></h1>
<p>这题是下面所有题的基础：</p>
<table>
<thead>
<tr>
<th><strong>题目</strong></th>
<th><strong>核心</strong></th>
</tr>
</thead>
<tbody>
<tr>
<td>合并区间</td>
<td>排序 + 合并</td>
</tr>
<tr>
<td>插入区间</td>
<td>分类 + 合并</td>
</tr>
<tr>
<td>区间交集</td>
<td>双指针</td>
</tr>
<tr>
<td>会议室问题</td>
<td>排序比较</td>
</tr>
<tr>
<td>覆盖区间</td>
<td>贪心</td>
</tr>
</tbody></table><p>👉 <strong>区间问题 = 排序 + 贪心</strong></p>
<hr />
<h1><strong>九、面试一句话总结（可以背）</strong></h1>
<blockquote>
<p>先按区间起点排序，然后线性遍历区间，如果当前区间与上一个区间重叠，则合并右边界，否则加入结果列表，时间复杂度 O(n log n)。</p>
</blockquote>
<hr />
<h1><strong>十、常见面试追问</strong></h1>
<h3><strong>Q：为什么可以贪心？</strong></h3>
<p>因为：</p>
<blockquote>
<p>排序后，局部最优（尽量扩展当前区间）一定是全局最优。</p>
</blockquote>
<hr />
<h3><strong>Q：如果输入是流式数据怎么办？</strong></h3>
<p>不能排序 → 要用平衡树 / 优先队列</p>
<p>（这个是进阶）</p>
<p>可以的：这题的关键仍然是 <strong>“先排序，再线性合并”</strong>，只不过把 intervals.sort() 换成你自己手撕的排序。</p>
<p>下面给你一份<strong>面试最稳</strong>的版本：用 <strong>归并排序（Merge Sort）</strong> 手写排序（稳定、最坏 O(n log n)），然后做合并。</p>
<hr />
<h2><strong>手撕归并排序 + 合并区间（不使用 sort）</strong></h2>
<pre><code class="language-python">from typing import List

class Solution:
    def merge(self, intervals: List[List[int]]) -&gt; List[List[int]]:
        if not intervals:
            return []

        # 1) 手撕排序：按 start 升序；start 相同按 end 升序
        intervals = self.merge_sort(intervals)

        # 2) 合并
        res = []
        cur_s, cur_e = intervals[0]
        for s, e in intervals[1:]:
            if s &lt;= cur_e:                 # 重叠（题目把 [1,4] 和 [4,5] 也算重叠）
                cur_e = max(cur_e, e)
            else:
                res.append([cur_s, cur_e])
                cur_s, cur_e = s, e
        res.append([cur_s, cur_e])
        return res

    def merge_sort(self, arr: List[List[int]]) -&gt; List[List[int]]:
        n = len(arr)
        if n &lt;= 1:
            return arr

        mid = n // 2
        left = self.merge_sort(arr[:mid])
        right = self.merge_sort(arr[mid:])

        return self.merge_two(left, right)

    def merge_two(self, a: List[List[int]], b: List[List[int]]) -&gt; List[List[int]]:
        i = j = 0
        out = []

        # 比较规则：先比 start，再比 end
        while i &lt; len(a) and j &lt; len(b):
            if a[i][0] &lt; b[j][0] or (a[i][0] == b[j][0] and a[i][1] &lt;= b[j][1]):
                out.append(a[i])
                i += 1
            else:
                out.append(b[j])
                j += 1

        # 收尾
        if i &lt; len(a):
            out.extend(a[i:])
        if j &lt; len(b):
            out.extend(b[j:])

        return out
</code></pre>
<hr />
<h2><strong>复杂度</strong></h2>
<ul>
<li>排序：O(n log n)（归并排序最坏也成立）</li>
<li>合并：O(n)</li>
<li>总：O(n log n)，空间 O(n)（归并的临时数组）</li>
</ul>
]]></content><link href="https://github.com/lihe/MyLeetcode/issues/26"/><category term="数组"/><published>2026-03-04T13:08:32+00:00</published></entry><entry><id>https://github.com/lihe/MyLeetcode/issues/25</id><title>Leetcode 1：两数之和</title><updated>2026-03-11T13:58:18.919590+00:00</updated><content type="html"><![CDATA[<p><a href="https://leetcode.cn/problems/two-sum/">1. 两数之和</a></p>
<p>这是<strong>哈希表思想的“第一性原理题”</strong>，也是面试里<strong>最常被当作“判断你是否会用哈希”的题</strong>。</p>
<p>我按 <strong>暴力 → 优化思路 → 标准解 → 面试话术</strong> 来讲，一步到位。</p>
<hr />
<h2><strong>一、为什么暴力是 O(n²)？</strong></h2>
<p>最直接的想法：</p>
<pre><code>枚举每一个 nums[i]
  再枚举 nums[j]（j &gt; i）
    看 nums[i] + nums[j] 是否等于 target
</code></pre>
<ul>
<li>两层循环</li>
<li>时间复杂度：<strong>O(n²)</strong></li>
</ul>
<p>👉 面试官问的“进阶”，就是：</p>
<p><strong>能不能把第二层循环干掉？</strong></p>
<hr />
<h2><strong>二、核心优化思想</strong></h2>
<blockquote>
<p><strong>我在找 nums[i] + nums[j] = target，</strong></p>
</blockquote>
<blockquote>
<p><strong>等价于：</strong></p>
</blockquote>
<blockquote>
<p><strong>我在找 nums[j] = target - nums[i]</strong></p>
</blockquote>
<p>所以问题变成：</p>
<blockquote>
<p><strong>我之前见没见过这个“差值”？</strong></p>
</blockquote>
<p>这正是 <strong>哈希表擅长的事情</strong>。</p>
<hr />
<h2><strong>三、哈希表解法</strong></h2>
<h3><strong>思路</strong></h3>
<ul>
<li>
<p>从左到右遍历数组</p>
</li>
<li>
<p>对当前数 x = nums[i]</p>
</li>
<li>
<p>计算需要的另一个数：need = target - x</p>
</li>
<li>
<p>如果 need 已经在哈希表里：</p>
<ul>
<li>说明之前某个位置 j 满足</li>
<li>直接返回 [j, i]</li>
</ul>
</li>
<li>
<p>否则：</p>
<ul>
<li>把当前数和下标存进哈希表</li>
</ul>
</li>
</ul>
<hr />
<h2><strong>四、标准 Python 实现（面试最稳）</strong></h2>
<pre><code class="language-python">class Solution:
    def twoSum(self, nums: list[int], target: int) -&gt; list[int]:
        mp = {}  # value -&gt; index

        for i, x in enumerate(nums):
            need = target - x
            if need in mp:
                return [mp[need], i]
            mp[x] = i
</code></pre>
<hr />
<h2><strong>五、用示例走一遍</strong></h2>
<h3><strong>示例 1</strong></h3>
<pre><code>nums = [2,7,11,15]
target = 9
</code></pre>
<table>
<thead>
<tr>
<th><strong>i</strong></th>
<th><strong>x</strong></th>
<th><strong>need</strong></th>
<th><strong>哈希表</strong></th>
<th><strong>动作</strong></th>
</tr>
</thead>
<tbody>
<tr>
<td>0</td>
<td>2</td>
<td>7</td>
<td>{}</td>
<td>存 2:0</td>
</tr>
<tr>
<td>1</td>
<td>7</td>
<td>2</td>
<td>{2:0}</td>
<td>找到 → 返回 [0,1]</td>
</tr>
</tbody></table><hr />
<h2><strong>六、为什么不会用到“同一个元素两次”？</strong></h2>
<p>因为：</p>
<ul>
<li>我是<strong>先查，再存</strong></li>
<li>当前元素 x 只有在<strong>下一轮</strong>才可能被用到</li>
<li>不可能和自己配对</li>
</ul>
<p>👉 这是很多人忽略但面试官很喜欢的点</p>
<hr />
<h2><strong>七、复杂度分析</strong></h2>
<ul>
<li>时间复杂度：<strong>O(n)</strong></li>
<li>空间复杂度：<strong>O(n)</strong>（哈希表）</li>
</ul>
<hr />
<h2><strong>八、面试一句话总结</strong></h2>
<blockquote>
<p>使用哈希表记录已经遍历过的数字及其下标，在遍历当前元素时查找是否存在 target - nums[i]，若存在则直接返回对应下标，从而将时间复杂度优化到 O(n)。</p>
</blockquote>
<hr />
<h2><strong>九、这题在“算法体系”里的地位</strong></h2>
<p>你已经无形中学会了一个<strong>通用模型</strong>：</p>
<table>
<thead>
<tr>
<th><strong>问题类型</strong></th>
<th><strong>核心技巧</strong></th>
</tr>
</thead>
<tbody>
<tr>
<td>两数之和</td>
<td>哈希表查差值</td>
</tr>
<tr>
<td>字母异位词</td>
<td>哈希表计数</td>
</tr>
<tr>
<td>子数组和</td>
<td>前缀和 + 哈希</td>
</tr>
<tr>
<td>滑动窗口</td>
<td>哈希 + 双指针</td>
</tr>
</tbody></table><p>👉 <strong>哈希表 = 用空间换时间的王者</strong></p>
<hr />
<h2><strong>十、常见追问</strong></h2>
<h3><strong>Q：如果有多个答案怎么办？</strong></h3>
<blockquote>
<p>本题保证只有一个；否则可以存多个下标或返回所有组合。</p>
</blockquote>
<h3><strong>Q：如果要求返回值而不是下标？</strong></h3>
<blockquote>
<p>逻辑不变，只是返回内容不同。</p>
</blockquote>
<h1>手动实现哈希表怎么做，哈希函数用什么好</h1>
<hr />
<h1><strong>一、先给面试官一个“总览答案”</strong></h1>
<p>你可以这样开口：</p>
<blockquote>
<p>手动实现哈希表一般包括四部分：</p>
</blockquote>
<blockquote>
<p>哈希函数、桶数组、冲突解决策略，以及扩容机制。</p>
</blockquote>
<blockquote>
<p>常见冲突解决方式是拉链法或开放寻址法；</p>
</blockquote>
<blockquote>
<p>哈希函数需要满足均匀分布、计算简单，整数可以直接取模，字符串可以用多项式 rolling hash。</p>
</blockquote>
<hr />
<h1><strong>二、哈希表最小实现结构</strong></h1>
<pre><code>key → hash(key) → index → bucket → value
</code></pre>
<p>代码结构一定是：</p>
<pre><code class="language-python">class HashMap:
    def __init__(self):
        self.capacity = ...
        self.buckets = [...]
</code></pre>
<hr />
<h1><strong>三、核心 1：哈希函数怎么选？</strong></h1>
<h2><strong>🎯 哈希函数的目标</strong></h2>
<p>你要说清楚这一句：</p>
<blockquote>
<p>哈希函数的目标是让 key 尽量均匀分布在桶中，减少冲突。</p>
</blockquote>
<hr />
<h2><strong>1️⃣ 整数 key</strong></h2>
<h3><strong>面试推荐答案</strong></h3>
<pre><code>hash = key % capacity
</code></pre>
<p>你可以补一句：</p>
<blockquote>
<p>capacity 通常选为 <strong>质数</strong>，可以减少模式冲突。</p>
</blockquote>
<p>这是<strong>加分点</strong>。</p>
<hr />
<h2><strong>2️⃣ 字符串 key</strong></h2>
<h3><strong>标准做法：</strong></h3>
<h3><strong>多项式 Rolling Hash</strong></h3>
<p>你可以直接说这个名字。</p>
<h3><strong>公式</strong></h3>
<pre><code>hash = (hash * base + char) % capacity
</code></pre>
<h3><strong>示例代码</strong></h3>
<pre><code class="language-python">def hash_string(s):
    h = 0
    base = 31
    for ch in s:
        h = (h * base + ord(ch)) % capacity
    return h
</code></pre>
<h3><strong>为什么用 31 / 131 / 13331？</strong></h3>
<p>你可以这样答：</p>
<blockquote>
<p>经验上这些质数可以让字符串分布更均匀，且计算成本低。</p>
</blockquote>
<hr />
<h2><strong>⚠️ 绝对不要说的雷区</strong></h2>
<p>❌ “我用 Python 的 hash()”</p>
<p>❌ “我随便取一个随机数”</p>
<p>网易二面会直接追问你：</p>
<p><strong>那冲突怎么分析？稳定性怎么保证？</strong></p>
<hr />
<h1><strong>四、核心 2：冲突怎么解决？</strong></h1>
<p>你必须能说出 <strong>两种</strong>。</p>
<hr />
<h2><strong>方案一：拉链法（最稳，最常用）</strong></h2>
<h3><strong>思想</strong></h3>
<pre><code>bucket[index] = 一个链表 / 数组
</code></pre>
<p>所有 hash 到同一 index 的元素，都挂在这个桶后面。</p>
<h3><strong>优点（你可以说）</strong></h3>
<ul>
<li>实现简单</li>
<li>扩容方便</li>
<li>工程中最常用（Java HashMap）</li>
</ul>
<h3><strong>简化代码示意</strong></h3>
<pre><code class="language-python">self.buckets = [[] for _ in range(capacity)]

def put(key, val):
    idx = hash(key)
    for k, v in buckets[idx]:
        if k == key:
            v = val
            return
    buckets[idx].append((key, val))
</code></pre>
<hr />
<h2><strong>方案二：开放寻址法（进阶）</strong></h2>
<h3><strong>思想</strong></h3>
<blockquote>
<p>冲突了就<strong>找下一个空位</strong></p>
</blockquote>
<pre><code>idx = (hash + i) % capacity
</code></pre>
<h3><strong>常见策略</strong></h3>
<ul>
<li>线性探测</li>
<li>二次探测</li>
<li>双重哈希</li>
</ul>
<h3><strong>你可以补一句（加分）</strong></h3>
<blockquote>
<p>开放寻址对删除和负载因子更敏感，实现复杂度更高。</p>
</blockquote>
<hr />
<h1><strong>五、核心 3：扩容机制</strong></h1>
<p>你可以这样说：</p>
<blockquote>
<p>当负载因子（size / capacity）超过阈值（比如 0.75），就进行扩容，一般扩容为原来的 2 倍，并重新 rehash 所有元素。</p>
</blockquote>
<p>这是 <strong>Java HashMap 标准设计</strong>。</p>
<hr />
<h1><strong>六、一个“最小可运行”的手写哈希表（示意级）</strong></h1>
<pre><code class="language-python">class MyHashMap:
    def __init__(self, capacity=101):
        self.cap = capacity
        self.buckets = [[] for _ in range(capacity)]

    def _hash(self, key):
        return key % self.cap

    def put(self, key, value):
        idx = self._hash(key)
        for i, (k, v) in enumerate(self.buckets[idx]):
            if k == key:
                self.buckets[idx][i] = (key, value)
                return
        self.buckets[idx].append((key, value))

    def get(self, key):
        idx = self._hash(key)
        for k, v in self.buckets[idx]:
            if k == key:
                return v
        return -1
</code></pre>
<hr />
<h1><strong>七、面试官追问 &amp; 标准回答</strong></h1>
<h3><strong>Q1：哈希函数越复杂越好吗？</strong></h3>
<p>❌ 不一定，关键是 <strong>均匀 + 快</strong></p>
<hr />
<h3><strong>Q2：最坏情况下复杂度是多少？</strong></h3>
<blockquote>
<p>冲突严重时，拉链法退化为 O(n)</p>
</blockquote>
<hr />
<h3><strong>Q3：为什么容量常选质数？</strong></h3>
<blockquote>
<p>可以减少取模后的周期性冲突</p>
</blockquote>
<hr />
<h1><strong>八、终极一句话总结（你可以背）</strong></h1>
<blockquote>
<p>手动实现哈希表需要设计哈希函数、桶结构、冲突解决策略和扩容机制。整数 key 通常取模，字符串常用多项式 rolling hash；冲突可以用拉链法或开放寻址法，当负载因子过高时进行 rehash 扩容。</p>
</blockquote>
<hr />
]]></content><link href="https://github.com/lihe/MyLeetcode/issues/25"/><category term="哈希表"/><published>2026-01-31T04:05:05+00:00</published></entry><entry><id>https://github.com/lihe/MyLeetcode/issues/24</id><title>Leetcode 202：快乐数</title><updated>2026-03-11T13:58:19.072289+00:00</updated><content type="html"><![CDATA[<p><a href="https://leetcode.cn/problems/happy-number/">202. 快乐数</a></p>
<p>对一个数 n 不断做这件事：</p>
<blockquote>
<p>👉 把 n 的每一位数字拆出来</p>
</blockquote>
<blockquote>
<p>👉 求每一位数字的平方和</p>
</blockquote>
<blockquote>
<p>👉 得到一个新数，继续重复</p>
</blockquote>
<p>结果只有两种可能：</p>
<ul>
<li>最终变成 1 → <strong>快乐数</strong></li>
<li>永远绕圈 → <strong>不是快乐数</strong></li>
</ul>
<hr />
<blockquote>
<p>如果某一步的结果<strong>重复出现</strong>，那后面一定会<strong>进入死循环</strong></p>
</blockquote>
<p>所以本质问题变成：</p>
<blockquote>
<p><strong>如何检测“重复状态”？</strong></p>
</blockquote>
<hr />
<h1><strong>方法一：哈希表</strong></h1>
<h2><strong>思路</strong></h2>
<ul>
<li>
<p>用一个 set 记录 <strong>已经出现过的数</strong></p>
</li>
<li>
<p>每次算平方和：</p>
<ul>
<li>如果等于 1 → 返回 True</li>
<li>如果已经在 set 里 → 说明循环 → False</li>
</ul>
</li>
</ul>
<hr />
<h2><strong>辅助函数：算“各位平方和”</strong></h2>
<pre><code class="language-python">def get_next(n):
    total = 0
    while n &gt; 0:
        digit = n % 10
        total += digit * digit
        n //= 10
    return total
</code></pre>
<hr />
<h2><strong>完整代码（哈希表版）</strong></h2>
<pre><code class="language-python">class Solution:
    def isHappy(self, n: int) -&gt; bool:
        seen = set()
        
        while n != 1:
            if n in seen:
                return False
            seen.add(n)
            n = self.get_next(n)
        
        return True

    def get_next(self, n):
        total = 0
        while n &gt; 0:
            digit = n % 10
            total += digit * digit
            n //= 10
        return total
</code></pre>
<hr />
<h2><strong>复杂度</strong></h2>
<ul>
<li>时间：O(循环长度)（很小，几十步内）</li>
<li>空间：O(循环长度)</li>
</ul>
<hr />
<h1><strong>方法二：快慢指针</strong></h1>
<h2><strong>核心思想（一句话）</strong></h2>
<blockquote>
<p>如果存在循环，</p>
</blockquote>
<blockquote>
<p><strong>快慢指针一定会在环里相遇</strong></p>
</blockquote>
<p>这和「判断链表是否有环」是<strong>同一个模型</strong>。</p>
<hr />
<h2><strong>指针定义</strong></h2>
<ul>
<li>slow：每次走一步</li>
<li>fast：每次走两步</li>
</ul>
<hr />
<h2><strong>规则</strong></h2>
<ul>
<li>如果 fast 或 slow 变成 1 → 快乐数</li>
<li>如果 slow == fast 且不是 1 → 有环 → 非快乐数</li>
</ul>
<hr />
<h2><strong>完整代码（快慢指针版，O(1) 空间）</strong></h2>
<pre><code class="language-python">class Solution:
    def isHappy(self, n: int) -&gt; bool:
        slow = n
        fast = self.get_next(n)

        while fast != 1 and slow != fast:
            slow = self.get_next(slow)
            fast = self.get_next(self.get_next(fast))

        return fast == 1

    def get_next(self, n):
        total = 0
        while n &gt; 0:
            digit = n % 10
            total += digit * digit
            n //= 10
        return total
</code></pre>
<hr />
<h2><strong>为什么一定能用快慢指针？</strong></h2>
<p>因为：</p>
<ul>
<li>
<p>数字平方和函数是 <strong>确定性的</strong></p>
</li>
<li>
<p>每个数只会指向一个“下一个数”</p>
</li>
<li>
<p>整个过程本质是一个 <strong>函数图</strong></p>
</li>
<li>
<p>一定会：</p>
<ul>
<li>到 1</li>
<li>或进入一个环（如 4 → 16 → 37 → 58 → 89 → 145 → 42 → 20 → 4）</li>
</ul>
</li>
</ul>
<hr />
<h2><strong>三、面试如何选择写法？</strong></h2>
<table>
<thead>
<tr>
<th><strong>场景</strong></th>
<th><strong>推荐</strong></th>
</tr>
</thead>
<tbody>
<tr>
<td>一般面试</td>
<td>哈希表（清晰稳妥）</td>
</tr>
<tr>
<td>要求 O(1) 空间</td>
<td>快慢指针</td>
</tr>
<tr>
<td>追问“还能优化吗”</td>
<td>快慢指针</td>
</tr>
</tbody></table><hr />
<h2><strong>四、面试一句话总结</strong></h2>
<blockquote>
<p>快乐数问题本质是循环检测问题，可以使用哈希表记录中间结果判断是否出现重复，或者使用快慢指针在 O(1) 空间内检测是否存在循环。</p>
</blockquote>
]]></content><link href="https://github.com/lihe/MyLeetcode/issues/24"/><category term="哈希表"/><published>2026-01-25T15:24:20+00:00</published></entry><entry><id>https://github.com/lihe/MyLeetcode/issues/23</id><title>Leetcode 242: 有效的字母异位词</title><updated>2026-03-11T13:58:19.224395+00:00</updated><content type="html"><![CDATA[<p><a href="https://leetcode.cn/problems/valid-anagram/">242. 有效的字母异位词</a></p>
<h2><strong>一、核心思想（一句话版）</strong></h2>
<blockquote>
<p><strong>两个字符串是字母异位词 ⇔ 每个字符出现的次数完全一样</strong></p>
</blockquote>
<p>顺序不重要，<strong>次数才重要</strong>。</p>
<hr />
<h2><strong>二、最优解（仅小写字母）——计数数组 O(n)</strong></h2>
<p>因为题目明确说：</p>
<blockquote>
<p>仅包含 a ~ z（26 个）</p>
</blockquote>
<h3><strong>思路</strong></h3>
<ul>
<li>用长度为 26 的数组</li>
<li>s 中字符出现：+1</li>
<li>t 中字符出现：-1</li>
<li>最后只要有非 0 → ❌</li>
</ul>
<hr />
<h3><strong>Python 实现（面试最推荐）</strong></h3>
<pre><code class="language-python">class Solution:
    def isAnagram(self, s: str, t: str) -&gt; bool:
        if len(s) != len(t):
            return False

        cnt = [0] * 26

        for ch in s:
            cnt[ord(ch) - ord(&#x27;a&#x27;)] += 1

        for ch in t:
            cnt[ord(ch) - ord(&#x27;a&#x27;)] -= 1

        return all(c == 0 for c in cnt)
</code></pre>
<hr />
<h3><strong>复杂度</strong></h3>
<ul>
<li>时间：<strong>O(n)</strong></li>
<li>空间：<strong>O(1)</strong>（固定 26）</li>
</ul>
<p>👉 这是<strong>这道题的最优解</strong></p>
<hr />
<h2><strong>三、为什么不能用排序？</strong></h2>
<pre><code>sorted(s) == sorted(t)
</code></pre>
<ul>
<li>时间复杂度：O(n log n)</li>
<li>虽然能过，但<strong>不是最优</strong></li>
</ul>
<p>面试官更想看到你用 <strong>计数思想</strong>。</p>
<hr />
<h2><strong>四、进阶：如果包含 Unicode 字符怎么办？</strong></h2>
<p>题目进阶问得非常好，这是<strong>真实工程问题</strong>。</p>
<h3><strong>问题在哪里？</strong></h3>
<ul>
<li>Unicode 字符集非常大</li>
<li>不能再用固定长度数组（比如 26）</li>
</ul>
<hr />
<h2><strong>五、Unicode 情况的正确解法 —— 哈希表（dict）</strong></h2>
<h3><strong>思路</strong></h3>
<ul>
<li>用 dict 统计每个字符的出现次数</li>
<li>适用于任何字符集（中文、emoji、日文……）</li>
</ul>
<hr />
<h3><strong>Python 实现（通用版）</strong></h3>
<pre><code class="language-python">class Solution:
    def isAnagram(self, s: str, t: str) -&gt; bool:
        if len(s) != len(t):
            return False

        freq = {}

        for ch in s:
            freq[ch] = freq.get(ch, 0) + 1

        for ch in t:
            if ch not in freq:
                return False
            freq[ch] -= 1
            if freq[ch] &lt; 0:
                return False

        return True
</code></pre>
<hr />
<h3><strong>复杂度</strong></h3>
<ul>
<li>时间：<strong>O(n)</strong></li>
<li>空间：<strong>O(n)</strong>（取决于不同字符的数量）</li>
</ul>
<hr />
<h2><strong>六、Python 的“偷懒神器”</strong></h2>
<pre><code>from collections import Counter

Counter(s) == Counter(t)
</code></pre>
<p>✔ 可读性强</p>
<p>❌ 面试中<strong>不推荐直接写</strong>（会被追问原理）</p>
<hr />
<h2><strong>七、面试一句话总结</strong></h2>
<blockquote>
<p>对于仅包含小写字母的情况，可以使用长度为 26 的计数数组统计字符频次；对于包含 Unicode 字符的情况，使用哈希表统计每个字符出现次数即可判断是否为字母异位词。</p>
</blockquote>
<hr />
]]></content><link href="https://github.com/lihe/MyLeetcode/issues/23"/><category term="哈希表"/><published>2026-01-22T15:19:24+00:00</published></entry><entry><id>https://github.com/lihe/MyLeetcode/issues/22</id><title>Leetcode 239：滑动窗口最大值</title><updated>2026-03-11T13:58:19.354507+00:00</updated><content type="html"><![CDATA[<p><a href="https://leetcode.cn/problems/sliding-window-maximum/">239. 滑动窗口最大值</a></p>
<h2><strong>一、为什么暴力不行？</strong></h2>
<p>每个窗口都算最大值：</p>
<ul>
<li>一个窗口大小 k</li>
<li>每次找最大值要 O(k)</li>
<li>一共 n 个窗口</li>
</ul>
<p>👉 总复杂度：O(nk)</p>
<p>当 n = 10^5、k = 10^5，直接 <strong>TLE</strong></p>
<hr />
<h2><strong>二、核心思想</strong></h2>
<blockquote>
<p><strong>用一个“单调递减的双端队列”，</strong></p>
</blockquote>
<blockquote>
<p><strong>队头永远是当前窗口的最大值</strong></p>
</blockquote>
<p>我们存的不是值，而是 <strong>下标 index</strong>。</p>
<hr />
<h2><strong>三、这个队列长什么样？</strong></h2>
<p>队列里满足两个条件：</p>
<h3><strong>条件 1️⃣：从队头到队尾，值是</strong></h3>
<h3><strong>递减的</strong></h3>
<pre><code>nums[queue[0]] ≥ nums[queue[1]] ≥ nums[queue[2]] ...
</code></pre>
<h3><strong>条件 2️⃣：队头的下标，一定在当前窗口内</strong></h3>
<hr />
<h2><strong>四、为什么能这样做？</strong></h2>
<h3><strong>❓ 为什么比新数小的可以直接删？</strong></h3>
<p>假设你遇到一个新数 x：</p>
<ul>
<li>
<p>队尾元素比 x 小</p>
</li>
<li>
<p>那它：</p>
<ul>
<li>更早进入窗口</li>
<li>数值还更小</li>
<li>未来永远不可能成为最大值</li>
</ul>
</li>
</ul>
<p>👉 <strong>直接淘汰</strong></p>
<hr />
<h2><strong>五、规则总结</strong></h2>
<p>对每个 nums[i]：</p>
<h3><strong>规则 1️⃣：维护单调性（从队尾删）</strong></h3>
<pre><code>while queue 非空 且 nums[queue[-1]] &lt;= nums[i]:
    queue.pop()
</code></pre>
<h3><strong>规则 2️⃣：新元素入队（下标）</strong></h3>
<pre><code>queue.append(i)
</code></pre>
<h3><strong>规则 3️⃣：窗口左边界移出时，从队头删</strong></h3>
<pre><code>if queue[0] &lt; i - k + 1:
    queue.popleft()
</code></pre>
<h3><strong>规则 4️⃣：窗口形成后，记录最大值</strong></h3>
<pre><code>nums[queue[0]]
</code></pre>
<hr />
<h2><strong>六、动画式走一遍示例（非常关键）</strong></h2>
<pre><code>nums = [1,3,-1,-3,5,3,6,7]
k = 3
</code></pre>
<p>我们只看队列里<strong>对应的值</strong>（实际存的是下标）。</p>
<hr />
<h3><strong>i = 0，nums[i]=1</strong></h3>
<pre><code>queue = [1]
</code></pre>
<hr />
<h3><strong>i = 1，nums[i]=3</strong></h3>
<ul>
<li>3 &gt; 1 → 1 出局</li>
</ul>
<pre><code>queue = [3]
</code></pre>
<hr />
<h3><strong>i = 2，nums[i]=-1</strong></h3>
<pre><code>queue = [3, -1]
</code></pre>
<p>窗口形成（i &gt;= 2）</p>
<p>最大值 = <strong>3</strong></p>
<hr />
<h3><strong>i = 3，nums[i]=-3</strong></h3>
<pre><code>queue = [3, -1, -3]
</code></pre>
<p>窗口左边界是 1，3 还在</p>
<p>最大值 = <strong>3</strong></p>
<hr />
<h3><strong>i = 4，nums[i]=5</strong></h3>
<ul>
<li>5 &gt; -3 → -3 出</li>
<li>5 &gt; -1 → -1 出</li>
<li>5 &gt; 3 → 3 出</li>
</ul>
<pre><code>queue = [5]
</code></pre>
<p>最大值 = <strong>5</strong></p>
<hr />
<p>后面同理，最终得到：</p>
<pre><code>[3, 3, 5, 5, 6, 7]
</code></pre>
<hr />
<h2><strong>七、标准 Python 代码（必须会写）</strong></h2>
<pre><code class="language-python">from collections import deque

class Solution:
    def maxSlidingWindow(self, nums: list[int], k: int) -&gt; list[int]:
        q = deque()   # 存下标
        res = []

        for i in range(len(nums)):
            # 1️⃣ 维护单调递减
            while q and nums[q[-1]] &lt;= nums[i]:
                q.pop()

            q.append(i)

            # 2️⃣ 移除窗口外的元素
            if q[0] &lt; i - k + 1:
                q.popleft()

            # 3️⃣ 记录窗口最大值
            if i &gt;= k - 1:
                res.append(nums[q[0]])

        return res
</code></pre>
<hr />
<h2><strong>八、复杂度分析</strong></h2>
<ul>
<li>
<p><strong>时间复杂度：O(n)</strong></p>
<ul>
<li>每个元素最多进队一次、出队一次</li>
</ul>
</li>
<li>
<p><strong>空间复杂度：O(k)</strong></p>
</li>
</ul>
<hr />
<h2><strong>九、面试一句话总结</strong></h2>
<blockquote>
<p>使用单调递减队列维护窗口内可能成为最大值的元素下标，队头始终为当前窗口最大值。每个元素只进出队列一次，因此时间复杂度为 O(n)。</p>
</blockquote>
]]></content><link href="https://github.com/lihe/MyLeetcode/issues/22"/><category term="链表"/><published>2026-01-19T15:07:39+00:00</published></entry><entry><id>https://github.com/lihe/MyLeetcode/issues/21</id><title>Leetcode 150： 逆波兰表达式求值</title><updated>2026-03-11T13:58:19.522466+00:00</updated><content type="html"><![CDATA[<p><a href="https://leetcode.cn/problems/evaluate-reverse-polish-notation/">150. 逆波兰表达式求值</a></p>
<h1><strong>一、一句话核心思想</strong></h1>
<blockquote>
<p><strong>遇到数字入栈，遇到运算符就弹出两个数计算，再把结果压回栈中。</strong></p>
</blockquote>
<p>因为 <strong>逆波兰表达式（后缀表达式）本身已经把“运算顺序”编码进顺序里了</strong>，</p>
<p>所以：<strong>不需要括号、不需要优先级判断</strong>。</p>
<hr />
<h1><strong>二、为什么一定要用栈？</strong></h1>
<p>逆波兰表达式的结构是：</p>
<pre><code>操作数 操作数 运算符
</code></pre>
<p>这意味着：</p>
<ul>
<li>运算符 <strong>总是作用在它前面的两个“最近结果”上</strong></li>
<li>最近的结果 → <strong>后进先出</strong></li>
<li>👉 天然符合 <strong>栈（LIFO）</strong></li>
</ul>
<hr />
<h1><strong>三、动画式走一遍示例 1</strong></h1>
<h3><strong>示例 1</strong></h3>
<pre><code>tokens = [&quot;2&quot;,&quot;1&quot;,&quot;+&quot;,&quot;3&quot;,&quot;*&quot;]
</code></pre>
<h3><strong>初始化</strong></h3>
<pre><code>stack = []
</code></pre>
<h3><strong>逐个处理</strong></h3>
<h4><strong>1️⃣ “2”（数字）</strong></h4>
<pre><code>stack = [2]
</code></pre>
<h4><strong>2️⃣ “1”（数字）</strong></h4>
<pre><code>stack = [2, 1]
</code></pre>
<h4><strong>3️⃣ “+”（运算符）</strong></h4>
<ul>
<li>弹出两个数：1 和 2</li>
<li>计算：2 + 1 = 3</li>
<li>压回栈</li>
</ul>
<pre><code>stack = [3]
</code></pre>
<h4><strong>4️⃣ “3”（数字）</strong></h4>
<pre><code>stack = [3, 3]
</code></pre>
<h4><strong>5️⃣ “*”（运算符）</strong></h4>
<ul>
<li>弹出：3 和 3</li>
<li>计算：3 * 3 = 9</li>
<li>压回</li>
</ul>
<pre><code>stack = [9]
</code></pre>
<h3><strong>结束</strong></h3>
<p>栈中唯一的数，就是答案 👉 <strong>9</strong></p>
<hr />
<h1><strong>四、特别容易踩的坑</strong></h1>
<h2><strong>1️⃣ 减法和除法的顺序不能反</strong></h2>
<pre><code>b = stack.pop()
a = stack.pop()
result = a - b   # 不是 b - a
</code></pre>
<p>同理：</p>
<pre><code>result = a / b
</code></pre>
<hr />
<h2><strong>2️⃣ Python 的除法问题（重点）</strong></h2>
<p>题目要求：</p>
<blockquote>
<p><strong>向 0 截断</strong></p>
</blockquote>
<p>Python 的 // 是 <strong>向下取整</strong>，不符合要求！</p>
<h3><strong>❌ 错误写法</strong></h3>
<pre><code>a // b
</code></pre>
<h3><strong>✅ 正确写法</strong></h3>
<pre><code>int(a / b)
</code></pre>
<p>因为 int() 是 <strong>向 0 截断</strong></p>
<hr />
<h1><strong>五、标准 Python 解法</strong></h1>
<pre><code class="language-python">class Solution:
    def evalRPN(self, tokens: list[str]) -&gt; int:
        stack = []
        
        for token in tokens:
            if token not in {&quot;+&quot;, &quot;-&quot;, &quot;*&quot;, &quot;/&quot;}:
                stack.append(int(token))
            else:
                b = stack.pop()
                a = stack.pop()
                
                if token == &quot;+&quot;:
                    stack.append(a + b)
                elif token == &quot;-&quot;:
                    stack.append(a - b)
                elif token == &quot;*&quot;:
                    stack.append(a * b)
                else:  # &quot;/&quot;
                    stack.append(int(a / b))  # 向 0 截断
        
        return stack[0]
</code></pre>
<hr />
<h1><strong>六、复杂度分析</strong></h1>
<ul>
<li>
<p>时间复杂度：<strong>O(n)</strong></p>
<p>每个 token 只处理一次</p>
</li>
<li>
<p>空间复杂度：<strong>O(n)</strong></p>
<p>最坏情况下全是数字</p>
</li>
</ul>
<hr />
<h1><strong>七、面试一句话总结</strong></h1>
<blockquote>
<p>使用栈遍历逆波兰表达式，遇到数字入栈，遇到运算符则弹出栈顶两个元素进行计算，并将结果压回栈中。最终栈中剩下的元素即为表达式结果。</p>
</blockquote>
<p>好，这一题就是<strong>阿里一面/二面超级高频的「表达式求值·终极形态」</strong>。</p>
<p>我直接按 <strong>面试官视角 + 你能现场手写</strong> 的方式讲。</p>
<blockquote>
<p>题目关键词：</p>
</blockquote>
<blockquote>
<p><strong>字符串表达式 + 括号 + 四则运算</strong></p>
</blockquote>
<h1><strong>二、阿里一面变种题目原型</strong></h1>
<blockquote>
<p>给定一个字符串表达式 s，包含</p>
</blockquote>
<blockquote>
<p>+ - * / ( ) 和整数，</p>
</blockquote>
<blockquote>
<p>返回表达式的计算结果（向 0 取整）。</p>
</blockquote>
<p>例如：</p>
<pre><code>s = &quot;(1+(4+5+2)-3)+(6+8)&quot;
</code></pre>
<p>答案：23</p>
<hr />
<h1><strong>三、核心解法总览</strong></h1>
<p>阿里一面<strong>最稳妥的方案</strong>是：</p>
<blockquote>
<p>✅ <strong>两个栈</strong></p>
</blockquote>
<ul>
<li>
<blockquote>
<p>数字栈 nums</p>
</blockquote>
</li>
<li>
<blockquote>
<p>运算符栈 ops</p>
</blockquote>
</li>
</ul>
<p>并遵守三条规则。</p>
<hr />
<h1><strong>四、三条“铁律”</strong></h1>
<h3><strong>规则 1️⃣</strong></h3>
<p><strong>数字 → 直接进 nums 栈</strong></p>
<hr />
<h3><strong>规则 2️⃣</strong></h3>
<p><strong>运算符 → 看优先级，能算就算</strong></p>
<pre><code>当前运算符 &lt;= 栈顶运算符优先级
→ 先计算
</code></pre>
<hr />
<h3><strong>规则 3️⃣（最关键）</strong></h3>
<p><strong>遇到右括号 ) → 一直算到左括号 (</strong></p>
<hr />
<h1><strong>五、运算符优先级表</strong></h1>
<pre><code>* /  优先级高
+ -  优先级低
(    最低（阻断计算）
</code></pre>
<hr />
<h1><strong>六、完整动画式流程</strong></h1>
<p>我们用这个例子：</p>
<pre><code>s = &quot;(1+(4+5+2)-3)+(6+8)&quot;
</code></pre>
<hr />
<h2><strong>🎬 初始化</strong></h2>
<pre><code>nums = []
ops  = []
</code></pre>
<hr />
<h2><strong>🎬 读到 ‘(’</strong></h2>
<pre><code>ops = [&#x27;(&#x27;]
</code></pre>
<blockquote>
<p>左括号：<strong>只进栈，不计算</strong></p>
</blockquote>
<hr />
<h2><strong>🎬 读到 ‘1’</strong></h2>
<pre><code>nums = [1]
</code></pre>
<hr />
<h2><strong>🎬 读到 ‘+’</strong></h2>
<pre><code>ops = [&#x27;(&#x27;, &#x27;+&#x27;]
</code></pre>
<hr />
<h2><strong>🎬 读到 ‘(’</strong></h2>
<pre><code>ops = [&#x27;(&#x27;, &#x27;+&#x27;, &#x27;(&#x27;]
</code></pre>
<hr />
<h2><strong>🎬 读到 ‘4 5 2’</strong></h2>
<pre><code>nums = [1, 4, 5, 2]
ops  = [&#x27;(&#x27;, &#x27;+&#x27;, &#x27;(&#x27;, &#x27;+&#x27;, &#x27;+&#x27;]
</code></pre>
<hr />
<h2><strong>🎬 读到 ‘)’</strong></h2>
<p>🔥 <strong>关键时刻</strong></p>
<p>规则：</p>
<blockquote>
<p>一直计算到 ‘(’ 为止</p>
</blockquote>
<pre><code>5 + 2 = 7
4 + 7 = 11
nums = [1, 11]
ops  = [&#x27;(&#x27;, &#x27;+&#x27;]
</code></pre>
<hr />
<h2><strong>🎬 继续读 ‘-3’</strong></h2>
<pre><code>nums = [1, 11, 3]
ops  = [&#x27;(&#x27;, &#x27;+&#x27;, &#x27;-&#x27;]
</code></pre>
<hr />
<h2><strong>🎬 再次 ‘)’</strong></h2>
<pre><code>11 - 3 = 8
1 + 8 = 9
nums = [9]
ops  = []
</code></pre>
<hr />
<h2><strong>🎬 最后 +(6+8)</strong></h2>
<pre><code>nums = [9, 6, 8]
ops  = [&#x27;+&#x27;, &#x27;(&#x27; , &#x27;+&#x27;]
</code></pre>
<p>遇到 )：</p>
<pre><code>6 + 8 = 14
</code></pre>
<p>最后：</p>
<pre><code>9 + 14 = 23
</code></pre>
<p>🎉 完成</p>
<hr />
<h1><strong>七、标准 Python 代码</strong></h1>
<pre><code class="language-python">class Solution:
    def calculate(self, s: str) -&gt; int:
        nums = []
        ops = []

        def priority(op):
            if op in (&#x27;+&#x27;, &#x27;-&#x27;):
                return 1
            if op in (&#x27;*&#x27;, &#x27;/&#x27;):
                return 2
            return 0

        def calc():
            b = nums.pop()
            a = nums.pop()
            op = ops.pop()
            if op == &#x27;+&#x27;:
                nums.append(a + b)
            elif op == &#x27;-&#x27;:
                nums.append(a - b)
            elif op == &#x27;*&#x27;:
                nums.append(a * b)
            else:
                nums.append(int(a / b))  # 向 0 取整

        i = 0
        while i &lt; len(s):
            if s[i] == &#x27; &#x27;:
                i += 1
            elif s[i].isdigit():
                num = 0
                while i &lt; len(s) and s[i].isdigit():
                    num = num * 10 + int(s[i])
                    i += 1
                nums.append(num)
            elif s[i] == &#x27;(&#x27;:
                ops.append(s[i])
                i += 1
            elif s[i] == &#x27;)&#x27;:
                while ops and ops[-1] != &#x27;(&#x27;:
                    calc()
                ops.pop()  # 弹出 &#x27;(&#x27;
                i += 1
            else:
                while ops and priority(ops[-1]) &gt;= priority(s[i]):
                    calc()
                ops.append(s[i])
                i += 1

        while ops:
            calc()

        return nums[0]
</code></pre>
<hr />
<h1><strong>八、面试常见追问</strong></h1>
<h3><strong>Q1：为什么要两个栈？</strong></h3>
<blockquote>
<p>一个栈无法同时处理 <strong>数值顺序 + 运算符优先级 + 括号结构</strong></p>
</blockquote>
<hr />
<h3><strong>Q2：能不能不用栈？</strong></h3>
<blockquote>
<p>本质还是递归，栈只是把递归<strong>显式化</strong></p>
</blockquote>
<hr />
<h3><strong>Q3：为什么括号能“阻断计算”？</strong></h3>
<blockquote>
<p>因为 ( 的优先级最低，直到遇到 )</p>
</blockquote>
<hr />
<h1><strong>九、面试一句话终极总结</strong></h1>
<blockquote>
<p>使用数字栈和运算符栈遍历字符串，数字直接入栈；运算符根据优先级进行计算；遇到右括号时计算到左括号为止，最终栈中结果即为表达式值。</p>
</blockquote>
]]></content><link href="https://github.com/lihe/MyLeetcode/issues/21"/><category term="栈与队列"/><published>2026-01-14T15:12:21+00:00</published></entry><entry><id>https://github.com/lihe/MyLeetcode/issues/20</id><title>LeetCode 232: 用栈实现队列</title><updated>2026-03-11T13:58:19.669878+00:00</updated><content type="html"><![CDATA[<p><a href="https://leetcode.cn/problems/implement-queue-using-stacks/"> LeetCode 232: 用队列实现栈</a></p>
<h1><strong>一、核心思想</strong></h1>
<blockquote>
<p>让最新入队的元素，永远排到队列的最前面。</p>
</blockquote>
<p>这样：</p>
<pre><code>队头 = 栈顶
</code></pre>
<hr />
<h1><strong>二、两个队列的角色分工</strong></h1>
<p>我们用：</p>
<ul>
<li>q1：主队列（始终保存“栈顺序”）</li>
<li>q2：辅助队列（临时倒腾用）</li>
</ul>
<p>目标结构：</p>
<pre><code>q1: [top, ..., bottom]
</code></pre>
<p>也就是说：</p>
<blockquote>
<p><strong>q1 的队头就是栈顶</strong></p>
</blockquote>
<hr />
<h1><strong>三、push(x) 的魔法</strong></h1>
<p>假设当前栈是：</p>
<pre><code>栈 = [1, 2]   (2 是栈顶)

q1 = [2, 1]
</code></pre>
<p>现在 push(3)</p>
<p>我们要让 3 成为“队头”。</p>
<h3><strong>步骤：</strong></h3>
<pre><code>1) 把 3 放进 q2
   q2 = [3]

2) 把 q1 所有元素依次倒进 q2
   q2 = [3, 2, 1]

3) 交换 q1 和 q2
   q1 = [3, 2, 1]
   q2 = []
</code></pre>
<p>🎯 现在队头 = 3 = 栈顶</p>
<hr />
<h1><strong>四、pop / top / empty</strong></h1>
<p>因为我们已经保证：</p>
<pre><code>q1.front = 栈顶
</code></pre>
<p>所以：</p>
<table>
<thead>
<tr>
<th><strong>操作</strong></th>
<th><strong>实现</strong></th>
</tr>
</thead>
<tbody>
<tr>
<td>pop</td>
<td>q1.pop_front()</td>
</tr>
<tr>
<td>top</td>
<td>q1.peek()</td>
</tr>
<tr>
<td>empty</td>
<td>len(q1)==0</td>
</tr>
</tbody></table><hr />
<h1><strong>五、完整 Python 实现（标准解）</strong></h1>
<pre><code class="language-python">from collections import deque

class MyStack:

    def __init__(self):
        self.q1 = deque()
        self.q2 = deque()

    def push(self, x: int) -&gt; None:
        # 新元素先进 q2
        self.q2.append(x)
        
        # 把 q1 全倒进 q2
        while self.q1:
            self.q2.append(self.q1.popleft())
        
        # 交换 q1 和 q2
        self.q1, self.q2 = self.q2, self.q1

    def pop(self) -&gt; int:
        return self.q1.popleft()

    def top(self) -&gt; int:
        return self.q1[0]

    def empty(self) -&gt; bool:
        return not self.q1
</code></pre>
<hr />
<h1><strong>六、复杂度分析（面试必考）</strong></h1>
<table>
<thead>
<tr>
<th><strong>操作</strong></th>
<th><strong>时间</strong></th>
</tr>
</thead>
<tbody>
<tr>
<td>push</td>
<td>O(n)</td>
</tr>
<tr>
<td>pop</td>
<td>O(1)</td>
</tr>
<tr>
<td>top</td>
<td>O(1)</td>
</tr>
<tr>
<td>empty</td>
<td>O(1)</td>
</tr>
</tbody></table><p>你可以说：</p>
<blockquote>
<p>通过在 push 时重排队列，把代价集中到 push 上。</p>
</blockquote>
<hr />
<h1><strong>七、进阶：只用一个队列</strong></h1>
<h3><strong>思路：</strong></h3>
<blockquote>
<p>push 后，把队列旋转，让新元素到队头</p>
</blockquote>
<hr />
<h3><strong>代码：</strong></h3>
<pre><code class="language-python">class MyStack:
    def __init__(self):
        self.q = deque()

    def push(self, x: int) -&gt; None:
        self.q.append(x)
        for _ in range(len(self.q) - 1):
            self.q.append(self.q.popleft())

    def pop(self) -&gt; int:
        return self.q.popleft()

    def top(self) -&gt; int:
        return self.q[0]

    def empty(self) -&gt; bool:
        return not self.q
</code></pre>
<hr />
<h1><strong>八、你应该记住的“对称思想”</strong></h1>
<table>
<thead>
<tr>
<th><strong>用什么</strong></th>
<th><strong>实现什么</strong></th>
</tr>
</thead>
<tbody>
<tr>
<td>两个栈</td>
<td>队列</td>
</tr>
<tr>
<td>两个队列</td>
<td>栈</td>
</tr>
</tbody></table><p>两者核心都是：</p>
<blockquote>
<p><strong>用一次“整体倒置”，换来多次 O(1) 操作</strong></p>
</blockquote>
]]></content><link href="https://github.com/lihe/MyLeetcode/issues/20"/><category term="栈与队列"/><published>2026-01-12T15:04:15+00:00</published></entry><entry><id>https://github.com/lihe/MyLeetcode/issues/19</id><title>LeetCode 28：实现 strStr()（子串查找）</title><updated>2026-03-11T13:58:19.788347+00:00</updated><content type="html"><![CDATA[<p><a href="https://leetcode.cn/problems/find-the-index-of-the-first-occurrence-in-a-string/description/">LeetCode 28：实现 strStr()（子串查找）</a></p>
<h2><strong>一、先给结论（最重要）</strong></h2>
<blockquote>
<p><strong>最简单正确解法：暴力匹配（双指针）</strong></p>
</blockquote>
<blockquote>
<p><strong>进阶高分解法：KMP（O(n+m)）</strong></p>
</blockquote>
<hr />
<h2><strong>二、解法一：暴力匹配（面试 100% 可过）</strong></h2>
<h3><strong>思路（一句话）</strong></h3>
<blockquote>
<p>从 haystack 的每个位置开始，尝试匹配 needle</p>
</blockquote>
<hr />
<h3><strong>代码（推荐你先写这个）</strong></h3>
<pre><code class="language-python">class Solution:
    def strStr(self, haystack: str, needle: str) -&gt; int:
        n, m = len(haystack), len(needle)

        for i in range(n - m + 1):
            if haystack[i:i+m] == needle:
                return i

        return -1
</code></pre>
<hr />
<h3><strong>示例推演</strong></h3>
<pre><code>haystack = &quot;sadbutsad&quot;
needle   = &quot;sad&quot;

i = 0 → haystack[0:3] == &quot;sad&quot; ✅
return 0
</code></pre>
<hr />
<h3><strong>复杂度</strong></h3>
<ul>
<li>时间复杂度：O(n * m)</li>
<li>空间复杂度：O(1)</li>
</ul>
<p>📌 <strong>在 LeetCode 数据范围内完全没问题</strong></p>
<hr />
<h2><strong>三、解法二：KMP（面试进阶 / 算法岗位）</strong></h2>
<h3><strong>核心思想（先理解，不背公式）</strong></h3>
<blockquote>
<p><strong>匹配失败时，不回退主串指针，而是利用已匹配的信息跳转模式串</strong></p>
</blockquote>
<hr />
<h2><strong>四、KMP 的完整实现（标准版）</strong></h2>
<pre><code class="language-python">class Solution:
    def strStr(self, haystack: str, needle: str) -&gt; int:
        if not needle:
            return 0

        # 1️⃣ 构建 next 数组
        next = [0] * len(needle)
        j = 0
        for i in range(1, len(needle)):
            while j &gt; 0 and needle[i] != needle[j]:
                j = next[j - 1]
            if needle[i] == needle[j]:
                j += 1
            next[i] = j

        # 2️⃣ 匹配过程
        j = 0
        for i in range(len(haystack)):
            while j &gt; 0 and haystack[i] != needle[j]:
                j = next[j - 1]
            if haystack[i] == needle[j]:
                j += 1
            if j == len(needle):
                return i - j + 1

        return -1
</code></pre>
<hr />
<h2><strong>五、KMP 的直觉解释</strong></h2>
<h3><strong>1️⃣ next[i] 表示什么？</strong></h3>
<blockquote>
<p>needle[0:i] 中</p>
</blockquote>
<blockquote>
<p><strong>最长的「前缀 = 后缀」长度</strong></p>
</blockquote>
<hr />
<h3><strong>2️⃣ 为什么能跳？</strong></h3>
<p>因为你已经知道：</p>
<pre><code>needle[0:j-1] == haystack[i-j+1:i]
</code></pre>
<p>所以失败时不需要从头再来。</p>
<hr />
<h2><strong>六、什么时候用暴力？什么时候用 KMP？</strong></h2>
<table>
<thead>
<tr>
<th><strong>场景</strong></th>
<th><strong>推荐</strong></th>
</tr>
</thead>
<tbody>
<tr>
<td>面试白板 / 一面</td>
<td>暴力</td>
</tr>
<tr>
<td>数据量不大</td>
<td>暴力</td>
</tr>
<tr>
<td>明确问“优化 / KMP”</td>
<td>KMP</td>
</tr>
<tr>
<td>算法岗 / 二面</td>
<td>KMP</td>
</tr>
</tbody></table><p>📌 <strong>99% 的面试：暴力 + 能解释即可过</strong></p>
<hr />
<h2><strong>七、Python 内置方法（工程常用，但面试要说明）</strong></h2>
<pre><code>haystack.find(needle)
</code></pre>
<p>或：</p>
<pre><code>haystack.index(needle)  # 找不到会抛异常
</code></pre>
<p>⚠️ 面试中要说一句：</p>
<blockquote>
<p>这是语言内置，我可以自己实现。</p>
</blockquote>
<hr />
<h2><strong>八、常见坑（一定注意）</strong></h2>
<h3><strong>❌ 忘记边界</strong></h3>
<pre><code>for i in range(n):   # ❌
</code></pre>
<p>应为：</p>
<pre><code>for i in range(n - m + 1)
</code></pre>
<hr />
<h3><strong>❌ needle 比 haystack 长</strong></h3>
<p>自然返回 -1（代码已覆盖）</p>
<hr />
<h2><strong>九、面试标准回答模板</strong></h2>
<blockquote>
<p>我可以使用双指针的暴力匹配方法，从主串的每个位置尝试匹配模式串，时间复杂度是 O(nm)。如果需要优化，可以使用 KMP 算法，通过构建 next 数组在匹配失败时避免重复比较，将复杂度降到 O(n+m)。</p>
</blockquote>
<h1><strong>next 数组是怎么生成的</strong></h1>
<h2><strong>一、先给一句“人话定义”</strong></h2>
<blockquote>
<p><strong>next[i] 表示：</strong></p>
</blockquote>
<blockquote>
<p><strong>在 needle[0..i] 这个前缀里，</strong></p>
</blockquote>
<blockquote>
<p><strong>最长的「相同前缀 = 后缀」的长度。</strong></p>
</blockquote>
<p>但现在<strong>先别记这句话</strong>，我们用动画来“算”。</p>
<hr />
<h2><strong>二、你在干什么？</strong></h2>
<p>想象你在做这件事👇</p>
<ul>
<li>你拿着 <strong>模式串 needle</strong></li>
<li>从左到右，一点点“扩建”</li>
<li>每加一个字符，就问自己一句话：</li>
</ul>
<blockquote>
<p><strong>“我现在这段字符串，有没有一部分‘开头’和‘结尾’是一样的？”</strong></p>
</blockquote>
<hr />
<h2><strong>三、动画主角登场</strong></h2>
<p>在构建 next 时，有两个指针：</p>
<pre><code>i：正在计算 next[i] 的位置（向右走）
j：当前能匹配上的“前缀长度”（会前进 / 后退）
</code></pre>
<p>👉 你可以把它想成：</p>
<ul>
<li>i：新字符登场</li>
<li>j：老经验能用多少</li>
</ul>
<hr />
<h2><strong>四、完整动画示例</strong></h2>
<p>我们用 <strong>最经典的例子</strong>：</p>
<pre><code>needle = &quot;a b a b c&quot;
index     0 1 2 3 4
</code></pre>
<hr />
<h3>** 第 0 位（i = 0）**</h3>
<pre><code>a
</code></pre>
<ul>
<li>只有一个字符</li>
<li>不可能有前缀 = 后缀</li>
</ul>
<pre><code>next[0] = 0
</code></pre>
<p>🎬 <strong>画面</strong>：啥都没发生</p>
<hr />
<h3><strong>第 1 位（i = 1）</strong></h3>
<pre><code>a b
</code></pre>
<p>前缀候选：a</p>
<p>后缀候选：b</p>
<p>❌ 不相等</p>
<pre><code>next[1] = 0
</code></pre>
<hr />
<h3><strong>第 2 位（i = 2）</strong></h3>
<pre><code>a b a
</code></pre>
<p>现在你心里问：</p>
<blockquote>
<p>“我能不能把开头的 a</p>
</blockquote>
<blockquote>
<p>和结尾的 a 对上？”</p>
</blockquote>
<p>✅ 能！</p>
<pre><code>a b a
↑     ↑
</code></pre>
<p>于是：</p>
<pre><code>j = 1
next[2] = 1
</code></pre>
<hr />
<h3><strong>第 3 位（i = 3）</strong></h3>
<pre><code>a b a b
</code></pre>
<p>现在你已经知道：</p>
<ul>
<li>前面有一个长度为 1 的可复用前缀：a</li>
</ul>
<p>你试着继续：</p>
<pre><code>a b a b
↑ ↑     ↑ ↑
</code></pre>
<p>ab == ab ✅</p>
<pre><code>j = 2
next[3] = 2
</code></pre>
<hr />
<h3><strong>第 4 位（i = 4）</strong></h3>
<pre><code>a b a b c
</code></pre>
<p>你想继续扩展：</p>
<pre><code>前缀下一位：a
当前字符：c
</code></pre>
<p>❌ 不匹配</p>
<hr />
<h4><strong>关键动画：</strong></h4>
<h4><strong>回退 j</strong></h4>
<p>你不会傻傻地回到 0，而是：</p>
<blockquote>
<p>“我现在的 ab 不行，</p>
</blockquote>
<blockquote>
<p>那有没有更短的前缀还能用？”</p>
</blockquote>
<p>于是你：</p>
<pre><code>j = next[j - 1]   # j = next[1] = 0
</code></pre>
<p>再试一次：</p>
<pre><code>a  vs  c
</code></pre>
<p>❌ 还是不行</p>
<pre><code>next[4] = 0
</code></pre>
<hr />
<h2><strong>五、最终 next 数组</strong></h2>
<pre><code>index:  0 1 2 3 4
char:   a b a b c
next:   0 0 1 2 0
</code></pre>
<p>🎉 <strong>这就是 KMP 的“记忆表”</strong></p>
<hr />
<h2><strong>六、把动画翻译成代码</strong></h2>
<pre><code class="language-python">def build_next(p):
    next = [0] * len(p)
    j = 0

    for i in range(1, len(p)):
        while j &gt; 0 and p[i] != p[j]:
            j = next[j - 1]   # 🎬 回退

        if p[i] == p[j]:
            j += 1           # 🎬 扩展成功

        next[i] = j          # 🎬 记录结果

    return next
</code></pre>
<hr />
<h2><strong>七、你一定要记住的 3 条“动画规则”</strong></h2>
<p>1️⃣ i 一直往右，不回头</p>
<p>2️⃣ j 表示“当前能复用的前缀长度”</p>
<p>3️⃣ 不匹配时，j = next[j-1]（向过去找经验）</p>
<hr />
<h2><strong>八、为什么 next 能加速匹配？</strong></h2>
<blockquote>
<p>因为它提前帮你算好了：</p>
</blockquote>
<blockquote>
<p><strong>“失败时，模式串最多能滑到哪里而不白比”</strong></p>
</blockquote>
<hr />
<h2><strong>动画设定（和前面保持一致）</strong></h2>
<ul>
<li><strong>主串 haystack</strong>：一条很长的跑道</li>
<li><strong>模式串 needle</strong>：一块会“自我反思”的脚印模板</li>
<li><strong>next 数组</strong>：模板上刻着的“滑动刻度线”</li>
<li><strong>你</strong>：负责把模板贴在跑道上对齐</li>
</ul>
<hr />
<h2><strong>我们用这个经典例子</strong></h2>
<pre><code>haystack = &quot;a b a b a b c&quot;
needle   = &quot;a b a b c&quot;
next     = [0, 0, 1, 2, 0]
</code></pre>
<p>（这是你刚刚算出来的 next）</p>
<hr />
<h2><strong>第一幕：模板第一次贴上跑道</strong></h2>
<pre><code>跑道： a b a b a b c
模板： a b a b c
         ✔ ✔ ✔ ✔ ✘
</code></pre>
<ul>
<li>前 4 个字符：完美匹配</li>
<li>第 5 个字符：c ≠ a → ❌ 失败</li>
</ul>
<p>🎥 <strong>镜头暂停</strong></p>
<hr />
<h2><strong>暴力算法会怎么做？</strong></h2>
<blockquote>
<p>“算了，我不记得刚才的成功了。”</p>
</blockquote>
<pre><code>跑道： a b a b a b c
模板：   a b a b c
</code></pre>
<ul>
<li>主串指针回退</li>
<li>已经对过的字符重新再比一次</li>
</ul>
<p>👉 <strong>浪费</strong></p>
<hr />
<h2><strong>KMP 登场：模板开始“反思”</strong></h2>
<p>你低头看模板，模板自己说话了：</p>
<blockquote>
<p>“等等，我刚才成功匹配的是 abab</p>
</blockquote>
<blockquote>
<p>而 abab 里面，有一部分我自己是重复的。”</p>
</blockquote>
<hr />
<h2><strong>第二幕：next 数组开始工作</strong></h2>
<h3><strong>当前状态</strong></h3>
<ul>
<li>模式串在第 j = 4 位失败</li>
<li>你查刻度线：</li>
</ul>
<pre><code>j = next[j - 1] = next[3] = 2
</code></pre>
<p>🎥 <strong>动画含义</strong>：</p>
<blockquote>
<p>“别把模板抬走，</p>
</blockquote>
<blockquote>
<p>只需要把它滑到还能复用 ab 的位置。”</p>
</blockquote>
<hr />
<h2><strong>模板“聪明滑动”</strong></h2>
<pre><code>跑道： a b a b a b c
模板：     a b a b c
           ↑
</code></pre>
<p>⚠️ 注意一个<strong>关键点</strong>：</p>
<p>👉 <strong>主串指针 i 没有回退！</strong></p>
<p>你没有重新踩已经踩过的跑道。</p>
<hr />
<h2><strong>第三幕：继续对齐</strong></h2>
<pre><code>跑道： a b a b a b c
模板：     a b a b c
           ✔ ✔ ✔ ✔ ✔
</code></pre>
<p>这一次：</p>
<ul>
<li>从 ab 开始</li>
<li>一路对到 c</li>
<li>🎉 <strong>完全匹配成功</strong></li>
</ul>
<hr />
<h2><strong>KMP 在这里返回答案</strong></h2>
<pre><code>匹配起点 = i - len(needle) + 1
</code></pre>
<p>你找到了 <strong>needle 在 haystack 中第一次出现的位置</strong>。</p>
<hr />
<h2><strong>用“人话”总结整个匹配过程</strong></h2>
<blockquote>
<p>KMP 匹配时，主串指针永远向前走，模式串失败时不会回到起点，而是借助 next 数组，滑到一个“已经被证明可能成功”的位置。</p>
</blockquote>
<hr />
<h2><strong>KMP 匹配阶段的 3 条“动画规则”（必背）</strong></h2>
<h3><strong>规则 1️⃣</strong></h3>
<p><strong>主串指针 i 从不回退</strong></p>
<hr />
<h3><strong>规则 2️⃣</strong></h3>
<p><strong>失败时只移动模式串指针 j</strong></p>
<pre><code>j = next[j - 1]
</code></pre>
<hr />
<h3><strong>规则 3️⃣</strong></h3>
<p><strong>匹配成功就一起前进</strong></p>
<pre><code>if haystack[i] == needle[j]:
    i += 1
    j += 1
</code></pre>
<hr />
<h2><strong>把动画翻译成代码</strong></h2>
<pre><code class="language-python">j = 0
for i in range(len(haystack)):
    while j &gt; 0 and haystack[i] != needle[j]:
        j = next[j - 1]   # 🎬 模板滑动

    if haystack[i] == needle[j]:
        j += 1           # 🎬 对齐成功

    if j == len(needle):
        return i - j + 1
</code></pre>
<hr />
<h2><strong>为什么 KMP 一定是 O(n + m)？</strong></h2>
<p>动画角度：</p>
<ul>
<li>
<p>跑道（haystack）：</p>
<p>👉 你只走一遍，不后退</p>
</li>
<li>
<p>模板（needle）：</p>
<p>👉 滑动次数由 next 控制，总次数有限</p>
</li>
</ul>
<p>👉 <strong>没有“重复踩同一段跑道”</strong></p>
<hr />
]]></content><link href="https://github.com/lihe/MyLeetcode/issues/19"/><category term="字符串"/><published>2026-01-10T16:00:09+00:00</published></entry><entry><id>https://github.com/lihe/MyLeetcode/issues/18</id><title>LeetCode 151：反转字符串中的单词</title><updated>2026-03-11T13:58:19.959475+00:00</updated><content type="html"><![CDATA[<p><a href="https://leetcode.cn/problems/reverse-words-in-a-string/">LeetCode 151：反转字符串中的单词</a></p>
<h2>一、题目本质（先想清楚你到底在干嘛）</h2>
<p>这题<strong>不是简单反转字符串</strong>，而是三件事同时发生：</p>
<p>1️⃣ <strong>去掉多余空格</strong></p>
<ul>
<li>前导空格 ❌</li>
<li>尾随空格 ❌</li>
<li>单词间多个空格 → 1 个空格</li>
</ul>
<p>2️⃣ <strong>以“单词”为单位反转顺序</strong></p>
<ul>
<li>单词内部顺序 <strong>不变</strong></li>
<li>单词整体顺序 <strong>反转</strong></li>
</ul>
<p>3️⃣ 返回一个 <strong>规范化的新字符串</strong></p>
<hr />
<h2><strong>二、Python 最优解（面试 &amp; 实战首选）</strong></h2>
<h3><strong>✅ 推荐写法（一句话版）</strong></h3>
<pre><code class="language-python">class Solution:
    def reverseWords(self, s: str) -&gt; str:
        return &quot; &quot;.join(s.split()[::-1])
</code></pre>
<h3><strong>为什么这一行就够？</strong></h3>
<p>我们拆解一下：</p>
<pre><code>s.split()
</code></pre>
<ul>
<li>
<p>自动：</p>
<ul>
<li>去掉前导 / 尾随空格</li>
<li>合并多个空格</li>
</ul>
</li>
<li>
<p>得到：<strong>单词数组</strong></p>
</li>
</ul>
<pre><code>[::-1]
</code></pre>
<ul>
<li>反转单词顺序</li>
</ul>
<pre><code>&quot; &quot;.join(...)
</code></pre>
<ul>
<li>用 <strong>单个空格</strong> 拼接</li>
</ul>
<hr />
<h3><strong>示例推演</strong></h3>
<pre><code>s = &quot;  a good   example  &quot;

s.split()
# [&#x27;a&#x27;, &#x27;good&#x27;, &#x27;example&#x27;]

[::-1]
# [&#x27;example&#x27;, &#x27;good&#x27;, &#x27;a&#x27;]

join
# &quot;example good a&quot;
</code></pre>
<p>✔ 完全符合题意</p>
<hr />
<h2><strong>三、复杂度分析</strong></h2>
<ul>
<li>时间复杂度：O(n)</li>
<li>空间复杂度：O(n)（split 会创建新数组）</li>
</ul>
<p>⚠️ <strong>Python 中这是最合理的解法</strong></p>
<hr />
<h2><strong>四、进阶：O(1) 空间的“原地解法”思路</strong></h2>
<blockquote>
<p>⚠️ 仅适用于 <strong>字符串是可变类型</strong>（如 char[]）</p>
</blockquote>
<h3><strong>原地解法三步走（一定要记住）</strong></h3>
<h4><strong>1️⃣ 去除多余空格（压缩）</strong></h4>
<pre><code>&quot;  a  good   example  &quot;
→ &quot;a good example&quot;
</code></pre>
<h4><strong>2️⃣ 整体反转字符串</strong></h4>
<pre><code>&quot;a good example&quot;
→ &quot;elpmaxe doog a&quot;
</code></pre>
<h4><strong>3️⃣ 逐个单词反转</strong></h4>
<pre><code>&quot;elpmaxe doog a&quot;
→ &quot;example good a&quot;
</code></pre>
<hr />
<h3><strong>为什么这样可行？</strong></h3>
<ul>
<li>整体反转 → <strong>单词顺序反了</strong></li>
<li>单词再反转 → <strong>单词内部顺序恢复</strong></li>
</ul>
<p>👉 <strong>反转的经典组合技</strong></p>
<hr />
<h2><strong>五、为什么 Python 不强求原地？</strong></h2>
<p>你可以这样回答面试官：</p>
<blockquote>
<p>Python 的字符串是不可变类型，无法真正原地修改；在 Python 中通常通过 split 和 join 返回新字符串即可。如果是字符数组场景，可以使用三步反转法在 O(1) 额外空间内完成。</p>
</blockquote>
<hr />
<h2><strong>六、常见翻车点</strong></h2>
<h3><strong>❌ 错误 1：直接反转字符串</strong></h3>
<pre><code>s[::-1]  # ❌
</code></pre>
<p>会得到：</p>
<pre><code>&quot;eulb si yks eht&quot;
</code></pre>
<p>👉 <strong>单词内部顺序也被反了</strong></p>
<hr />
<h3><strong>❌ 错误 2：只 split，不处理空格</strong></h3>
<pre><code>s.split(&quot; &quot;)  # ❌
</code></pre>
<p>会产生空字符串：</p>
<pre><code>[&#x27;&#x27;, &#x27;&#x27;, &#x27;hello&#x27;, &#x27;&#x27;, &#x27;world&#x27;, &#x27;&#x27;, &#x27;&#x27;]
</code></pre>
<hr />
<h3><strong>❌ 错误 3：join 用错分隔符</strong></h3>
<pre><code>&quot;&quot;.join(...)  # ❌
</code></pre>
<hr />
<h2><strong>七、面试标准回答模板</strong></h2>
<blockquote>
<p>我先将字符串按空格分割成单词数组，split 会自动去除多余空格；然后反转单词数组，最后使用单个空格拼接，得到反转后的字符串。</p>
</blockquote>
<h2>变形</h2>
<p>反转单词顺序，但“中间的空格不压缩、不合并”，原来有几个空格就保留几个空格</p>
<h3><strong>变形题的精确定义（常见问法）</strong></h3>
<ul>
<li><strong>单词</strong>：连续的非空格字符</li>
<li><strong>空格块</strong>：连续的空格（可能是 1 个、2 个、很多个）</li>
<li>要做的是：<strong>把单词整体顺序反过来，但所有空格块原样保留在原来的位置/形态</strong></li>
</ul>
<p>举例：</p>
<ul>
<li>&quot;a good  example&quot; → &quot;example good  a&quot;</li>
<li>&quot; hello  world &quot; → &quot; world  hello &quot;（如果也要求保留前后空格，那就这样；下面的方案会连前后一起保留，最稳）</li>
</ul>
<hr />
<h2><strong>面试可背解法：分成“单词块 / 空格块”，只反转单词块</strong></h2>
<h3><strong>思路</strong></h3>
<ol>
<li>扫描字符串，按顺序提取 token：要么是一段空格 &quot;  &quot;，要么是一段单词 &quot;hello&quot;.</li>
<li>把所有单词收集起来并反转。</li>
<li>再按原 token 序列重建：遇到“空格 token”就原样放回；遇到“单词 token”就从反转后的单词表里依次取。</li>
</ol>
<h3><strong>Python 实现</strong></h3>
<pre><code class="language-python">class Solution:
    def reverseWords_keep_spaces(self, s: str) -&gt; str:
        tokens = []   # 按原顺序保存：(&#x27;space&#x27;, &#x27;   &#x27;) 或 (&#x27;word&#x27;, &#x27;hello&#x27;)
        words = []

        n, i = len(s), 0
        while i &lt; n:
            j = i
            if s[i] == &#x27; &#x27;:
                while j &lt; n and s[j] == &#x27; &#x27;:
                    j += 1
                tokens.append((&#x27;space&#x27;, s[i:j]))
            else:
                while j &lt; n and s[j] != &#x27; &#x27;:
                    j += 1
                w = s[i:j]
                tokens.append((&#x27;word&#x27;, w))
                words.append(w)
            i = j

        words.reverse()

        # 重建：空格原样保留；单词用反转后的顺序替换
        res = []
        wi = 0
        for typ, val in tokens:
            if typ == &#x27;space&#x27;:
                res.append(val)
            else:
                res.append(words[wi])
                wi += 1

        return &#x27;&#x27;.join(res)
</code></pre>
<hr />
<h2><strong>复杂度</strong></h2>
<ul>
<li>时间：O(n)</li>
<li>额外空间：O(n)（Python 字符串不可变，想做到严格 O(1) 很难；面试一般接受）</li>
</ul>
<hr />
<h2><strong>面试官追问你怎么答</strong></h2>
<blockquote>
<p>为什么不能用 split()？</p>
</blockquote>
<ul>
<li>split() 会把多个空格合并（或者产生空串），<strong>会破坏“保留中间所有空格”的要求</strong>。</li>
</ul>
]]></content><link href="https://github.com/lihe/MyLeetcode/issues/18"/><category term="字符串"/><published>2026-01-08T15:07:04+00:00</published></entry><entry><id>https://github.com/lihe/MyLeetcode/issues/17</id><title>LeetCode 541：反转字符串 II</title><updated>2026-03-11T13:58:20.118035+00:00</updated><content type="html"><![CDATA[<p><a href="https://leetcode.cn/problems/reverse-string-ii/">LeetCode 541：反转字符串 II</a></p>
<hr />
<h2><strong>一、题意快速翻译</strong></h2>
<blockquote>
<p>从字符串起始位置开始，每 <strong>2k</strong> 个字符为一组，<strong>只反转这一组中的前 k 个字符</strong>，其余按规则处理直到结束。</p>
</blockquote>
<hr />
<h2><strong>二、核心规则拆解</strong></h2>
<p>对字符串从左到右，每次看 <strong>2k</strong> 个字符：</p>
<ol>
<li>
<p><strong>剩余字符 &lt; k</strong></p>
<ul>
<li>👉 <strong>全部反转</strong></li>
</ul>
</li>
<li>
<p><strong>k ≤ 剩余字符 &lt; 2k</strong></p>
<ul>
<li>👉 <strong>只反转前 k 个</strong></li>
<li>👉 后面的保持不变</li>
</ul>
</li>
<li>
<p><strong>剩余字符 ≥ 2k</strong></p>
<ul>
<li>👉 <strong>反转前 k 个</strong></li>
<li>👉 后 k 个不动</li>
</ul>
</li>
</ol>
<hr />
<h2><strong>三、算法思路</strong></h2>
<h3><strong>思路关键词</strong></h3>
<ul>
<li>步长：2k</li>
<li>操作对象：[i, i+k)</li>
<li>原地 or 转 list 处理都可以</li>
</ul>
<h3><strong>步骤</strong></h3>
<ol>
<li>把字符串转成字符数组（方便修改）</li>
<li>从 i = 0 开始，每次 i += 2k</li>
<li>对区间 [i, min(i+k, n)) 做反转</li>
</ol>
<hr />
<h2><strong>四、Python 标准实现（面试推荐写法）</strong></h2>
<pre><code class="language-Python">def reverseStr(s: str, k: int) -&gt; str:
    s = list(s)
    n = len(s)

    for i in range(0, n, 2 * k):
        # 反转 [i, i+k)
        s[i:i+k] = reversed(s[i:i+k])

    return &quot;&quot;.join(s)
</code></pre>
<hr />
<h2><strong>五、为什么这一行就够了？</strong></h2>
<pre><code>s[i:i+k] = reversed(s[i:i+k])
</code></pre>
<p>因为：</p>
<ul>
<li>
<p>Python 切片 <strong>自动处理越界</strong></p>
</li>
<li>
<p>当剩余字符：</p>
<ul>
<li>&lt; k → 切片只取剩余部分（全部反转）</li>
<li>≥ k → 正好反转前 k 个</li>
</ul>
</li>
<li>
<p>完美覆盖所有题目条件 ✅</p>
</li>
</ul>
<hr />
<h2><strong>六、示例走一遍（加深理解）</strong></h2>
<h3><strong>示例</strong></h3>
<pre><code>s = &quot;abcdefg&quot;, k = 2
</code></pre>
<h3><strong>过程</strong></h3>
<ul>
<li>i = 0 → &quot;ab&quot; 反转 → &quot;ba&quot;</li>
<li>i = 4 → &quot;ef&quot; 反转 → &quot;fe&quot;</li>
<li>剩余 &quot;g&quot; &lt; k → 全反转（还是 &quot;g&quot;）</li>
</ul>
<h3><strong>结果</strong></h3>
<pre><code>&quot;bacdfeg&quot;
</code></pre>
<hr />
<h2><strong>七、复杂度分析</strong></h2>
<ul>
<li>
<p><strong>时间复杂度</strong>：O(n)</p>
</li>
<li>
<p><strong>空间复杂度</strong>：</p>
<ul>
<li>Python：O(n)（字符串不可变）</li>
<li>若允许原地字符数组：O(1) 额外空间</li>
</ul>
</li>
</ul>
<hr />
<h2><strong>八、面试官级总结</strong></h2>
<blockquote>
<p>本题通过每次以 2k 为步长遍历字符串，并对每一段的前 k 个字符进行反转即可。利用切片和越界特性可以统一处理所有边界情况，时间复杂度为 O(n)，实现简洁且高效。</p>
</blockquote>
<hr />
]]></content><link href="https://github.com/lihe/MyLeetcode/issues/17"/><category term="字符串"/><published>2026-01-06T13:48:12+00:00</published></entry><entry><id>https://github.com/lihe/MyLeetcode/issues/16</id><title>LeetCode 142：环形链表 II（找环的入口）</title><updated>2026-03-11T13:58:20.294206+00:00</updated><content type="html"><![CDATA[<p><a href="https://leetcode.cn/problems/linked-list-cycle-ii/">https://leetcode.cn/problems/linked-list-cycle-ii/</a></p>
<h2><strong>一、先给结论</strong></h2>
<blockquote>
<p><strong>先用快慢指针判断是否有环；</strong></p>
</blockquote>
<blockquote>
<p><strong>若有环，让一个指针从头节点出发，另一个从相遇点出发，</strong></p>
</blockquote>
<blockquote>
<p><strong>两者每次走一步，再次相遇的节点就是环的入口。</strong></p>
</blockquote>
<hr />
<h2><strong>二、标准解法（O(n) 时间 + O(1) 空间）</strong></h2>
<pre><code class="language-python">class Solution:
    def detectCycle(self, head):
        slow = fast = head

        # 1️⃣ 先判断是否有环
        while fast and fast.next:
            slow = slow.next
            fast = fast.next.next
            if slow is fast:
                break
        else:
            # 没有环
            return None

        # 2️⃣ 找环的入口
        slow = head
        while slow is not fast:
            slow = slow.next
            fast = fast.next

        return slow
</code></pre>
<p>✔ 不修改链表</p>
<p>✔ 不用额外空间</p>
<p>✔ 满足题目进阶要求</p>
<hr />
<h2><strong>三、为什么“重新从头走”就能找到入口？（这是灵魂）</strong></h2>
<h3><strong>定义几个距离（非常关键）</strong></h3>
<p>假设：</p>
<pre><code>head → … → 入环点 → … → 相遇点 → … → 入环点
</code></pre>
<p>定义：</p>
<ul>
<li>a：头节点 → 入环点 的距离</li>
<li>b：入环点 → 相遇点 的距离</li>
<li>c：相遇点 → 入环点 的距离</li>
<li>环长度：b + c</li>
</ul>
<hr />
<h3><strong>快慢指针第一次相遇时，发生了什么？</strong></h3>
<ul>
<li>慢指针走了：a + b</li>
<li>快指针走了：a + b + k(b + c)（多转了 k 圈）</li>
</ul>
<p>因为快指针速度是慢指针的 <strong>2 倍</strong>：</p>
<p>2(a + b) = a + b + k(b + c)</p>
<p>整理：</p>
<p>a = k(b + c) - b = (k-1)(b+c) + c</p>
<p>👉 <strong>关键结论：</strong></p>
<blockquote>
<p>a ≡ c (mod 环长度)</p>
</blockquote>
<p>也就是说：</p>
<ul>
<li>从 <strong>头节点走 a 步</strong></li>
<li>从 <strong>相遇点走 c 步</strong></li>
</ul>
<p>👉 <strong>都会到达入环点</strong></p>
<hr />
<h2><strong>四、这就是第二阶段的逻辑</strong></h2>
<pre><code>slow = head      # 走 a 步
fast = 相遇点    # 走 c 步

while slow != fast:
    slow = slow.next
    fast = fast.next
</code></pre>
<p>两者 <strong>必然在入环点相遇</strong>。</p>
<hr />
<h2><strong>五、手动推演一遍</strong></h2>
<p>示例：</p>
<pre><code>1 → 2 → 3 → 4 → 5
          ↑       ↓
          ← ← ← ←
</code></pre>
<ul>
<li>第一次相遇在 4</li>
<li>slow 回到 1</li>
<li>fast 留在 4</li>
</ul>
<p>走：</p>
<table>
<thead>
<tr>
<th><strong>步数</strong></th>
<th><strong>slow</strong></th>
<th><strong>fast</strong></th>
</tr>
</thead>
<tbody>
<tr>
<td>1</td>
<td>2</td>
<td>5</td>
</tr>
<tr>
<td>2</td>
<td>3</td>
<td>3 ✅</td>
</tr>
</tbody></table><p>👉 3 是入环点</p>
<hr />
<h2><strong>六、为什么不能直接“记住访问过的节点”？</strong></h2>
<pre><code>visited = set()
</code></pre>
<p>❌ 空间复杂度 O(n)</p>
<p>❌ 不满足题目进阶要求</p>
<p>❌ 面试会被扣分</p>
<hr />
<h2><strong>七、为什么不能修改链表？</strong></h2>
<p>题目明确要求：</p>
<blockquote>
<p><strong>不允许修改链表</strong></p>
</blockquote>
<p>所以以下都 ❌：</p>
<ul>
<li>改 next</li>
<li>打标记</li>
<li>反转链表</li>
</ul>
<hr />
<h2><strong>八、常见翻车点（非常重要）</strong></h2>
<h3><strong>❌ 错误 1：没用</strong></h3>
<h3><strong>else</strong></h3>
<h3><strong>判断无环</strong></h3>
<pre><code>while fast and fast.next:
    ...
# 直接进入第二阶段 ❌
</code></pre>
<p>👉 如果无环会死循环 / 空指针</p>
<hr />
<h3><strong>❌ 错误 2：第二阶段快指针没动</strong></h3>
<pre><code>slow = head
# fast 不动 ❌
</code></pre>
<p>👉 永远不相遇</p>
<hr />
<h3><strong>❌ 错误 3：用值判断</strong></h3>
<pre><code>slow.val == fast.val  # ❌
</code></pre>
<p>👉 必须是节点引用比较</p>
<hr />
<h2><strong>九、时间 &amp; 空间复杂度</strong></h2>
<ul>
<li>时间复杂度：O(n)</li>
<li>空间复杂度：O(1)</li>
</ul>
<hr />
<h2><strong>十、面试标准回答模板</strong></h2>
<blockquote>
<p>我先使用快慢指针判断链表是否存在环，如果存在，当快慢指针相遇后，将其中一个指针重新指向头节点，然后两个指针每次同时走一步，它们再次相遇的位置就是环的入口节点。</p>
</blockquote>
<hr />
]]></content><link href="https://github.com/lihe/MyLeetcode/issues/16"/><category term="链表"/><published>2026-01-04T15:10:56+00:00</published></entry><entry><id>https://github.com/lihe/MyLeetcode/issues/15</id><title>LeetCode 160：相交链表</title><updated>2026-03-11T13:58:20.455499+00:00</updated><content type="html"><![CDATA[<p><a href="https://leetcode.cn/problems/intersection-of-two-linked-lists/submissions/687890317/">https://leetcode.cn/problems/intersection-of-two-linked-lists/submissions/687890317/</a></p>
<h2><strong>一、先给结论</strong></h2>
<blockquote>
<p><strong>用两个指针分别从 headA 和 headB 出发，走到末尾后切换到另一条链表的头，如果两链表相交，两个指针一定会在交点相遇；如果不相交，最终都会走到 null。</strong></p>
</blockquote>
<hr />
<h2><strong>二、最优解法：双指针「换头法」（O(n) + O(1)）</strong></h2>
<h3><strong>标准代码（面试级）</strong></h3>
<pre><code class="language-python">class Solution:
    def getIntersectionNode(self, headA, headB):
        pA, pB = headA, headB

        while pA is not pB:
            pA = pA.next if pA else headB
            pB = pB.next if pB else headA

        return pA  # 可能是相交节点，也可能是 None
</code></pre>
<p>✔ 不改链表结构</p>
<p>✔ 不用额外空间</p>
<p>✔ 一趟“等效遍历”</p>
<hr />
<h2><strong>三、为什么这个方法一定成立？</strong></h2>
<h3><strong>关键事实（一定要理解）</strong></h3>
<ul>
<li>
<p>如果两个链表相交</p>
<p>👉 <strong>相交之后的所有节点是完全共享的</strong></p>
</li>
<li>
<p>两条链表长度可能不同</p>
<p>👉 前半段“独占部分”长度不同</p>
</li>
</ul>
<hr />
<h3><strong>核心思想：</strong></h3>
<h3><strong>让两指针“走过相同的总路程”</strong></h3>
<p>设：</p>
<ul>
<li>链表 A 长度 = a + c</li>
<li>链表 B 长度 = b + c</li>
<li>c 是公共尾部长度</li>
</ul>
<h4><strong>指针走的路径是：</strong></h4>
<pre><code>pA: A → B
pB: B → A
</code></pre>
<p>两者总路程都是：</p>
<pre><code>a + c + b
</code></pre>
<p>👉 <strong>在第二次进入公共尾部时，一定同时到达交点</strong></p>
<hr />
<h2><strong>四、用图形直觉理解</strong></h2>
<p>假设：</p>
<pre><code>A: a1 → a2 → c1 → c2 → c3
B: b1 → b2 → b3 → c1 → c2 → c3
</code></pre>
<p>第一次走：</p>
<ul>
<li>pA 先到 c1</li>
<li>pB 还没到</li>
</ul>
<p>换头后：</p>
<ul>
<li>pA 走 b1 b2 b3</li>
<li>pB 走 a1 a2</li>
</ul>
<p>👉 当两者都进入 c1 时：</p>
<pre><code>pA == pB == c1
</code></pre>
<hr />
<h2><strong>五、如果不相交会发生什么？</strong></h2>
<pre><code>A: a1 → a2 → a3 → None
B: b1 → b2 → None
</code></pre>
<p>路径：</p>
<ul>
<li>pA: A → B → None</li>
<li>pB: B → A → None</li>
</ul>
<p>👉 <strong>最终同时为 None</strong></p>
<pre><code>return None
</code></pre>
<p>符合题意。</p>
<hr />
<h2><strong>六、为什么不能用「值相等」判断？</strong></h2>
<p>题目明确强调：</p>
<blockquote>
<p><strong>相交是“节点相同”，不是“值相同”</strong></p>
</blockquote>
<p>所以判断必须是：</p>
<pre><code>pA is pB
</code></pre>
<p>而不是：</p>
<pre><code>pA.val == pB.val  # ❌
</code></pre>
<hr />
<h2><strong>七、为什么不能改链表结构？</strong></h2>
<p>题目要求：</p>
<blockquote>
<p><strong>返回结果后，链表必须保持原始结构</strong></p>
</blockquote>
<p>所以以下做法全部 ❌：</p>
<ul>
<li>改 next</li>
<li>反转链表</li>
<li>做标记</li>
</ul>
<hr />
<h2><strong>八、常见错误（高频翻车点）</strong></h2>
<h3><strong>❌ 错误 1：只遍历一次</strong></h3>
<pre><code>while pA and pB:
</code></pre>
<p>👉 长度不同，永远对不齐</p>
<hr />
<h3><strong>❌ 错误 2：不换头</strong></h3>
<pre><code>pA = pA.next
pB = pB.next
</code></pre>
<p>👉 只能对齐长度相同的链表</p>
<hr />
<h3><strong>❌ 错误 3：用 set / hash</strong></h3>
<pre><code>visited = set()
</code></pre>
<p>👉 空间复杂度 O(n)，不满足进阶</p>
<hr />
<h2><strong>九、另一种解法（了解即可）：长度对齐法</strong></h2>
<p>思路：</p>
<ol>
<li>分别算长度</li>
<li>长链表先走差值</li>
<li>同步走找交点</li>
</ol>
<p>✔ 正确</p>
<p>❌ 代码更长</p>
<p>❌ 两次遍历</p>
<p>👉 <strong>不如换头法优雅</strong></p>
<hr />
<h2><strong>十、面试标准回答模板</strong></h2>
<blockquote>
<p>使用双指针的方法，让两个指针分别从两条链表的头开始遍历，当走到末尾时切换到另一条链表的头。这样可以让两个指针走过相同的总路径长度，如果链表相交，它们一定会在交点相遇；如果不相交，则最终都会指向 null。</p>
</blockquote>
<hr />
]]></content><link href="https://github.com/lihe/MyLeetcode/issues/15"/><category term="链表"/><published>2025-12-29T08:52:46+00:00</published></entry><entry><id>https://github.com/lihe/MyLeetcode/issues/14</id><title>LeetCode 19：删除链表的倒数第 N 个节点</title><updated>2026-03-11T13:58:20.600944+00:00</updated><content type="html"><![CDATA[<p><a href="https://leetcode.cn/problems/remove-nth-node-from-end-of-list/">https://leetcode.cn/problems/remove-nth-node-from-end-of-list/</a></p>
<h2><strong>一、题目真正考什么？</strong></h2>
<p>不是“删节点”，而是：</p>
<blockquote>
<p><strong>如何在一次遍历中，定位“倒数第 n 个节点的前一个节点”</strong></p>
</blockquote>
<p>这件事用普通遍历是做不到的，</p>
<p>必须用 <strong>快慢指针制造“距离差”</strong>。</p>
<hr />
<h2><strong>二、核心思路</strong></h2>
<blockquote>
<p>使用快慢指针，让快指针先走 n 步，然后快慢指针一起走，当快指针到达末尾时，慢指针正好指向要删除节点的前一个节点。</p>
</blockquote>
<hr />
<h2><strong>三、为什么一定要用 dummy（虚拟头节点）？</strong></h2>
<p>因为要删除的节点 <strong>可能是头节点</strong>：</p>
<ul>
<li>[1], n = 1</li>
<li>[1,2], n = 2</li>
</ul>
<p>👉 dummy 可以 <strong>统一处理所有情况</strong>，不用特判。</p>
<hr />
<h2><strong>四、标准解法（⭐一趟扫描，强烈推荐）</strong></h2>
<pre><code class="language-python">class Solution:
    def removeNthFromEnd(self, head, n):
        dummy = ListNode(0)
        dummy.next = head

        fast = dummy
        slow = dummy

        # 1️⃣ fast 先走 n 步
        for _ in range(n):
            fast = fast.next

        # 2️⃣ fast 和 slow 一起走
        while fast.next:
            fast = fast.next
            slow = slow.next

        # 3️⃣ 删除倒数第 n 个节点
        slow.next = slow.next.next

        return dummy.next
</code></pre>
<hr />
<h2><strong>五、指针推演</strong></h2>
<h3><strong>示例 1</strong></h3>
<pre><code>head = [1,2,3,4,5], n = 2
</code></pre>
<p>加 dummy：</p>
<pre><code>dummy → 1 → 2 → 3 → 4 → 5
↑
fast, slow
</code></pre>
<hr />
<h3><strong>1️⃣ fast 先走 n = 2 步</strong></h3>
<pre><code>dummy → 1 → 2 → 3 → 4 → 5
          ↑
         fast
↑
slow
</code></pre>
<hr />
<h3><strong>2️⃣ fast、slow 一起走</strong></h3>
<p>直到 fast.next == None：</p>
<pre><code>dummy → 1 → 2 → 3 → 4 → 5
                    ↑
                   fast
               ↑
              slow
</code></pre>
<hr />
<h3><strong>3️⃣ 删除 slow.next（也就是 4）</strong></h3>
<pre><code>slow.next = slow.next.next
</code></pre>
<p>结果：</p>
<pre><code>dummy → 1 → 2 → 3 → 5
</code></pre>
<p>返回 dummy.next。</p>
<hr />
<h2><strong>六、为什么这是“一趟扫描”？</strong></h2>
<p>虽然代码看起来有两段循环，但：</p>
<ul>
<li>fast 指针：只走一遍</li>
<li>slow 指针：只走一遍</li>
</ul>
<p>👉 <strong>每个节点最多访问一次</strong></p>
<p>👉 时间复杂度仍然是 O(n)</p>
<hr />
<h2><strong>七、时间 &amp; 空间复杂度</strong></h2>
<ul>
<li>时间复杂度：O(n)</li>
<li>空间复杂度：O(1)</li>
</ul>
<hr />
<h2><strong>八、常见翻车点（非常重要）</strong></h2>
<h3><strong>❌ 错误 1：不用 dummy</strong></h3>
<pre><code>fast = head
slow = head
</code></pre>
<p>👉 删除头节点时必崩</p>
<hr />
<h3><strong>❌ 错误 2：fast 走 n-1 步</strong></h3>
<pre><code>for _ in range(n-1):  # ❌
</code></pre>
<p>👉 删除位置偏一位</p>
<hr />
<h3><strong>❌ 错误 3：while fast 而不是 fast.next</strong></h3>
<pre><code>while fast:   # ❌
</code></pre>
<p>👉 slow 会多走一步</p>
<hr />
<h2><strong>面试标准回答模板</strong></h2>
<blockquote>
<p>我使用快慢指针的方法，让快指针先走 n 步，然后快慢指针同步前进，当快指针到达链表末尾时，慢指针正好位于待删除节点的前一个节点，从而可以在一趟遍历中完成删除操作。</p>
</blockquote>
]]></content><link href="https://github.com/lihe/MyLeetcode/issues/14"/><category term="链表"/><published>2025-12-29T08:13:36+00:00</published></entry><entry><id>https://github.com/lihe/MyLeetcode/issues/13</id><title>LeetCode 24：两两交换链表中的节点</title><updated>2026-03-11T13:58:20.748427+00:00</updated><content type="html"><![CDATA[<p><a href="https://leetcode.cn/problems/swap-nodes-in-pairs/">https://leetcode.cn/problems/swap-nodes-in-pairs/</a></p>
<h2><strong>一、题目核心约束（先抓重点）</strong></h2>
<ul>
<li>❌ 不能交换 val</li>
<li>✅ 只能交换 <strong>节点本身（next 指针）</strong></li>
<li>每次交换 <strong>相邻两个节点</strong></li>
<li>节点个数可能是 0 / 1 / 奇数 / 偶数</li>
</ul>
<p>👉 这直接排除了“偷懒交换值”的做法。</p>
<hr />
<h2><strong>二、最稳解法：</strong></h2>
<h2><strong>dummy + 迭代（强烈推荐）</strong></h2>
<h3><strong>一句话思路</strong></h3>
<blockquote>
<p>使用一个虚拟头节点，把链表每两个节点当成一组，通过指针重连完成交换，然后移动到下一组。</p>
</blockquote>
<hr />
<h2><strong>三、标准代码（面试级，零特判）</strong></h2>
<pre><code class="language-python">class Solution:
    def swapPairs(self, head):
        dummy = ListNode(0)
        dummy.next = head
        prev = dummy

        while prev.next and prev.next.next:
            first = prev.next
            second = first.next

            # 交换两个节点
            prev.next = second
            first.next = second.next
            second.next = first

            # 移动到下一组
            prev = first

        return dummy.next
</code></pre>
<hr />
<h2><strong>四、指针交换理解</strong></h2>
<h3><strong>初始状态（示例 1）</strong></h3>
<pre><code>dummy → 1 → 2 → 3 → 4
          ↑
         prev
</code></pre>
<hr />
<h3><strong>第一次交换（1 和 2）</strong></h3>
<pre><code>prev.next = 2
1.next = 3
2.next = 1
</code></pre>
<p>结果：</p>
<pre><code>dummy → 2 → 1 → 3 → 4
               ↑
              prev
</code></pre>
<blockquote>
<p>⚠️ 注意：prev 移动到 <strong>交换后这一组的第二个节点（first）</strong></p>
</blockquote>
<hr />
<h3><strong>第二次交换（3 和 4）</strong></h3>
<pre><code>dummy → 2 → 1 → 4 → 3
</code></pre>
<p>结束。</p>
<hr />
<h2><strong>五、为什么一定要用 dummy？</strong></h2>
<p>如果不用 dummy：</p>
<ul>
<li>交换第一组时</li>
<li>你必须单独处理头节点</li>
<li>代码会分成两套逻辑</li>
</ul>
<p>👉 <strong>dummy 的作用：统一所有情况</strong></p>
<blockquote>
<p>删除 / 交换头节点 = 删除 / 交换中间节点</p>
</blockquote>
<p>这是链表题的“工程级技巧”。</p>
<hr />
<h2><strong>六、while 条件为什么是这样？</strong></h2>
<pre><code>while prev.next and prev.next.next:
</code></pre>
<p>含义：</p>
<ul>
<li>
<p>至少还剩 <strong>两个节点</strong></p>
</li>
<li>
<p>自动处理：</p>
<ul>
<li>空链表 []</li>
<li>单节点 [1]</li>
</ul>
</li>
</ul>
<hr />
<h2><strong>七、时间 &amp; 空间复杂度</strong></h2>
<ul>
<li>时间复杂度：O(n)</li>
<li>空间复杂度：O(1)（迭代写法）</li>
</ul>
<hr />
<h2><strong>八、递归解法（理解用，可选）</strong></h2>
<h3><strong>思路</strong></h3>
<ul>
<li>交换前两个</li>
<li>剩下的递归处理</li>
</ul>
<h3><strong>代码</strong></h3>
<pre><code class="language-python">class Solution:
    def swapPairs(self, head):
        if not head or not head.next:
            return head

        first = head
        second = head.next

        first.next = self.swapPairs(second.next)
        second.next = first

        return second
</code></pre>
<p>📌 面试推荐 <strong>迭代版</strong>，因为无额外栈空间。</p>
<hr />
<h2><strong>九、常见翻车点（非常重要）</strong></h2>
<h3><strong>❌ 错误 1：交换顺序写错</strong></h3>
<p>指针顺序一错就断链 / 成环。</p>
<hr />
<h3><strong>❌ 错误 2：prev 移动错</strong></h3>
<pre><code>prev = second  # ❌ 会死循环
</code></pre>
<hr />
<h3><strong>❌ 错误 3：交换 val（违规）</strong></h3>
<pre><code>a.val, b.val = b.val, a.val  # ❌
</code></pre>
<hr />
<h2><strong>十、面试标准回答模板</strong></h2>
<blockquote>
<p>通过引入一个虚拟头节点来统一处理头节点的交换问题，在遍历过程中每次将相邻的两个节点重新连接，并将指针移动到下一组，整个过程只修改 next 指针，不修改节点值，时间复杂度是 O(n)。</p>
</blockquote>
<hr />
<p>看到题目出现：</p>
<ul>
<li>链表</li>
<li>成对 / 成组操作</li>
<li>不能改值</li>
</ul>
<p>👉 <strong>dummy + 成组指针重连</strong></p>
<hr />
]]></content><link href="https://github.com/lihe/MyLeetcode/issues/13"/><category term="链表"/><published>2025-12-29T07:56:19+00:00</published></entry><entry><id>https://github.com/lihe/MyLeetcode/issues/12</id><title>LeetCode 707：设计链表</title><updated>2026-03-11T13:58:21.066109+00:00</updated><content type="html"><![CDATA[<p><a href="https://leetcode.cn/problems/design-linked-list/description/">https://leetcode.cn/problems/design-linked-list/description/</a></p>
<h1><strong>一、设计选择</strong></h1>
<p>👉 <strong>选择：单链表 + 虚拟头节点（dummy） + 记录长度</strong></p>
<p>为什么？</p>
<table>
<thead>
<tr>
<th><strong>设计</strong></th>
<th><strong>好处</strong></th>
</tr>
</thead>
<tbody>
<tr>
<td>单链表</td>
<td>结构简单、好写</td>
</tr>
<tr>
<td>dummy</td>
<td>所有操作不用特判 index=0</td>
</tr>
<tr>
<td>size</td>
<td>快速判断 index 合法性</td>
</tr>
</tbody></table><p>这套设计是 <strong>最稳、最不容易出 bug 的</strong>。</p>
<hr />
<h1><strong>二、链表节点定义</strong></h1>
<pre><code class="language-python">class ListNode:
    def __init__(self, val=0, next=None):
        self.val = val
        self.next = next
</code></pre>
<hr />
<h1><strong>三、MyLinkedList 完整实现</strong></h1>
<pre><code class="language-python">class MyLinkedList:

    def __init__(self):
        self.dummy = ListNode(0)
        self.size = 0

    def get(self, index):
        if index &lt; 0 or index &gt;= self.size:
            return -1

        cur = self.dummy.next
        for _ in range(index):
            cur = cur.next
        return cur.val

    def addAtHead(self, val):
        self.addAtIndex(0, val)

    def addAtTail(self, val):
        self.addAtIndex(self.size, val)

    def addAtIndex(self, index, val):
        if index &lt; 0 or index &gt; self.size:
            return

        prev = self.dummy
        for _ in range(index):
            prev = prev.next

        node = ListNode(val)
        node.next = prev.next
        prev.next = node
        self.size += 1

    def deleteAtIndex(self, index):
        if index &lt; 0 or index &gt;= self.size:
            return

        prev = self.dummy
        for _ in range(index):
            prev = prev.next

        prev.next = prev.next.next
        self.size -= 1
</code></pre>
<hr />
<h1><strong>四、逐个方法讲清楚“为什么这样写”</strong></h1>
<h2><strong>1️⃣</strong></h2>
<h2><strong>get(index)</strong></h2>
<pre><code>if index &lt; 0 or index &gt;= self.size:
    return -1
</code></pre>
<ul>
<li>链表 <strong>不能随机访问</strong></li>
<li>必须从头走 index 步</li>
</ul>
<p>👉 时间复杂度：O(index)</p>
<hr />
<h2><strong>2️⃣</strong></h2>
<h2><strong>addAtHead(val)</strong></h2>
<pre><code>self.addAtIndex(0, val)
</code></pre>
<p>🔥 <strong>关键点</strong>：</p>
<p><strong>所有插入统一走 addAtIndex</strong></p>
<hr />
<h2><strong>3️⃣</strong></h2>
<h2><strong>addAtTail(val)</strong></h2>
<pre><code>self.addAtIndex(self.size, val)
</code></pre>
<p>当 index == size：</p>
<p>👉 插入到尾部（题目明确允许）</p>
<hr />
<h2><strong>4️⃣</strong></h2>
<h2><strong>addAtIndex(index, val)</strong></h2>
<h3><strong>核心逻辑</strong></h3>
<pre><code>prev = self.dummy
for _ in range(index):
    prev = prev.next
</code></pre>
<ul>
<li>找到 <strong>index 前一个节点</strong></li>
<li>插入操作永远是：</li>
</ul>
<pre><code>prev → new → prev.next
</code></pre>
<h3><strong>插入代码</strong></h3>
<pre><code>node.next = prev.next
prev.next = node
</code></pre>
<hr />
<h2><strong>5️⃣</strong></h2>
<h2><strong>deleteAtIndex(index)</strong></h2>
<h3><strong>删除的本质</strong></h3>
<p>删除 index 节点</p>
<p>👉 <strong>找到 index-1 的节点</strong></p>
<pre><code>prev.next = prev.next.next
</code></pre>
<p>dummy 的存在让：</p>
<ul>
<li>删除头节点</li>
<li>删除中间节点</li>
</ul>
<p><strong>写法完全一样</strong></p>
<hr />
<h1><strong>五、为什么 dummy 是“王炸设计”？</strong></h1>
<p>如果不用 dummy：</p>
<ul>
<li>index == 0 要特判</li>
<li>删除头节点要改 head</li>
<li>插入头节点要写两套逻辑</li>
</ul>
<p>用了 dummy：</p>
<blockquote>
<p><strong>所有操作都当“中间节点”处理</strong></p>
</blockquote>
<p>这是链表题的<strong>工程级思维</strong>。</p>
<hr />
<h1><strong>六、复杂度分析</strong></h1>
<table>
<thead>
<tr>
<th><strong>操作</strong></th>
<th><strong>时间复杂度</strong></th>
</tr>
</thead>
<tbody>
<tr>
<td>get</td>
<td>O(n)</td>
</tr>
<tr>
<td>addAtHead</td>
<td>O(1)</td>
</tr>
<tr>
<td>addAtTail</td>
<td>O(n)</td>
</tr>
<tr>
<td>addAtIndex</td>
<td>O(n)</td>
</tr>
<tr>
<td>deleteAtIndex</td>
<td>O(n)</td>
</tr>
</tbody></table><p>空间复杂度：O(1)（不算节点本身）</p>
<hr />
<h1><strong>七、常见翻车点</strong></h1>
<h3><strong>❌ 忘记更新 size</strong></h3>
<pre><code>self.size += 1
self.size -= 1
</code></pre>
<hr />
<h3><strong>❌ index 合法范围写错</strong></h3>
<ul>
<li>插入：index &gt; size ❌</li>
<li>删除：index &gt;= size ❌</li>
</ul>
<hr />
<h3><strong>❌ 删除后还移动指针</strong></h3>
<pre><code>prev = prev.next  # ❌ 删除时不能动
</code></pre>
<hr />
<h1><strong>一句话终极总结</strong></h1>
<blockquote>
<p>通过虚拟头节点和长度记录，可以统一处理所有边界情况，使链表的增删查操作逻辑简洁且稳定。</p>
</blockquote>
<hr />
<p>你选一个，我继续。</p>
]]></content><link href="https://github.com/lihe/MyLeetcode/issues/12"/><category term="链表"/><published>2025-12-28T13:43:27+00:00</published></entry><entry><id>https://github.com/lihe/MyLeetcode/issues/11</id><title>LeetCode 203：移除链表元素</title><updated>2026-03-11T13:58:21.189109+00:00</updated><content type="html"><![CDATA[<p><a href="https://leetcode.cn/problems/remove-linked-list-elements/">https://leetcode.cn/problems/remove-linked-list-elements/</a></p>
<h2><strong>一、这道题的“真正难点”是什么？</strong></h2>
<p>不是删中间节点，而是：</p>
<blockquote>
<p><strong>当要删除的节点可能是头节点时，如何统一处理？</strong></p>
</blockquote>
<p>例如：</p>
<ul>
<li>[6,1,2] 删除 6</li>
<li>[6,6,6] 删除 6</li>
<li>[] 空链表</li>
</ul>
<p>👉 如果你单独处理头节点，代码会变得又臭又长。</p>
<hr />
<h2><strong>二、最稳妥的解法：</strong></h2>
<h2><strong>虚拟头节点（dummy node）</strong></h2>
<h3><strong>一句话思路</strong></h3>
<blockquote>
<p>在原链表前加一个虚拟头节点，用它统一处理所有删除逻辑。</p>
</blockquote>
<hr />
<h2><strong>三、标准解法（强烈推荐）</strong></h2>
<pre><code class="language-python">class Solution:
    def removeElements(self, head, val):
        dummy = ListNode(0)
        dummy.next = head

        cur = dummy
        while cur.next:
            if cur.next.val == val:
                cur.next = cur.next.next
            else:
                cur = cur.next

        return dummy.next
</code></pre>
<hr />
<h2><strong>四、为什么要用 dummy</strong></h2>
<h3><strong>如果不用 dummy，会发生什么？</strong></h3>
<p>你必须写很多特判：</p>
<pre><code>while head and head.val == val:
    head = head.next
</code></pre>
<p>然后再处理后面的节点……</p>
<p>👉 <strong>逻辑分裂，容易漏情况</strong></p>
<hr />
<h3><strong>dummy 的好处</strong></h3>
<pre><code>dummy → head → ...
</code></pre>
<ul>
<li>dummy 永远不删</li>
<li>所有删除操作都变成：</li>
</ul>
<pre><code>cur.next = cur.next.next
</code></pre>
<p>👉 <strong>删除头节点和删除中间节点，写法完全一致</strong></p>
<hr />
<h2><strong>五、手动推演一遍</strong></h2>
<h3><strong>示例 1</strong></h3>
<pre><code>head = [1,2,6,3,4,5,6], val = 6
</code></pre>
<p>加 dummy：</p>
<pre><code>0 → 1 → 2 → 6 → 3 → 4 → 5 → 6
↑
cur
</code></pre>
<p>过程关键点：</p>
<ul>
<li>cur.next = 6 → 删除</li>
<li>cur 不动（因为可能连续是 6）</li>
<li>最终链表：</li>
</ul>
<pre><code>0 → 1 → 2 → 3 → 4 → 5
</code></pre>
<p>返回：</p>
<pre><code>dummy.next → [1,2,3,4,5]
</code></pre>
<hr />
<h3><strong>示例 3（全删）</strong></h3>
<pre><code>head = [7,7,7,7], val = 7
</code></pre>
<p>结果：</p>
<pre><code>dummy → None
</code></pre>
<p>返回 None，正确。</p>
<hr />
<h2><strong>六、为什么是 while cur.next，而不是 while cur？</strong></h2>
<p>因为我们判断的是：</p>
<pre><code>cur.next.val == val
</code></pre>
<p>如果 cur.next 是 None，就不能访问 .val 了。</p>
<p>👉 <strong>这是链表题里非常经典的写法</strong></p>
<hr />
<h2><strong>七、时间 &amp; 空间复杂度</strong></h2>
<ul>
<li>时间复杂度：O(n)</li>
<li>空间复杂度：O(1)（dummy 是常数级）</li>
</ul>
<hr />
<h2><strong>八、常见错误（高频翻车）</strong></h2>
<h3><strong>❌ 错误 1：不处理头节点</strong></h3>
<pre><code>cur = head
while cur.next:
    ...
</code></pre>
<p>👉 头节点如果等于 val，会漏删</p>
<hr />
<h3><strong>❌ 错误 2：删除后还移动 cur</strong></h3>
<pre><code>cur.next = cur.next.next
cur = cur.next   # ❌
</code></pre>
<p>👉 会跳过连续的 val</p>
<hr />
<h3><strong>❌ 错误 3：空链表没处理</strong></h3>
<p>dummy 写法自动解决这个问题。</p>
<h2><strong>九、面试标准回答模板</strong></h2>
<blockquote>
<p>我通过引入一个虚拟头节点来统一处理删除逻辑，从而避免对头节点进行特殊判断。遍历过程中，如果当前节点的下一个节点值等于目标值，就跳过该节点，否则继续向后移动，最终返回虚拟头节点的下一个节点。</p>
</blockquote>
<hr />
<p>看到题目出现：</p>
<ul>
<li>删除链表节点</li>
<li>可能删除头节点</li>
<li>删除条件是某个值</li>
</ul>
<p>👉 <strong>第一反应：dummy 虚拟头节点</strong></p>
]]></content><link href="https://github.com/lihe/MyLeetcode/issues/11"/><category term="链表"/><published>2025-12-28T07:03:29+00:00</published></entry><entry><id>https://github.com/lihe/MyLeetcode/issues/10</id><title>LeetCode 18：四数之和（4Sum）</title><updated>2026-03-11T13:58:21.299016+00:00</updated><content type="html"><![CDATA[<p><a href="https://leetcode.cn/problems/4sum/">
LeetCode 18：四数之和（4Sum）</a></p>
<h2><strong>一、核心思路</strong></h2>
<blockquote>
<p><strong>先排序，然后固定前两个数，把问题转化为一个有序数组上的 Two Sum，用双指针解决，并在每一层做去重。</strong></p>
</blockquote>
<hr />
<h2><strong>二、为什么一定要排序？</strong></h2>
<p>排序后可以：</p>
<ol>
<li>使用 <strong>双指针</strong>（单调性）</li>
<li><strong>天然去重</strong>（相同元素相邻）</li>
<li>做 <strong>剪枝优化</strong>（提前 break / continue）</li>
</ol>
<hr />
<h2><strong>三、标准解法（O(n³)，面试推荐）</strong></h2>
<pre><code class="language-python">class Solution:
    def fourSum(self, nums, target):
        nums.sort()
        n = len(nums)
        res = []

        for i in range(n - 3):
            # 1️⃣ 第一层去重
            if i &gt; 0 and nums[i] == nums[i - 1]:
                continue

            # 剪枝：最小和都大于 target
            if nums[i] + nums[i+1] + nums[i+2] + nums[i+3] &gt; target:
                break
            # 剪枝：最大和都小于 target
            if nums[i] + nums[n-1] + nums[n-2] + nums[n-3] &lt; target:
                continue

            for j in range(i + 1, n - 2):
                # 2️⃣ 第二层去重
                if j &gt; i + 1 and nums[j] == nums[j - 1]:
                    continue

                # 第二层剪枝
                if nums[i] + nums[j] + nums[j+1] + nums[j+2] &gt; target:
                    break
                if nums[i] + nums[j] + nums[n-1] + nums[n-2] &lt; target:
                    continue

                left, right = j + 1, n - 1

                while left &lt; right:
                    total = nums[i] + nums[j] + nums[left] + nums[right]

                    if total == target:
                        res.append([nums[i], nums[j], nums[left], nums[right]])

                        # 3️⃣ 第三层去重（left）
                        while left &lt; right and nums[left] == nums[left + 1]:
                            left += 1
                        # 4️⃣ 第四层去重（right）
                        while left &lt; right and nums[right] == nums[right - 1]:
                            right -= 1

                        left += 1
                        right -= 1

                    elif total &lt; target:
                        left += 1
                    else:
                        right -= 1

        return res
</code></pre>
<hr />
<h2><strong>四、去重逻辑（这是这题的灵魂）</strong></h2>
<h3><strong>一共</strong></h3>
<h3><strong>4 个位置</strong></h3>
<h3><strong>需要考虑去重：</strong></h3>
<table>
<thead>
<tr>
<th><strong>层级</strong></th>
<th><strong>变量</strong></th>
<th><strong>去重方式</strong></th>
</tr>
</thead>
<tbody>
<tr>
<td>第 1 个数</td>
<td>i</td>
<td>nums[i] == nums[i-1]</td>
</tr>
<tr>
<td>第 2 个数</td>
<td>j</td>
<td>nums[j] == nums[j-1]</td>
</tr>
<tr>
<td>第 3 个数</td>
<td>left</td>
<td>while 跳过</td>
</tr>
<tr>
<td>第 4 个数</td>
<td>right</td>
<td>while 跳过</td>
</tr>
</tbody></table><p>👉 <strong>任何一层不去重，都会产生重复四元组</strong></p>
<hr />
<h2><strong>五、手动推演</strong></h2>
<pre><code>nums = [1,0,-1,0,-2,2]
target = 0
</code></pre>
<p>排序后：</p>
<pre><code>[-2, -1, 0, 0, 1, 2]
</code></pre>
<h3><strong>i = -2, j = -1</strong></h3>
<ul>
<li>left = 0, right = 2</li>
<li>sum = -1 → left++</li>
<li>sum = 0 → ✅ [-2, -1, 1, 2]</li>
</ul>
<h3><strong>i = -2, j = 0</strong></h3>
<ul>
<li>sum = 0 → ✅ [-2, 0, 0, 2]</li>
</ul>
<h3><strong>i = -1, j = 0</strong></h3>
<ul>
<li>sum = 0 → ✅ [-1, 0, 0, 1]</li>
</ul>
<hr />
<h2><strong>六、时间 &amp; 空间复杂度</strong></h2>
<ul>
<li>
<p>时间复杂度：</p>
<ul>
<li>排序：O(n log n)</li>
<li>两层循环 + 双指针：O(n³)</li>
</ul>
</li>
<li>
<p>空间复杂度：</p>
<ul>
<li>排序栈空间：O(log n)</li>
<li>输出不计入额外空间</li>
</ul>
</li>
</ul>
<hr />
<h2><strong>七、常见错误（高频翻车点）</strong></h2>
<h3><strong>❌ 错误 1：去重只做一层</strong></h3>
<p>👉 结果中会出现重复四元组</p>
<hr />
<h3><strong>❌ 错误 2：忘记排序</strong></h3>
<p>👉 双指针和去重全部失效</p>
<hr />
<h3><strong>❌ 错误 3：剪枝条件写反</strong></h3>
<pre><code>if min_sum &gt; target: continue  # ❌ 应该 break
</code></pre>
<hr />
<h2><strong>八、从 4Sum 到 kSum（重要升华）</strong></h2>
<h3><strong>本质关系</strong></h3>
<table>
<thead>
<tr>
<th><strong>题目</strong></th>
<th><strong>本质</strong></th>
</tr>
</thead>
<tbody>
<tr>
<td>Two Sum</td>
<td>哈希 / 双指针</td>
</tr>
<tr>
<td>Three Sum</td>
<td>固定 1 个 + Two Sum</td>
</tr>
<tr>
<td>Four Sum</td>
<td>固定 2 个 + Two Sum</td>
</tr>
<tr>
<td>kSum</td>
<td>固定 k-2 个 + Two Sum</td>
</tr>
</tbody></table><hr />
<h3><strong>kSum 通用思想（一句话）</strong></h3>
<blockquote>
<p><strong>排序 + 递归固定前 k-2 个数，底层用双指针做 Two Sum，并在每一层去重。</strong></p>
</blockquote>
<hr />
<h2><strong>九、面试标准回答模板（直接背）</strong></h2>
<blockquote>
<p>我先对数组排序，通过两层循环固定前两个数，再在剩余区间内使用双指针查找另外两个数，使四数之和等于 target。通过在每一层跳过重复元素来保证结果不重复，整体时间复杂度是 O(n³)。</p>
</blockquote>
]]></content><link href="https://github.com/lihe/MyLeetcode/issues/10"/><category term="数组"/><published>2025-12-28T06:37:15+00:00</published></entry><entry><id>https://github.com/lihe/MyLeetcode/issues/9</id><title>LeetCode 15：三数之和（3Sum）</title><updated>2026-03-11T13:58:21.443566+00:00</updated><content type="html"><![CDATA[<p><a href="https://leetcode.cn/problems/3sum/">https://leetcode.cn/problems/3sum/</a></p>
<h2><strong>一、这道题的关键难点是什么？</strong></h2>
<p>不是“能不能找到三个数”，而是：</p>
<blockquote>
<p><strong>如何在不重复的前提下，找全所有和为 0 的三元组</strong></p>
</blockquote>
<p>核心难点只有两个：</p>
<ol>
<li><strong>去重</strong></li>
<li><strong>把暴力 O(n³) 降到 O(n²)</strong></li>
</ol>
<hr />
<h2><strong>二、核心思路</strong></h2>
<h3><strong>一句话版本</strong></h3>
<blockquote>
<p><strong>先排序，然后固定一个数，用双指针在右侧区间内找另外两个数。</strong></p>
</blockquote>
<hr />
<h2><strong>三、为什么要排序？</strong></h2>
<p>排序后可以：</p>
<ol>
<li>使用双指针（利用单调性）</li>
<li>轻松去重（相同元素会相邻）</li>
</ol>
<hr />
<h2><strong>四、标准解法（O(n²)，强烈推荐）</strong></h2>
<pre><code class="language-python">class Solution:
    def threeSum(self, nums):
        nums.sort()
        res = []
        n = len(nums)

        for i in range(n):
            # 1️⃣ 剪枝：最小值已经 &gt; 0，不可能再凑出 0
            if nums[i] &gt; 0:
                break

            # 2️⃣ 去重：跳过重复的 nums[i]
            if i &gt; 0 and nums[i] == nums[i - 1]:
                continue

            left, right = i + 1, n - 1

            while left &lt; right:
                total = nums[i] + nums[left] + nums[right]

                if total == 0:
                    res.append([nums[i], nums[left], nums[right]])

                    # 3️⃣ 去重：跳过重复的 left
                    while left &lt; right and nums[left] == nums[left + 1]:
                        left += 1
                    # 4️⃣ 去重：跳过重复的 right
                    while left &lt; right and nums[right] == nums[right - 1]:
                        right -= 1

                    left += 1
                    right -= 1

                elif total &lt; 0:
                    left += 1
                else:
                    right -= 1

        return res
</code></pre>
<hr />
<h2><strong>五、手动走一遍</strong></h2>
<h3><strong>示例</strong></h3>
<pre><code>nums = [-1,0,1,2,-1,-4]
</code></pre>
<p>排序后：</p>
<pre><code>[-4, -1, -1, 0, 1, 2]
</code></pre>
<h3><strong>i = 1（nums[i] = -1）</strong></h3>
<ul>
<li>left = 2（-1）</li>
<li>right = 5（2）</li>
<li>sum = 0 → 记录 [-1, -1, 2]</li>
</ul>
<p>继续：</p>
<ul>
<li>left = 3（0）</li>
<li>right = 4（1）</li>
<li>sum = 0 → 记录 [-1, 0, 1]</li>
</ul>
<p>完成。</p>
<hr />
<h2><strong>六、为什么这样能避免重复？</strong></h2>
<h3><strong>去重一共</strong></h3>
<h3><strong>三层</strong></h3>
<h4><strong>1️⃣ 固定 i 时去重</strong></h4>
<pre><code>if i &gt; 0 and nums[i] == nums[i - 1]:
    continue
</code></pre>
<p>避免同一个数作为第一个元素重复出现。</p>
<hr />
<h4><strong>2️⃣ left 指针去重</strong></h4>
<pre><code>while left &lt; right and nums[left] == nums[left + 1]:
    left += 1
</code></pre>
<hr />
<h4><strong>3️⃣ right 指针去重</strong></h4>
<pre><code>while left &lt; right and nums[right] == nums[right - 1]:
    right -= 1
</code></pre>
<hr />
<h2><strong>七、时间 &amp; 空间复杂度</strong></h2>
<ul>
<li>
<p>时间复杂度：</p>
<ul>
<li>排序：O(n log n)</li>
<li>双指针：O(n²)</li>
<li>总体：<strong>O(n²)</strong></li>
</ul>
</li>
<li>
<p>空间复杂度：</p>
<ul>
<li>排序：O(log n)（Python 内部）</li>
<li>结果集不算额外空间</li>
</ul>
</li>
</ul>
<hr />
<h2><strong>八、常见错误</strong></h2>
<h3><strong>❌ 错误 1：忘记排序</strong></h3>
<p>👉 双指针失效 + 无法去重</p>
<hr />
<h3><strong>❌ 错误 2：去重写错位置</strong></h3>
<pre><code>if nums[left] == nums[left + 1]: left += 1  # ❌ 只跳一次
</code></pre>
<p>👉 必须用 while</p>
<hr />
<h3><strong>❌ 错误 3：没做剪枝</strong></h3>
<pre><code>if nums[i] &gt; 0: break
</code></pre>
<p>👉 会做很多无效计算</p>
<hr />
<h2><strong>九、面试标准回答模板</strong></h2>
<blockquote>
<p>我先对数组进行排序，然后固定一个数，使用双指针在其右侧查找另外两个数，使三数之和为 0。通过在固定元素和双指针移动过程中跳过重复值，保证结果中不包含重复三元组，时间复杂度为 O(n²)。</p>
</blockquote>
<hr />
<p>看到题目出现：</p>
<ul>
<li>三数之和 / 四数之和</li>
<li>去重</li>
<li>不要求下标，只要组合</li>
</ul>
<p>👉 <strong>排序 + 双指针</strong></p>
]]></content><link href="https://github.com/lihe/MyLeetcode/issues/9"/><category term="数组"/><published>2025-12-28T06:25:58+00:00</published></entry><entry><id>https://github.com/lihe/MyLeetcode/issues/8</id><title>LeetCode 59：螺旋矩阵 II</title><updated>2026-03-11T13:58:21.576725+00:00</updated><content type="html"><![CDATA[<p><a href="https://leetcode.cn/problems/spiral-matrix-ii/">https://leetcode.cn/problems/spiral-matrix-ii/</a></p>
<h2><strong>一、核心思路</strong></h2>
<blockquote>
<p>用 <strong>四个边界</strong> 控制方向：</p>
</blockquote>
<blockquote>
<p>上 → 右 → 下 → 左</p>
</blockquote>
<blockquote>
<p>每走完一条边，就把对应边界往里收缩一圈。</p>
</blockquote>
<hr />
<h2><strong>二、为什么这是最稳的解法？</strong></h2>
<ul>
<li>n ≤ 20，规模很小</li>
<li>只需要 <strong>模拟填数</strong></li>
<li>不需要复杂数据结构</li>
<li>时间复杂度 <strong>O(n²)</strong>（刚好填满矩阵）</li>
</ul>
<hr />
<h2><strong>三、标准解法</strong></h2>
<pre><code class="language-python">class Solution:
    def generateMatrix(self, n):
        matrix = [[0] * n for _ in range(n)]

        top, bottom = 0, n - 1
        left, right = 0, n - 1
        num = 1

        while top &lt;= bottom and left &lt;= right:
            # 1. 从左到右
            for col in range(left, right + 1):
                matrix[top][col] = num
                num += 1
            top += 1

            # 2. 从上到下
            for row in range(top, bottom + 1):
                matrix[row][right] = num
                num += 1
            right -= 1

            # 3. 从右到左
            if top &lt;= bottom:
                for col in range(right, left - 1, -1):
                    matrix[bottom][col] = num
                    num += 1
                bottom -= 1

            # 4. 从下到上
            if left &lt;= right:
                for row in range(bottom, top - 1, -1):
                    matrix[row][left] = num
                    num += 1
                left += 1

        return matrix
</code></pre>
<hr />
<h2><strong>四、手动推演</strong></h2>
<h3><strong>示例：n = 3</strong></h3>
<p>初始：</p>
<pre><code>top=0 bottom=2 left=0 right=2
num=1
</code></pre>
<h3><strong>第一圈：</strong></h3>
<p>1️⃣ 左 → 右（top=0）</p>
<pre><code>[1, 2, 3]
[0, 0, 0]
[0, 0, 0]
</code></pre>
<p>2️⃣ 上 → 下（right=2）</p>
<pre><code>[1, 2, 3]
[0, 0, 4]
[0, 0, 5]
</code></pre>
<p>3️⃣ 右 → 左（bottom=2）</p>
<pre><code>[1, 2, 3]
[0, 0, 4]
[7, 6, 5]
</code></pre>
<p>4️⃣ 下 → 上（left=0）</p>
<pre><code>[1, 2, 3]
[8, 0, 4]
[7, 6, 5]
</code></pre>
<hr />
<h3><strong>第二圈：</strong></h3>
<pre><code>top=1 bottom=1 left=1 right=1
</code></pre>
<p>填中间：</p>
<pre><code>[1, 2, 3]
[8, 9, 4]
[7, 6, 5]
</code></pre>
<hr />
<h2><strong>五、边界判断为什么不能省？</strong></h2>
<p>这两句是<strong>防止重复填 / 越界</strong>的关键：</p>
<pre><code>if top &lt;= bottom:
if left &lt;= right:
</code></pre>
<h3><strong>否则会发生什么？</strong></h3>
<ul>
<li>n 为奇数时</li>
<li>中心点会被重复填</li>
<li>或方向走“反了”</li>
</ul>
<p>👉 <strong>这是本题最常见的翻车点</strong></p>
<hr />
<h2><strong>六、时间 &amp; 空间复杂度</strong></h2>
<ul>
<li>时间复杂度：O(n²)</li>
<li>空间复杂度：O(n²)（输出矩阵）</li>
</ul>
<p>这是 <strong>最优 &amp; 不可避免的</strong>。</p>
<hr />
<h2><strong>七、另一种写法（方向数组法，了解即可）</strong></h2>
<p>思路：</p>
<p>用方向数组 [(0,1),(1,0),(0,-1),(-1,0)] 控制方向</p>
<p>碰壁或已访问就转向</p>
<p>⚠️ 写法更灵活，但更容易出 bug</p>
<p>👉 <strong>面试不如边界法稳</strong></p>
<hr />
<h2><strong>八、常见错误总结</strong></h2>
<h3><strong>❌ 错误 1：忘记边界判断</strong></h3>
<pre><code>for col in range(right, left - 1, -1):  # 没判断
</code></pre>
<hr />
<h3><strong>❌ 错误 2：边界更新顺序错</strong></h3>
<ul>
<li>top += 1、right -= 1 写错位置</li>
<li>会导致覆盖 / 漏填</li>
</ul>
<hr />
<h3><strong>❌ 错误 3：while 条件写错</strong></h3>
<pre><code>while top &lt; bottom and left &lt; right:  # ❌
</code></pre>
<p>👉 会漏掉中间一圈</p>
<hr />
<h2><strong>九、面试标准回答模板</strong></h2>
<blockquote>
<p>我通过维护上下左右四个边界来模拟螺旋填充，每次按照右、下、左、上的顺序填数，并在填完一条边后收缩对应边界，直到所有元素填完。</p>
</blockquote>
<hr />
]]></content><link href="https://github.com/lihe/MyLeetcode/issues/8"/><category term="数组"/><published>2025-12-28T05:57:46+00:00</published></entry><entry><id>https://github.com/lihe/MyLeetcode/issues/7</id><title>LeetCode 209：长度最小的子数组</title><updated>2026-03-11T13:58:21.714329+00:00</updated><content type="html"><![CDATA[<p><a href="https://leetcode.cn/problems/minimum-size-subarray-sum/description/">https://leetcode.cn/problems/minimum-size-subarray-sum/description/</a></p>
<hr />
<h2><strong>一、这题为什么能用 O(n)？</strong></h2>
<p>题目里有一个<strong>隐藏但决定性的条件</strong>：</p>
<blockquote>
<p><strong>nums 中全是正整数</strong></p>
</blockquote>
<p>这意味着：</p>
<ul>
<li>窗口右扩 → 和 <strong>只会变大</strong></li>
<li>窗口左缩 → 和 <strong>只会变小</strong></li>
</ul>
<p>👉 <strong>窗口具有单调性</strong></p>
<p>👉 <strong>可以用滑动窗口，而不是暴力枚举</strong></p>
<hr />
<h2><strong>二、O(n) 解法：滑动窗口</strong></h2>
<h3><strong>核心思想</strong></h3>
<blockquote>
<p>用两个指针维护一个窗口，右指针负责扩展直到满足条件，左指针负责收缩以缩短长度。</p>
</blockquote>
<hr />
<h2><strong>三、标准代码</strong></h2>
<pre><code class="language-python">class Solution:
    def minSubArrayLen(self, target, nums):
        left = 0
        curr_sum = 0
        res = float(&#x27;inf&#x27;)

        for right in range(len(nums)):
            curr_sum += nums[right]

            while curr_sum &gt;= target:
                res = min(res, right - left + 1)
                curr_sum -= nums[left]
                left += 1

        return 0 if res == float(&#x27;inf&#x27;) else res
</code></pre>
<hr />
<h2><strong>四、手动走一遍</strong></h2>
<h3><strong>示例 1</strong></h3>
<pre><code>target = 7
nums = [2,3,1,2,4,3]
</code></pre>
<p>初始化：</p>
<pre><code>left=0, curr_sum=0, res=∞
</code></pre>
<h3><strong>过程关键片段：</strong></h3>
<p>1️⃣ right=3 → curr_sum = 8</p>
<p>满足条件，开始收缩：</p>
<ul>
<li>res = 4</li>
<li>去掉 nums[0]=2 → sum=6（不满足）</li>
</ul>
<p>2️⃣ right=4 → curr_sum = 10</p>
<p>收缩：</p>
<ul>
<li>res = 4</li>
<li>去掉 nums[1]=3 → sum=7</li>
<li>res = 3</li>
<li>去掉 nums[2]=1 → sum=6（停止）</li>
</ul>
<p>3️⃣ right=5 → curr_sum = 9</p>
<p>收缩：</p>
<ul>
<li>res = 3</li>
<li>去掉 nums[3]=2 → sum=7</li>
<li>res = 2</li>
<li>去掉 nums[4]=4 → sum=3</li>
</ul>
<p>最终：</p>
<pre><code>res = 2
</code></pre>
<hr />
<h2><strong>五、为什么时间复杂度是 O(n)？</strong></h2>
<p>虽然看起来有两层循环：</p>
<pre><code>for right in ...
    while curr_sum &gt;= target
</code></pre>
<p>但注意：</p>
<ul>
<li>right 只走一遍</li>
<li>left 也只走一遍</li>
</ul>
<p>👉 <strong>每个元素最多被加一次、减一次</strong></p>
<p>所以是：</p>
<pre><code>O(n)
</code></pre>
<hr />
<h2><strong>六、常见错误</strong></h2>
<h3><strong>❌ 错误 1：用 if 而不是 while</strong></h3>
<pre><code>if curr_sum &gt;= target:   # ❌
</code></pre>
<p>👉 你会错过更短的子数组</p>
<hr />
<h3><strong>❌ 错误 2：没意识到“正整数”是前提</strong></h3>
<p>如果 nums 里有负数：</p>
<ul>
<li>滑动窗口 ❌ 不成立</li>
<li>要换方法（前缀和 + 二分 / 哈希）</li>
</ul>
<hr />
<h3><strong>❌ 错误 3：忘记返回 0 的情况</strong></h3>
<hr />
<h2><strong>七、进阶：O(n log n) 解法</strong></h2>
<h3><strong>思路</strong></h3>
<ul>
<li>用 <strong>前缀和</strong></li>
<li>对每个起点 i</li>
<li>二分找最小的 j，使：</li>
</ul>
<p>$prefix[j] - prefix[i] \ge target$</p>
<hr />
<h3><strong>代码</strong></h3>
<pre><code class="language-python">import bisect

class Solution:
    def minSubArrayLen(self, target, nums):
        n = len(nums)
        prefix = [0] * (n + 1)
        for i in range(n):
            prefix[i + 1] = prefix[i] + nums[i]

        res = float(&#x27;inf&#x27;)
        for i in range(n):
            need = target + prefix[i]
            j = bisect.bisect_left(prefix, need)
            if j &lt;= n:
                res = min(res, j - i)

        return 0 if res == float(&#x27;inf&#x27;) else res
</code></pre>
<hr />
<h3><strong>复杂度</strong></h3>
<ul>
<li>构建前缀和：O(n)</li>
<li>每次二分：O(log n)</li>
<li>总体：O(n log n)</li>
</ul>
<hr />
<h2><strong>八、O(n) vs O(n log n) 怎么选？</strong></h2>
<blockquote>
<p>因为数组元素全为正数，滑动窗口具有单调性，所以可以用 O(n) 的双指针解法。</p>
</blockquote>
<blockquote>
<p>如果不满足单调性，或者需要支持更复杂查询，可以用前缀和加二分。</p>
</blockquote>
<hr />
<h2><strong>九、这道题在“滑动窗口体系”中的地位</strong></h2>
<p>这是 <strong>最标准的滑动窗口模型</strong>：</p>
<ul>
<li>固定条件：sum &gt;= target</li>
<li>优化目标：<strong>窗口长度最小</strong></li>
</ul>
<p>你后面会遇到大量同构题：</p>
<ul>
<li>最长 / 最短子数组</li>
<li>满足条件的子串</li>
<li>至多 / 至少 K 个元素</li>
</ul>
<p>👉 <strong>模板一模一样</strong></p>
<p>看到题目出现：</p>
<ul>
<li>连续子数组</li>
<li>全是正数</li>
<li>求最短 / 最长</li>
</ul>
<p><strong>第一反应：滑动窗口</strong></p>
]]></content><link href="https://github.com/lihe/MyLeetcode/issues/7"/><category term="数组"/><published>2025-12-28T04:42:13+00:00</published></entry><entry><id>https://github.com/lihe/MyLeetcode/issues/6</id><title>LeetCode 704：二分查找（Binary Search）</title><updated>2026-03-11T13:58:21.871539+00:00</updated><content type="html"><![CDATA[<p><a href="https://leetcode.cn/problems/binary-search/">https://leetcode.cn/problems/binary-search/</a></p>
<h2><strong>一、核心结论</strong></h2>
<blockquote>
<p><strong>有序数组 + O(log n)</strong></p>
</blockquote>
<blockquote>
<p>👉 <strong>只能用二分查找</strong></p>
</blockquote>
<hr />
<h2><strong>二、标准二分查找（推荐写法）</strong></h2>
<h3><strong>✅ Python 正确实现（闭区间写法）</strong></h3>
<pre><code class="language-python">class Solution:
    def search(self, nums, target):
        left, right = 0, len(nums) - 1

        while left &lt;= right:
            mid = (left + right) // 2

            if nums[mid] == target:
                return mid
            elif nums[mid] &lt; target:
                left = mid + 1
            else:
                right = mid - 1

        return -1
</code></pre>
<hr />
<h2><strong>三、用示例完整走一遍</strong></h2>
<h3><strong>示例 1</strong></h3>
<pre><code>nums = [-1,0,3,5,9,12]
target = 9
</code></pre>
<table>
<thead>
<tr>
<th><strong>left</strong></th>
<th><strong>right</strong></th>
<th><strong>mid</strong></th>
<th><strong>nums[mid]</strong></th>
<th><strong>操作</strong></th>
</tr>
</thead>
<tbody>
<tr>
<td>0</td>
<td>5</td>
<td>2</td>
<td>3</td>
<td>target &gt; 3 → left=3</td>
</tr>
<tr>
<td>3</td>
<td>5</td>
<td>4</td>
<td>9</td>
<td>命中 → return 4</td>
</tr>
</tbody></table><hr />
<h3><strong>示例 2</strong></h3>
<pre><code>nums = [-1,0,3,5,9,12]
target = 2
</code></pre>
<table>
<thead>
<tr>
<th><strong>left</strong></th>
<th><strong>right</strong></th>
<th><strong>mid</strong></th>
<th><strong>nums[mid]</strong></th>
<th><strong>操作</strong></th>
</tr>
</thead>
<tbody>
<tr>
<td>0</td>
<td>5</td>
<td>2</td>
<td>3</td>
<td>target &lt; 3 → right=1</td>
</tr>
<tr>
<td>0</td>
<td>1</td>
<td>0</td>
<td>-1</td>
<td>target &gt; -1 → left=1</td>
</tr>
<tr>
<td>1</td>
<td>1</td>
<td>1</td>
<td>0</td>
<td>target &gt; 0 → left=2</td>
</tr>
<tr>
<td>2</td>
<td>1</td>
<td>结束</td>
<td></td>
<td>return -1</td>
</tr>
</tbody></table><hr />
<h2><strong>四、为什么时间复杂度是 O(log n)？</strong></h2>
<h3><strong>每一步都在干什么？</strong></h3>
<ul>
<li>把搜索区间 <strong>缩小一半</strong></li>
<li>n → n/2 → n/4 → …</li>
</ul>
<p>查找次数：</p>
<p>$\log_2 n$</p>
<p>👉 所以是 <strong>O(log n)</strong></p>
<hr />
<h2><strong>五、二分查找的三个“铁律”</strong></h2>
<h3><strong>1️⃣ 数组必须有序</strong></h3>
<p>（本题已保证）</p>
<hr />
<h3><strong>2️⃣ 区间定义要统一</strong></h3>
<p>你这版用的是：</p>
<pre><code>[left, right]  闭区间
</code></pre>
<p>所以：</p>
<ul>
<li>while 用 &lt;=</li>
<li>左边收缩：left = mid + 1</li>
<li>右边收缩：right = mid - 1</li>
</ul>
<hr />
<h3><strong>3️⃣ mid 一定要这样算</strong></h3>
<pre><code>mid = (left + right) // 2
</code></pre>
<p>（Python 不会溢出，但这是标准写法）</p>
<hr />
<h2><strong>六、常见错误（90% 翻车点）</strong></h2>
<h3><strong>❌ 错误 1：while 写成</strong></h3>
<h3><strong>&lt;</strong></h3>
<pre><code>while left &lt; right:   # ❌
</code></pre>
<p>👉 会漏掉最后一个元素</p>
<hr />
<h3><strong>❌ 错误 2：区间没对齐</strong></h3>
<pre><code>right = mid      # ❌
</code></pre>
<p>👉 可能死循环</p>
<hr />
<h3><strong>❌ 错误 3：忘记 return -1</strong></h3>
<hr />
<h2><strong>七、另一种写法（左闭右开区间，了解即可）</strong></h2>
<pre><code class="language-python">class Solution:
    def search(self, nums, target):
        left, right = 0, len(nums)

        while left &lt; right:
            mid = (left + right) // 2
            if nums[mid] == target:
                return mid
            elif nums[mid] &lt; target:
                left = mid + 1
            else:
                right = mid

        return -1
</code></pre>
<hr />
<h2><strong>八、面试标准回答模板</strong></h2>
<blockquote>
<p>这是一个典型的二分查找问题。</p>
</blockquote>
<blockquote>
<p>因为数组是有序的，并且要求 O(log n) 的时间复杂度，我使用二分查找，在每一步把搜索区间缩小一半，最终找到目标值或返回 -1。</p>
</blockquote>
<hr />
<h1>进阶</h1>
<h2><strong>一、问题</strong></h2>
<blockquote>
<p><strong>数组有重复元素</strong></p>
</blockquote>
<blockquote>
<p>要求：<strong>返回 target 第一次出现的位置（最左边）</strong></p>
</blockquote>
<p>这是经典问题，通常叫：</p>
<ul>
<li><strong>Binary Search – First Occurrence</strong></li>
<li>或 <strong>lower_bound 的等值版本</strong></li>
</ul>
<hr />
<h2><strong>二、思路</strong></h2>
<blockquote>
<p>在搜索到 target 之后 <strong>不立即返回</strong>，</p>
</blockquote>
<blockquote>
<p><strong>记录 mid</strong>，然后把 right 向左移动一位继续找</p>
</blockquote>
<p>但要注意两点细节：</p>
<ol>
<li><strong>right 不是简单减 1</strong></li>
<li>而是 <strong>right = mid - 1</strong></li>
</ol>
<hr />
<h2><strong>三、正确的“找第一个 target”的二分逻辑</strong></h2>
<h3><strong>关键思想（一句话）</strong></h3>
<blockquote>
<p><strong>当 nums[mid] == target 时，说明答案在左侧（包括 mid），</strong></p>
</blockquote>
<blockquote>
<p><strong>先记下 mid，再继续向左收缩区间。</strong></p>
</blockquote>
<hr />
<h2><strong>四、标准代码</strong></h2>
<pre><code class="language-python">class Solution:
    def searchFirst(self, nums, target):
        left, right = 0, len(nums) - 1
        ans = -1  # 用来记录答案

        while left &lt;= right:
            mid = (left + right) // 2

            if nums[mid] == target:
                ans = mid              # 记录当前命中位置
                right = mid - 1        # 继续向左找
            elif nums[mid] &lt; target:
                left = mid + 1
            else:
                right = mid - 1

        return ans
</code></pre>
<hr />
<h2><strong>五、手动走一遍（你一定要能走）</strong></h2>
<h3><strong>示例</strong></h3>
<pre><code>nums = [1,2,2,2,3,4]
target = 2
</code></pre>
<table>
<thead>
<tr>
<th><strong>left</strong></th>
<th><strong>right</strong></th>
<th><strong>mid</strong></th>
<th><strong>nums[mid]</strong></th>
<th><strong>操作</strong></th>
<th><strong>ans</strong></th>
</tr>
</thead>
<tbody>
<tr>
<td>0</td>
<td>5</td>
<td>2</td>
<td>2</td>
<td>记录，right=1</td>
<td>2</td>
</tr>
<tr>
<td>0</td>
<td>1</td>
<td>0</td>
<td>1</td>
<td>left=1</td>
<td>2</td>
</tr>
<tr>
<td>1</td>
<td>1</td>
<td>1</td>
<td>2</td>
<td>记录，right=0</td>
<td>1</td>
</tr>
<tr>
<td>1</td>
<td>0</td>
<td>结束</td>
<td></td>
<td></td>
<td>1</td>
</tr>
</tbody></table><p>✅ 返回 1（第一个 2）</p>
<hr />
<h2><strong>六、和“普通二分”的本质区别</strong></h2>
<h3><strong>普通二分（无重复）</strong></h3>
<pre><code>if nums[mid] == target:
    return mid
</code></pre>
<hr />
<h3><strong>找第一个（有重复）</strong></h3>
<pre><code>if nums[mid] == target:
    ans = mid
    right = mid - 1
</code></pre>
<p>📌 <strong>区别只在这一点</strong></p>
<hr />
<h2><strong>七、面试官标准听法</strong></h2>
<blockquote>
<p>当数组中存在重复元素时，如果要求返回第一个 target，在二分查找到 target 后不能立即返回，而是记录当前位置，并继续在左半区间查找，最终得到最左侧的 target 下标。</p>
</blockquote>
<p>这句话 <strong>非常标准</strong>。</p>
<hr />
<h2><strong>八、顺手给一个“对称版本”（找最后一个）</strong></h2>
<h3><strong>找最后一个 target（最右）</strong></h3>
<pre><code class="language-python">class Solution:
    def searchLast(self, nums, target):
        left, right = 0, len(nums) - 1
        ans = -1

        while left &lt;= right:
            mid = (left + right) // 2

            if nums[mid] == target:
                ans = mid
                left = mid + 1     # 向右找
            elif nums[mid] &lt; target:
                left = mid + 1
            else:
                right = mid - 1

        return ans
</code></pre>
<hr />
<h2><strong>九、二分查找“进阶口诀”</strong></h2>
<blockquote>
<p><strong>找第一个：命中后右边界左移</strong></p>
</blockquote>
<blockquote>
<p><strong>找最后一个：命中后左边界右移</strong></p>
</blockquote>
<hr />
]]></content><link href="https://github.com/lihe/MyLeetcode/issues/6"/><category term="数组"/><published>2025-12-27T05:54:00+00:00</published></entry><entry><id>https://github.com/lihe/MyLeetcode/issues/5</id><title>LeetCode 215. 数组中的第K个最大元素</title><updated>2026-03-11T13:58:22.019815+00:00</updated><content type="html"><![CDATA[<p><a href="https://leetcode.cn/problems/kth-largest-element-in-an-array/solutions/821137/ji-yu-kuai-pai-de-suo-you-topkwen-ti-jia-ylsd/">https://leetcode.cn/problems/kth-largest-element-in-an-array/solutions/821137/ji-yu-kuai-pai-de-suo-you-topkwen-ti-jia-ylsd/</a></p>
<hr />
<h2><strong>一、TopK 问题的本质</strong></h2>
<blockquote>
<p><strong>TopK = Partition（切分） + 选择（不完全排序）</strong></p>
</blockquote>
<p>不是排序问题，</p>
<p>而是 <strong>“把第 k 个元素放到它最终该在的位置”</strong>。</p>
<hr />
<h2><strong>二、面试中 TopK 的 5 种标准问法</strong></h2>
<table>
<thead>
<tr>
<th><strong>问法</strong></th>
<th><strong>本质</strong></th>
</tr>
</thead>
<tbody>
<tr>
<td>前 k 小的数</td>
<td>切到索引 k-1</td>
</tr>
<tr>
<td>第 k 小的数</td>
<td>找索引 k-1</td>
</tr>
<tr>
<td>前 k 大的数</td>
<td>切到索引 n-k</td>
</tr>
<tr>
<td>第 k 大的数</td>
<td>找索引 n-k</td>
</tr>
<tr>
<td>只排序前 / 后 k 个</td>
<td>Quickselect + 局部排序</td>
</tr>
</tbody></table><p>👉 <strong>90% 的 TopK 都能映射到这 5 个</strong></p>
<hr />
<h2><strong>三、两种主流解法选型</strong></h2>
<h3><strong>1️⃣ 快选（Quickselect）—— 面试首选</strong></h3>
<ul>
<li>平均时间复杂度：O(n)</li>
<li>空间复杂度：O(1)</li>
<li>原地</li>
<li>不需要完全排序</li>
</ul>
<p>📌 <strong>当 k 不接近 n，或强调效率 → 一定选它</strong></p>
<hr />
<h3><strong>2️⃣ 堆（Heap）—— 稳定保底方案</strong></h3>
<ul>
<li>时间复杂度：O(n log k)</li>
<li>适合 <strong>流式数据 / 超大数据 / 在线 TopK</strong></li>
</ul>
<p>📌 面试官追问时你可以说：</p>
<blockquote>
<p>如果是数据流或内存受限，我会用小根堆 / 大根堆。</p>
</blockquote>
<hr />
<h2><strong>四、Partition（核心中的核心）</strong></h2>
<h3><strong>标准挖坑法</strong></h3>
<pre><code class="language-python">def partition(nums, left, right):
    pivot = nums[left]
    i, j = left, right
    while i &lt; j:
        while i &lt; j and nums[j] &gt;= pivot:
            j -= 1
        nums[i] = nums[j]
        while i &lt; j and nums[i] &lt;= pivot:
            i += 1
        nums[j] = nums[i]
    nums[i] = pivot
    return i
</code></pre>
<p>📌 面试官可能追问：</p>
<ul>
<li>
<p>这是 Hoare 还是 Lomuto？</p>
<p>👉 <strong>Hoare 变体（挖坑法）</strong></p>
</li>
</ul>
<hr />
<h2><strong>五、Quickselect 模板</strong></h2>
<blockquote>
<p><strong>k 一定是 0-based 索引</strong></p>
</blockquote>
<pre><code class="language-python">def quickselect(nums, k, left, right):
    if left &lt; right:
        index = partition(nums, left, right)
        if index == k:
            return
        elif index &lt; k:
            quickselect(nums, k, index + 1, right)
        else:
            quickselect(nums, k, left, index - 1)
</code></pre>
<hr />
<h2><strong>六、TopK 全家桶</strong></h2>
<h3><strong>1️⃣ 前 k 小的数</strong></h3>
<pre><code class="language-python">def topk_smalls(nums, k):
    quickselect(nums, k - 1, 0, len(nums) - 1)
    return nums[:k]
</code></pre>
<hr />
<h3><strong>2️⃣ 第 k 小的数</strong></h3>
<pre><code class="language-python">def kth_small(nums, k):
    quickselect(nums, k - 1, 0, len(nums) - 1)
    return nums[k - 1]
</code></pre>
<hr />
<h3><strong>3️⃣ 前 k 大的数</strong></h3>
<pre><code class="language-python">def topk_larges(nums, k):
    n = len(nums)
    quickselect(nums, n - k, 0, n - 1)
    return nums[n - k:]
</code></pre>
<hr />
<h3><strong>4️⃣ 第 k 大的数</strong></h3>
<pre><code class="language-python">def kth_large(nums, k):
    n = len(nums)
    quickselect(nums, n - k, 0, n - 1)
    return nums[n - k]
</code></pre>
<hr />
<h3><strong>5️⃣ 只排序前 k 个小的数</strong></h3>
<pre><code class="language-python">def topk_sort_left(nums, k):
    quickselect(nums, k - 1, 0, len(nums) - 1)
    quicksort(nums, 0, k - 1)
    return nums
</code></pre>
<hr />
<h3><strong>6️⃣ 只排序后 k 个大的数</strong></h3>
<pre><code class="language-python">def topk_sort_right(nums, k):
    n = len(nums)
    quickselect(nums, n - k, 0, n - 1)
    quicksort(nums, n - k, n - 1)
    return nums
</code></pre>
<hr />
<h2><strong>七、k 的语义速查表</strong></h2>
<table>
<thead>
<tr>
<th><strong>人类语义</strong></th>
<th><strong>Quickselect 用的 k</strong></th>
</tr>
</thead>
<tbody>
<tr>
<td>第 k 小</td>
<td>k-1</td>
</tr>
<tr>
<td>前 k 小</td>
<td>k-1</td>
</tr>
<tr>
<td>第 k 大</td>
<td>n-k</td>
</tr>
<tr>
<td>前 k 大</td>
<td>n-k</td>
</tr>
</tbody></table><p>📌 <strong>90% TopK bug 来自这里</strong></p>
<hr />
<h2><strong>八、面试官高频追问 &amp; 标准回答</strong></h2>
<h3><strong>Q1：为什么不用快速排序？</strong></h3>
<blockquote>
<p>因为 TopK 不需要全局有序，只需要第 k 个元素就位。</p>
</blockquote>
<blockquote>
<p>Quickselect 平均时间复杂度是 O(n)，优于排序的 O(n log n)。</p>
</blockquote>
<hr />
<h3><strong>Q2：Quickselect 最坏情况？</strong></h3>
<blockquote>
<p>最坏是 O(n^2)，可以通过随机 pivot 或三数取中优化。</p>
</blockquote>
<hr />
<h3><strong>Q3：什么时候用堆？</strong></h3>
<blockquote>
<p>数据流、k 很小、或者无法一次性加载全部数据时。</p>
</blockquote>
<hr />
<h3><strong>Q4：TopK 会稳定吗？</strong></h3>
<blockquote>
<p>不稳定。Quickselect 和 Heap 都不保证稳定性。</p>
</blockquote>
<hr />
<h2><strong>九、30 秒面试答题心法</strong></h2>
<blockquote>
<p>TopK 本质是选择问题而不是排序问题。</p>
</blockquote>
<blockquote>
<p>我会使用基于 Partition 的 Quickselect，在平均 O(n) 时间内把第 k 个元素放到正确位置。</p>
</blockquote>
<blockquote>
<p>如果需要排序前 / 后 k 个元素，只在局部再做一次排序即可。</p>
</blockquote>
<hr />
<h1><strong>进阶：无序数组找中位数（字节高频题）</strong></h1>
<hr />
<h2><strong>一、题目描述（面试原型）</strong></h2>
<blockquote>
<p>给定一个<strong>无序数组</strong>，在 <strong>不排序整个数组</strong> 的前提下，找到数组的 <strong>中位数</strong>，并分析时间复杂度。</p>
</blockquote>
<hr />
<h2><strong>二、「一句话答案」</strong></h2>
<blockquote>
<p>无序数组找中位数是一个<strong>选择问题</strong>，</p>
</blockquote>
<blockquote>
<p>可以用 <strong>Quickselect（基于 Partition）</strong>，</p>
</blockquote>
<blockquote>
<p>平均时间复杂度是 <strong>O(n)</strong>，空间复杂度 <strong>O(1)</strong>。</p>
</blockquote>
<hr />
<h2><strong>三、中位数 = 第 k 小元素（关键映射）</strong></h2>
<p>设数组长度为 n（0-based 索引）：</p>
<h3><strong>1️⃣ n 为奇数</strong></h3>
<pre><code>中位数 = 第 (n//2 + 1) 小
索引 = n//2
</code></pre>
<h3><strong>2️⃣ n 为偶数（两种情况）</strong></h3>
<table>
<thead>
<tr>
<th><strong>面试要求</strong></th>
<th><strong>处理方式</strong></th>
</tr>
</thead>
<tbody>
<tr>
<td>任意一个中位数</td>
<td>找第 n//2 小</td>
</tr>
<tr>
<td>数学中位数</td>
<td>找第 n//2 和 n//2 - 1，取平均</td>
</tr>
</tbody></table><hr />
<h2><strong>四、标准解法：Quickselect（核心）</strong></h2>
<h3><strong>1️⃣ 为什么不用排序？</strong></h3>
<blockquote>
<p>排序是 O(n log n)，</p>
</blockquote>
<blockquote>
<p>但中位数只需要一个元素的位置，不需要全局有序。</p>
</blockquote>
<p>这是面试官<strong>一定会追问</strong>的点。</p>
<hr />
<h3><strong>2️⃣ Quickselect 思想</strong></h3>
<blockquote>
<p>每一次 partition，都能确定一个元素的最终位置</p>
</blockquote>
<blockquote>
<p>如果这个位置正好是中位数索引，直接返回</p>
</blockquote>
<blockquote>
<p>否则只递归一边</p>
</blockquote>
<hr />
<h2><strong>五、代码模板</strong></h2>
<h3><strong>Partition（挖坑法）</strong></h3>
<pre><code class="language-python">def partition(nums, left, right):
    pivot = nums[left]
    i, j = left, right
    while i &lt; j:
        while i &lt; j and nums[j] &gt;= pivot:
            j -= 1
        nums[i] = nums[j]
        while i &lt; j and nums[i] &lt;= pivot:
            i += 1
        nums[j] = nums[i]
    nums[i] = pivot
    return i
</code></pre>
<hr />
<h3><strong>Quickselect</strong></h3>
<pre><code class="language-python">def quickselect(nums, k, left, right):
    if left &lt; right:
        index = partition(nums, left, right)
        if index == k:
            return
        elif index &lt; k:
            quickselect(nums, k, index + 1, right)
        else:
            quickselect(nums, k, left, index - 1)
</code></pre>
<hr />
<h3><strong>找中位数（奇数长度）</strong></h3>
<pre><code class="language-python">def find_median(nums):
    n = len(nums)
    k = n // 2
    quickselect(nums, k, 0, n - 1)
    return nums[k]
</code></pre>
<hr />
<h2><strong>六、时间复杂度分析</strong></h2>
<h3><strong>平均时间复杂度：✅ O(n)</strong></h3>
<ul>
<li>每次 partition 是 O(n)</li>
<li>但只递归一半</li>
<li>所以是线性期望</li>
</ul>
<hr />
<h3><strong>最坏时间复杂度：⚠️ O(n²)</strong></h3>
<ul>
<li>pivot 每次都选到极端值（如已排序数组）</li>
</ul>
<hr />
<h3><strong>优化一句话</strong></h3>
<blockquote>
<p>可以通过 <strong>随机选 pivot</strong> 或 <strong>三数取中</strong>，</p>
</blockquote>
<blockquote>
<p>将最坏情况概率极大降低。</p>
</blockquote>
<hr />
<h2><strong>七、面试官可能的追问 &amp; 标准回答</strong></h2>
<h3><strong>Q1：如果是数据流，怎么找中位数？</strong></h3>
<blockquote>
<p>使用两个堆：</p>
</blockquote>
<ul>
<li>
<blockquote>
<p>一个大根堆存左半部分</p>
</blockquote>
</li>
<li>
<blockquote>
<p>一个小根堆存右半部分</p>
</blockquote>
<blockquote>
<p>动态维护平衡</p>
</blockquote>
</li>
</ul>
<hr />
<h3><strong>Q2：为什么 Quickselect 不稳定？</strong></h3>
<blockquote>
<p>因为 Partition 会交换相等元素的相对顺序，</p>
</blockquote>
<blockquote>
<p>不保证稳定性。</p>
</blockquote>
<hr />
<h3><strong>Q3：你会不会用堆来做？</strong></h3>
<blockquote>
<p>会，但堆是 O(n log n) 或 O(n log k)，在静态数组中不如 Quickselect 高效。</p>
</blockquote>
<hr />
<h2><strong>八、30 秒「字节标准回答模板」（可直接背）</strong></h2>
<blockquote>
<p>无序数组找中位数可以转化为选择问题。</p>
</blockquote>
<blockquote>
<p>我会使用基于 Partition 的 Quickselect 算法，在平均 O(n) 时间内把中位数放到正确位置。</p>
</blockquote>
<blockquote>
<p>相比排序的 O(n log n)，效率更高。</p>
</blockquote>
<blockquote>
<p>最坏情况是 O(n²)，可以通过随机 pivot 优化。</p>
</blockquote>
<hr />
]]></content><link href="https://github.com/lihe/MyLeetcode/issues/5"/><category term="数组"/><published>2025-12-25T14:54:28+00:00</published></entry><entry><id>https://github.com/lihe/MyLeetcode/issues/1</id><title>LeetCode 206 反转链表</title><updated>2026-03-11T13:58:22.170579+00:00</updated><content type="html"><![CDATA[<p><a href="https://leetcode.cn/problems/reverse-linked-list/description/">https://leetcode.cn/problems/reverse-linked-list/description/</a></p>
<h2><strong>一、题目回顾</strong></h2>
<p><strong>问题</strong></p>
<p>给定单链表头节点 head，反转链表并返回反转后的头节点。</p>
<hr />
<h2><strong>二、两种标准解法概览</strong></h2>
<h3><strong>1️⃣ 迭代法（首插法 / 三指针）</strong></h3>
<pre><code class="language-python">class Solution:
    def reverseList(self, head: Optional[ListNode]) -&gt; Optional[ListNode]:
        pre = None
        cur = head
        while cur:
            nxt = cur.next
            cur.next = pre
            pre = cur
            cur = nxt
        return pre
</code></pre>
<p><strong>核心思想</strong></p>
<p>不断把当前节点 cur 插到新链表头部 pre 前。</p>
<hr />
<h3><strong>2️⃣ 递归法（重点）</strong></h3>
<pre><code class="language-python">class Solution:
    def reverseList(self, head: Optional[ListNode]) -&gt; Optional[ListNode]:
        if head is None or head.next is None:
            return head
        
        rev_head = self.reverseList(head.next)
        tail = head.next
        tail.next = head
        head.next = None
        return rev_head
</code></pre>
<hr />
<h2><strong>三、递归反转链表的本质理解</strong></h2>
<h3><strong>递归一句话定义</strong></h3>
<blockquote>
<p><strong>我不反转整个链表，只负责把“当前节点”接到“已经反转好的后半段”后面。</strong></p>
</blockquote>
<hr />
<h2><strong>四、递归执行的完整过程（示例）</strong></h2>
<p>链表：</p>
<pre><code>1 → 2 → 3 → None
</code></pre>
<p>调用：</p>
<pre><code>reverseList(1)
</code></pre>
<hr />
<h3><strong>1️⃣ 递归下潜（只做调用，不反转）</strong></h3>
<pre><code>reverse(1)
 → reverse(2)
   → reverse(3)
</code></pre>
<p>在 head = 3 时触发 base case：</p>
<pre><code>if head.next is None:
    return head
</code></pre>
<p>返回节点 3</p>
<hr />
<h3><strong>2️⃣ 回溯阶段（真正发生反转）</strong></h3>
<h4><strong>回到head = 2</strong></h4>
<pre><code>tail = head.next      # 3
tail.next = head      # 3 → 2
head.next = None      # 2 → None
</code></pre>
<p>链表变为：</p>
<pre><code>3 → 2 → None
</code></pre>
<p>返回 rev_head = 3</p>
<hr />
<h4><strong>回到head = 1</strong></h4>
<pre><code>tail = head.next      # 2
tail.next = head      # 2 → 1
head.next = None      # 1 → None
</code></pre>
<p>最终链表：</p>
<pre><code>3 → 2 → 1 → None
</code></pre>
<hr />
<h2><strong>五、为什么始终返回 rev_head？</strong></h2>
<ul>
<li>rev_head <strong>永远指向反转后链表的头节点</strong></li>
<li>它来自递归最深处（原链表尾部）</li>
<li>后续每一层只是在“接节点”，<strong>不改变头指针</strong></li>
</ul>
<p>❌ 错误理解：</p>
<p>“当前 head 是新的头” —— 这是错的</p>
<hr />
<h2><strong>六、为什么必须写 head.next = None？</strong></h2>
<p>如果省略这一句：</p>
<pre><code>tail.next = head
# 没有 head.next = None
</code></pre>
<p>会发生：</p>
<pre><code>1 ↔ 2 ↔ 3
</code></pre>
<p>→ <strong>形成环</strong></p>
<p>✅ 正确作用：</p>
<blockquote>
<p>断开原有正向指针，避免产生环</p>
</blockquote>
<hr />
<h2><strong>七、递归反转链表的「通用模板」</strong></h2>
<p>遇到链表递归题，直接套：</p>
<p>1️⃣ <strong>Base Case</strong></p>
<p>空链表 / 单节点直接返回</p>
<p>2️⃣ <strong>递归调用</strong></p>
<p>先处理 head.next</p>
<p>3️⃣ <strong>局部操作</strong></p>
<p>修改当前节点与后继节点的指针关系</p>
<p>4️⃣ <strong>返回结果</strong></p>
<p>始终返回“最深层”的头节点</p>
<hr />
<h2><strong>八、迭代 vs 递归（建立直觉）</strong></h2>
<table>
<thead>
<tr>
<th><strong>方法</strong></th>
<th><strong>思维方式</strong></th>
</tr>
</thead>
<tbody>
<tr>
<td>迭代</td>
<td>我一步一步往前推</td>
</tr>
<tr>
<td>递归</td>
<td>我相信后面已经反转好了</td>
</tr>
</tbody></table><hr />
<h2><strong>九、🔥 高阶考点：带环链表会发生什么？</strong></h2>
<h3><strong>输入：带环链表</strong></h3>
<pre><code>A → B → C → D → E
        ↑       ↓
        └───────┘
</code></pre>
<hr />
<h3><strong>✅ 结论（面试官标准答案）</strong></h3>
<p><strong>结论 1：不会死循环</strong></p>
<ul>
<li>不会无限递归</li>
<li>不会栈溢出</li>
</ul>
<p><strong>结论 2：最终结果是</strong></p>
<blockquote>
<p><strong>环内被反转，环外保持原样</strong></p>
</blockquote>
<hr />
<h2><strong>十、为什么不会死循环？</strong></h2>
<p>关键不在「有没有环」，而在这里：</p>
<pre><code>head.next = None
</code></pre>
<h3><strong>关键机制</strong></h3>
<ul>
<li>递归下潜时：<strong>不会修改指针</strong></li>
<li>第一次回溯时：</li>
</ul>
<pre><code>head.next = None
</code></pre>
<ul>
<li>👉 <strong>直接剪断环</strong></li>
</ul>
<p>一旦环被破坏：</p>
<ul>
<li>后续递归不会再绕回</li>
<li>递归自然结束</li>
</ul>
<p>📌 加分表述：</p>
<blockquote>
<p>虽然输入是带环链表，但递归反转在回溯阶段会主动破坏环结构，因此不会死循环。</p>
</blockquote>
<hr />
<h2><strong>十一、为什么“环内反转，环外不动”？</strong></h2>
<p>原因只有一句话：</p>
<blockquote>
<p><strong>只有参与回溯的节点，才会执行反转逻辑</strong></p>
</blockquote>
<ul>
<li>递归深入到环内后，才开始回溯</li>
<li>回溯只覆盖环内节点</li>
<li>环外节点的 next 从未被置为 None</li>
<li>因此顺序保持不变</li>
</ul>
<hr />
<h3><strong>最终结构示意</strong></h3>
<p>原始：</p>
<pre><code>A → B → C → D → E
        ↑       ↓
        └───────┘
</code></pre>
<p>结果：</p>
<pre><code>A → B → C ← D ← E
        |
       None
</code></pre>
<hr />
<h2><strong>十二、面试满分回答模板（可直接背）</strong></h2>
<blockquote>
<p>对于带环链表，这个递归反转函数不会死循环。</p>
</blockquote>
<blockquote>
<p>因为在回溯阶段会执行 head.next = None，第一次回溯就会破坏环结构，使递归能够正常终止。</p>
</blockquote>
<blockquote>
<p>最终结果是：环内节点被反转，环外节点保持原有顺序不变。</p>
</blockquote>
]]></content><link href="https://github.com/lihe/MyLeetcode/issues/1"/><category term="链表"/><published>2025-12-24T12:54:45+00:00</published></entry></feed>