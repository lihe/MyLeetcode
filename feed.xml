<?xml version='1.0' encoding='UTF-8'?>
<feed xmlns="http://www.w3.org/2005/Atom"><id>https://github.com/lihe/MyLeetcode</id><title>RSS feed of lihe's MyLeetcode</title><updated>2026-01-06T13:48:45.757353+00:00</updated><link href="https://github.com/lihe/MyLeetcode"/><link href="https://raw.githubusercontent.com/lihe/MyLeetcode/master/feed.xml" rel="self"/><generator uri="https://lkiesow.github.io/python-feedgen" version="1.0.0">python-feedgen</generator><entry><id>https://github.com/lihe/MyLeetcode/issues/17</id><title>LeetCode 541：反转字符串 II</title><updated>2026-01-06T13:48:46.267314+00:00</updated><content type="html"><![CDATA[<p><a href="https://leetcode.cn/problems/reverse-string-ii/">LeetCode 541：反转字符串 II</a></p>
<hr />
<h2><strong>一、题意快速翻译</strong></h2>
<blockquote>
<p>从字符串起始位置开始，每 <strong>2k</strong> 个字符为一组，<strong>只反转这一组中的前 k 个字符</strong>，其余按规则处理直到结束。</p>
</blockquote>
<hr />
<h2><strong>二、核心规则拆解</strong></h2>
<p>对字符串从左到右，每次看 <strong>2k</strong> 个字符：</p>
<ol>
<li>
<p><strong>剩余字符 &lt; k</strong></p>
<ul>
<li>👉 <strong>全部反转</strong></li>
</ul>
</li>
<li>
<p><strong>k ≤ 剩余字符 &lt; 2k</strong></p>
<ul>
<li>👉 <strong>只反转前 k 个</strong></li>
<li>👉 后面的保持不变</li>
</ul>
</li>
<li>
<p><strong>剩余字符 ≥ 2k</strong></p>
<ul>
<li>👉 <strong>反转前 k 个</strong></li>
<li>👉 后 k 个不动</li>
</ul>
</li>
</ol>
<hr />
<h2><strong>三、算法思路</strong></h2>
<h3><strong>思路关键词</strong></h3>
<ul>
<li>步长：2k</li>
<li>操作对象：[i, i+k)</li>
<li>原地 or 转 list 处理都可以</li>
</ul>
<h3><strong>步骤</strong></h3>
<ol>
<li>把字符串转成字符数组（方便修改）</li>
<li>从 i = 0 开始，每次 i += 2k</li>
<li>对区间 [i, min(i+k, n)) 做反转</li>
</ol>
<hr />
<h2><strong>四、Python 标准实现（面试推荐写法）</strong></h2>
<pre><code class="language-Python">def reverseStr(s: str, k: int) -&gt; str:
    s = list(s)
    n = len(s)

    for i in range(0, n, 2 * k):
        # 反转 [i, i+k)
        s[i:i+k] = reversed(s[i:i+k])

    return &quot;&quot;.join(s)
</code></pre>
<hr />
<h2><strong>五、为什么这一行就够了？</strong></h2>
<pre><code>s[i:i+k] = reversed(s[i:i+k])
</code></pre>
<p>因为：</p>
<ul>
<li>
<p>Python 切片 <strong>自动处理越界</strong></p>
</li>
<li>
<p>当剩余字符：</p>
<ul>
<li>&lt; k → 切片只取剩余部分（全部反转）</li>
<li>≥ k → 正好反转前 k 个</li>
</ul>
</li>
<li>
<p>完美覆盖所有题目条件 ✅</p>
</li>
</ul>
<hr />
<h2><strong>六、示例走一遍（加深理解）</strong></h2>
<h3><strong>示例</strong></h3>
<pre><code>s = &quot;abcdefg&quot;, k = 2
</code></pre>
<h3><strong>过程</strong></h3>
<ul>
<li>i = 0 → &quot;ab&quot; 反转 → &quot;ba&quot;</li>
<li>i = 4 → &quot;ef&quot; 反转 → &quot;fe&quot;</li>
<li>剩余 &quot;g&quot; &lt; k → 全反转（还是 &quot;g&quot;）</li>
</ul>
<h3><strong>结果</strong></h3>
<pre><code>&quot;bacdfeg&quot;
</code></pre>
<hr />
<h2><strong>七、复杂度分析</strong></h2>
<ul>
<li>
<p><strong>时间复杂度</strong>：O(n)</p>
</li>
<li>
<p><strong>空间复杂度</strong>：</p>
<ul>
<li>Python：O(n)（字符串不可变）</li>
<li>若允许原地字符数组：O(1) 额外空间</li>
</ul>
</li>
</ul>
<hr />
<h2><strong>八、面试官级总结</strong></h2>
<blockquote>
<p>本题通过每次以 2k 为步长遍历字符串，并对每一段的前 k 个字符进行反转即可。利用切片和越界特性可以统一处理所有边界情况，时间复杂度为 O(n)，实现简洁且高效。</p>
</blockquote>
<hr />
]]></content><link href="https://github.com/lihe/MyLeetcode/issues/17"/><category term="字符串"/><published>2026-01-06T13:48:12+00:00</published></entry><entry><id>https://github.com/lihe/MyLeetcode/issues/16</id><title>LeetCode 142：环形链表 II（找环的入口）</title><updated>2026-01-06T13:48:46.472870+00:00</updated><content type="html"><![CDATA[<p><a href="https://leetcode.cn/problems/linked-list-cycle-ii/">https://leetcode.cn/problems/linked-list-cycle-ii/</a></p>
<h2><strong>一、先给结论</strong></h2>
<blockquote>
<p><strong>先用快慢指针判断是否有环；</strong></p>
</blockquote>
<blockquote>
<p><strong>若有环，让一个指针从头节点出发，另一个从相遇点出发，</strong></p>
</blockquote>
<blockquote>
<p><strong>两者每次走一步，再次相遇的节点就是环的入口。</strong></p>
</blockquote>
<hr />
<h2><strong>二、标准解法（O(n) 时间 + O(1) 空间）</strong></h2>
<pre><code class="language-python">class Solution:
    def detectCycle(self, head):
        slow = fast = head

        # 1️⃣ 先判断是否有环
        while fast and fast.next:
            slow = slow.next
            fast = fast.next.next
            if slow is fast:
                break
        else:
            # 没有环
            return None

        # 2️⃣ 找环的入口
        slow = head
        while slow is not fast:
            slow = slow.next
            fast = fast.next

        return slow
</code></pre>
<p>✔ 不修改链表</p>
<p>✔ 不用额外空间</p>
<p>✔ 满足题目进阶要求</p>
<hr />
<h2><strong>三、为什么“重新从头走”就能找到入口？（这是灵魂）</strong></h2>
<h3><strong>定义几个距离（非常关键）</strong></h3>
<p>假设：</p>
<pre><code>head → … → 入环点 → … → 相遇点 → … → 入环点
</code></pre>
<p>定义：</p>
<ul>
<li>a：头节点 → 入环点 的距离</li>
<li>b：入环点 → 相遇点 的距离</li>
<li>c：相遇点 → 入环点 的距离</li>
<li>环长度：b + c</li>
</ul>
<hr />
<h3><strong>快慢指针第一次相遇时，发生了什么？</strong></h3>
<ul>
<li>慢指针走了：a + b</li>
<li>快指针走了：a + b + k(b + c)（多转了 k 圈）</li>
</ul>
<p>因为快指针速度是慢指针的 <strong>2 倍</strong>：</p>
<p>2(a + b) = a + b + k(b + c)</p>
<p>整理：</p>
<p>a = k(b + c) - b = (k-1)(b+c) + c</p>
<p>👉 <strong>关键结论：</strong></p>
<blockquote>
<p>a ≡ c (mod 环长度)</p>
</blockquote>
<p>也就是说：</p>
<ul>
<li>从 <strong>头节点走 a 步</strong></li>
<li>从 <strong>相遇点走 c 步</strong></li>
</ul>
<p>👉 <strong>都会到达入环点</strong></p>
<hr />
<h2><strong>四、这就是第二阶段的逻辑</strong></h2>
<pre><code>slow = head      # 走 a 步
fast = 相遇点    # 走 c 步

while slow != fast:
    slow = slow.next
    fast = fast.next
</code></pre>
<p>两者 <strong>必然在入环点相遇</strong>。</p>
<hr />
<h2><strong>五、手动推演一遍</strong></h2>
<p>示例：</p>
<pre><code>1 → 2 → 3 → 4 → 5
          ↑       ↓
          ← ← ← ←
</code></pre>
<ul>
<li>第一次相遇在 4</li>
<li>slow 回到 1</li>
<li>fast 留在 4</li>
</ul>
<p>走：</p>
<table>
<thead>
<tr>
<th><strong>步数</strong></th>
<th><strong>slow</strong></th>
<th><strong>fast</strong></th>
</tr>
</thead>
<tbody>
<tr>
<td>1</td>
<td>2</td>
<td>5</td>
</tr>
<tr>
<td>2</td>
<td>3</td>
<td>3 ✅</td>
</tr>
</tbody></table><p>👉 3 是入环点</p>
<hr />
<h2><strong>六、为什么不能直接“记住访问过的节点”？</strong></h2>
<pre><code>visited = set()
</code></pre>
<p>❌ 空间复杂度 O(n)</p>
<p>❌ 不满足题目进阶要求</p>
<p>❌ 面试会被扣分</p>
<hr />
<h2><strong>七、为什么不能修改链表？</strong></h2>
<p>题目明确要求：</p>
<blockquote>
<p><strong>不允许修改链表</strong></p>
</blockquote>
<p>所以以下都 ❌：</p>
<ul>
<li>改 next</li>
<li>打标记</li>
<li>反转链表</li>
</ul>
<hr />
<h2><strong>八、常见翻车点（非常重要）</strong></h2>
<h3><strong>❌ 错误 1：没用</strong></h3>
<h3><strong>else</strong></h3>
<h3><strong>判断无环</strong></h3>
<pre><code>while fast and fast.next:
    ...
# 直接进入第二阶段 ❌
</code></pre>
<p>👉 如果无环会死循环 / 空指针</p>
<hr />
<h3><strong>❌ 错误 2：第二阶段快指针没动</strong></h3>
<pre><code>slow = head
# fast 不动 ❌
</code></pre>
<p>👉 永远不相遇</p>
<hr />
<h3><strong>❌ 错误 3：用值判断</strong></h3>
<pre><code>slow.val == fast.val  # ❌
</code></pre>
<p>👉 必须是节点引用比较</p>
<hr />
<h2><strong>九、时间 &amp; 空间复杂度</strong></h2>
<ul>
<li>时间复杂度：O(n)</li>
<li>空间复杂度：O(1)</li>
</ul>
<hr />
<h2><strong>十、面试标准回答模板</strong></h2>
<blockquote>
<p>我先使用快慢指针判断链表是否存在环，如果存在，当快慢指针相遇后，将其中一个指针重新指向头节点，然后两个指针每次同时走一步，它们再次相遇的位置就是环的入口节点。</p>
</blockquote>
<hr />
]]></content><link href="https://github.com/lihe/MyLeetcode/issues/16"/><category term="链表"/><published>2026-01-04T15:10:56+00:00</published></entry><entry><id>https://github.com/lihe/MyLeetcode/issues/15</id><title>LeetCode 160：相交链表</title><updated>2026-01-06T13:48:46.881182+00:00</updated><content type="html"><![CDATA[<p><a href="https://leetcode.cn/problems/intersection-of-two-linked-lists/submissions/687890317/">https://leetcode.cn/problems/intersection-of-two-linked-lists/submissions/687890317/</a></p>
<h2><strong>一、先给结论</strong></h2>
<blockquote>
<p><strong>用两个指针分别从 headA 和 headB 出发，走到末尾后切换到另一条链表的头，如果两链表相交，两个指针一定会在交点相遇；如果不相交，最终都会走到 null。</strong></p>
</blockquote>
<hr />
<h2><strong>二、最优解法：双指针「换头法」（O(n) + O(1)）</strong></h2>
<h3><strong>标准代码（面试级）</strong></h3>
<pre><code class="language-python">class Solution:
    def getIntersectionNode(self, headA, headB):
        pA, pB = headA, headB

        while pA is not pB:
            pA = pA.next if pA else headB
            pB = pB.next if pB else headA

        return pA  # 可能是相交节点，也可能是 None
</code></pre>
<p>✔ 不改链表结构</p>
<p>✔ 不用额外空间</p>
<p>✔ 一趟“等效遍历”</p>
<hr />
<h2><strong>三、为什么这个方法一定成立？</strong></h2>
<h3><strong>关键事实（一定要理解）</strong></h3>
<ul>
<li>
<p>如果两个链表相交</p>
<p>👉 <strong>相交之后的所有节点是完全共享的</strong></p>
</li>
<li>
<p>两条链表长度可能不同</p>
<p>👉 前半段“独占部分”长度不同</p>
</li>
</ul>
<hr />
<h3><strong>核心思想：</strong></h3>
<h3><strong>让两指针“走过相同的总路程”</strong></h3>
<p>设：</p>
<ul>
<li>链表 A 长度 = a + c</li>
<li>链表 B 长度 = b + c</li>
<li>c 是公共尾部长度</li>
</ul>
<h4><strong>指针走的路径是：</strong></h4>
<pre><code>pA: A → B
pB: B → A
</code></pre>
<p>两者总路程都是：</p>
<pre><code>a + c + b
</code></pre>
<p>👉 <strong>在第二次进入公共尾部时，一定同时到达交点</strong></p>
<hr />
<h2><strong>四、用图形直觉理解</strong></h2>
<p>假设：</p>
<pre><code>A: a1 → a2 → c1 → c2 → c3
B: b1 → b2 → b3 → c1 → c2 → c3
</code></pre>
<p>第一次走：</p>
<ul>
<li>pA 先到 c1</li>
<li>pB 还没到</li>
</ul>
<p>换头后：</p>
<ul>
<li>pA 走 b1 b2 b3</li>
<li>pB 走 a1 a2</li>
</ul>
<p>👉 当两者都进入 c1 时：</p>
<pre><code>pA == pB == c1
</code></pre>
<hr />
<h2><strong>五、如果不相交会发生什么？</strong></h2>
<pre><code>A: a1 → a2 → a3 → None
B: b1 → b2 → None
</code></pre>
<p>路径：</p>
<ul>
<li>pA: A → B → None</li>
<li>pB: B → A → None</li>
</ul>
<p>👉 <strong>最终同时为 None</strong></p>
<pre><code>return None
</code></pre>
<p>符合题意。</p>
<hr />
<h2><strong>六、为什么不能用「值相等」判断？</strong></h2>
<p>题目明确强调：</p>
<blockquote>
<p><strong>相交是“节点相同”，不是“值相同”</strong></p>
</blockquote>
<p>所以判断必须是：</p>
<pre><code>pA is pB
</code></pre>
<p>而不是：</p>
<pre><code>pA.val == pB.val  # ❌
</code></pre>
<hr />
<h2><strong>七、为什么不能改链表结构？</strong></h2>
<p>题目要求：</p>
<blockquote>
<p><strong>返回结果后，链表必须保持原始结构</strong></p>
</blockquote>
<p>所以以下做法全部 ❌：</p>
<ul>
<li>改 next</li>
<li>反转链表</li>
<li>做标记</li>
</ul>
<hr />
<h2><strong>八、常见错误（高频翻车点）</strong></h2>
<h3><strong>❌ 错误 1：只遍历一次</strong></h3>
<pre><code>while pA and pB:
</code></pre>
<p>👉 长度不同，永远对不齐</p>
<hr />
<h3><strong>❌ 错误 2：不换头</strong></h3>
<pre><code>pA = pA.next
pB = pB.next
</code></pre>
<p>👉 只能对齐长度相同的链表</p>
<hr />
<h3><strong>❌ 错误 3：用 set / hash</strong></h3>
<pre><code>visited = set()
</code></pre>
<p>👉 空间复杂度 O(n)，不满足进阶</p>
<hr />
<h2><strong>九、另一种解法（了解即可）：长度对齐法</strong></h2>
<p>思路：</p>
<ol>
<li>分别算长度</li>
<li>长链表先走差值</li>
<li>同步走找交点</li>
</ol>
<p>✔ 正确</p>
<p>❌ 代码更长</p>
<p>❌ 两次遍历</p>
<p>👉 <strong>不如换头法优雅</strong></p>
<hr />
<h2><strong>十、面试标准回答模板</strong></h2>
<blockquote>
<p>使用双指针的方法，让两个指针分别从两条链表的头开始遍历，当走到末尾时切换到另一条链表的头。这样可以让两个指针走过相同的总路径长度，如果链表相交，它们一定会在交点相遇；如果不相交，则最终都会指向 null。</p>
</blockquote>
<hr />
]]></content><link href="https://github.com/lihe/MyLeetcode/issues/15"/><category term="链表"/><published>2025-12-29T08:52:46+00:00</published></entry><entry><id>https://github.com/lihe/MyLeetcode/issues/14</id><title>LeetCode 19：删除链表的倒数第 N 个节点</title><updated>2026-01-06T13:48:47.058862+00:00</updated><content type="html"><![CDATA[<p><a href="https://leetcode.cn/problems/remove-nth-node-from-end-of-list/">https://leetcode.cn/problems/remove-nth-node-from-end-of-list/</a></p>
<h2><strong>一、题目真正考什么？</strong></h2>
<p>不是“删节点”，而是：</p>
<blockquote>
<p><strong>如何在一次遍历中，定位“倒数第 n 个节点的前一个节点”</strong></p>
</blockquote>
<p>这件事用普通遍历是做不到的，</p>
<p>必须用 <strong>快慢指针制造“距离差”</strong>。</p>
<hr />
<h2><strong>二、核心思路</strong></h2>
<blockquote>
<p>使用快慢指针，让快指针先走 n 步，然后快慢指针一起走，当快指针到达末尾时，慢指针正好指向要删除节点的前一个节点。</p>
</blockquote>
<hr />
<h2><strong>三、为什么一定要用 dummy（虚拟头节点）？</strong></h2>
<p>因为要删除的节点 <strong>可能是头节点</strong>：</p>
<ul>
<li>[1], n = 1</li>
<li>[1,2], n = 2</li>
</ul>
<p>👉 dummy 可以 <strong>统一处理所有情况</strong>，不用特判。</p>
<hr />
<h2><strong>四、标准解法（⭐一趟扫描，强烈推荐）</strong></h2>
<pre><code class="language-python">class Solution:
    def removeNthFromEnd(self, head, n):
        dummy = ListNode(0)
        dummy.next = head

        fast = dummy
        slow = dummy

        # 1️⃣ fast 先走 n 步
        for _ in range(n):
            fast = fast.next

        # 2️⃣ fast 和 slow 一起走
        while fast.next:
            fast = fast.next
            slow = slow.next

        # 3️⃣ 删除倒数第 n 个节点
        slow.next = slow.next.next

        return dummy.next
</code></pre>
<hr />
<h2><strong>五、指针推演</strong></h2>
<h3><strong>示例 1</strong></h3>
<pre><code>head = [1,2,3,4,5], n = 2
</code></pre>
<p>加 dummy：</p>
<pre><code>dummy → 1 → 2 → 3 → 4 → 5
↑
fast, slow
</code></pre>
<hr />
<h3><strong>1️⃣ fast 先走 n = 2 步</strong></h3>
<pre><code>dummy → 1 → 2 → 3 → 4 → 5
          ↑
         fast
↑
slow
</code></pre>
<hr />
<h3><strong>2️⃣ fast、slow 一起走</strong></h3>
<p>直到 fast.next == None：</p>
<pre><code>dummy → 1 → 2 → 3 → 4 → 5
                    ↑
                   fast
               ↑
              slow
</code></pre>
<hr />
<h3><strong>3️⃣ 删除 slow.next（也就是 4）</strong></h3>
<pre><code>slow.next = slow.next.next
</code></pre>
<p>结果：</p>
<pre><code>dummy → 1 → 2 → 3 → 5
</code></pre>
<p>返回 dummy.next。</p>
<hr />
<h2><strong>六、为什么这是“一趟扫描”？</strong></h2>
<p>虽然代码看起来有两段循环，但：</p>
<ul>
<li>fast 指针：只走一遍</li>
<li>slow 指针：只走一遍</li>
</ul>
<p>👉 <strong>每个节点最多访问一次</strong></p>
<p>👉 时间复杂度仍然是 O(n)</p>
<hr />
<h2><strong>七、时间 &amp; 空间复杂度</strong></h2>
<ul>
<li>时间复杂度：O(n)</li>
<li>空间复杂度：O(1)</li>
</ul>
<hr />
<h2><strong>八、常见翻车点（非常重要）</strong></h2>
<h3><strong>❌ 错误 1：不用 dummy</strong></h3>
<pre><code>fast = head
slow = head
</code></pre>
<p>👉 删除头节点时必崩</p>
<hr />
<h3><strong>❌ 错误 2：fast 走 n-1 步</strong></h3>
<pre><code>for _ in range(n-1):  # ❌
</code></pre>
<p>👉 删除位置偏一位</p>
<hr />
<h3><strong>❌ 错误 3：while fast 而不是 fast.next</strong></h3>
<pre><code>while fast:   # ❌
</code></pre>
<p>👉 slow 会多走一步</p>
<hr />
<h2><strong>面试标准回答模板</strong></h2>
<blockquote>
<p>我使用快慢指针的方法，让快指针先走 n 步，然后快慢指针同步前进，当快指针到达链表末尾时，慢指针正好位于待删除节点的前一个节点，从而可以在一趟遍历中完成删除操作。</p>
</blockquote>
]]></content><link href="https://github.com/lihe/MyLeetcode/issues/14"/><category term="链表"/><published>2025-12-29T08:13:36+00:00</published></entry><entry><id>https://github.com/lihe/MyLeetcode/issues/13</id><title>LeetCode 24：两两交换链表中的节点</title><updated>2026-01-06T13:48:47.276160+00:00</updated><content type="html"><![CDATA[<p><a href="https://leetcode.cn/problems/swap-nodes-in-pairs/">https://leetcode.cn/problems/swap-nodes-in-pairs/</a></p>
<h2><strong>一、题目核心约束（先抓重点）</strong></h2>
<ul>
<li>❌ 不能交换 val</li>
<li>✅ 只能交换 <strong>节点本身（next 指针）</strong></li>
<li>每次交换 <strong>相邻两个节点</strong></li>
<li>节点个数可能是 0 / 1 / 奇数 / 偶数</li>
</ul>
<p>👉 这直接排除了“偷懒交换值”的做法。</p>
<hr />
<h2><strong>二、最稳解法：</strong></h2>
<h2><strong>dummy + 迭代（强烈推荐）</strong></h2>
<h3><strong>一句话思路</strong></h3>
<blockquote>
<p>使用一个虚拟头节点，把链表每两个节点当成一组，通过指针重连完成交换，然后移动到下一组。</p>
</blockquote>
<hr />
<h2><strong>三、标准代码（面试级，零特判）</strong></h2>
<pre><code class="language-python">class Solution:
    def swapPairs(self, head):
        dummy = ListNode(0)
        dummy.next = head
        prev = dummy

        while prev.next and prev.next.next:
            first = prev.next
            second = first.next

            # 交换两个节点
            prev.next = second
            first.next = second.next
            second.next = first

            # 移动到下一组
            prev = first

        return dummy.next
</code></pre>
<hr />
<h2><strong>四、指针交换理解</strong></h2>
<h3><strong>初始状态（示例 1）</strong></h3>
<pre><code>dummy → 1 → 2 → 3 → 4
          ↑
         prev
</code></pre>
<hr />
<h3><strong>第一次交换（1 和 2）</strong></h3>
<pre><code>prev.next = 2
1.next = 3
2.next = 1
</code></pre>
<p>结果：</p>
<pre><code>dummy → 2 → 1 → 3 → 4
               ↑
              prev
</code></pre>
<blockquote>
<p>⚠️ 注意：prev 移动到 <strong>交换后这一组的第二个节点（first）</strong></p>
</blockquote>
<hr />
<h3><strong>第二次交换（3 和 4）</strong></h3>
<pre><code>dummy → 2 → 1 → 4 → 3
</code></pre>
<p>结束。</p>
<hr />
<h2><strong>五、为什么一定要用 dummy？</strong></h2>
<p>如果不用 dummy：</p>
<ul>
<li>交换第一组时</li>
<li>你必须单独处理头节点</li>
<li>代码会分成两套逻辑</li>
</ul>
<p>👉 <strong>dummy 的作用：统一所有情况</strong></p>
<blockquote>
<p>删除 / 交换头节点 = 删除 / 交换中间节点</p>
</blockquote>
<p>这是链表题的“工程级技巧”。</p>
<hr />
<h2><strong>六、while 条件为什么是这样？</strong></h2>
<pre><code>while prev.next and prev.next.next:
</code></pre>
<p>含义：</p>
<ul>
<li>
<p>至少还剩 <strong>两个节点</strong></p>
</li>
<li>
<p>自动处理：</p>
<ul>
<li>空链表 []</li>
<li>单节点 [1]</li>
</ul>
</li>
</ul>
<hr />
<h2><strong>七、时间 &amp; 空间复杂度</strong></h2>
<ul>
<li>时间复杂度：O(n)</li>
<li>空间复杂度：O(1)（迭代写法）</li>
</ul>
<hr />
<h2><strong>八、递归解法（理解用，可选）</strong></h2>
<h3><strong>思路</strong></h3>
<ul>
<li>交换前两个</li>
<li>剩下的递归处理</li>
</ul>
<h3><strong>代码</strong></h3>
<pre><code class="language-python">class Solution:
    def swapPairs(self, head):
        if not head or not head.next:
            return head

        first = head
        second = head.next

        first.next = self.swapPairs(second.next)
        second.next = first

        return second
</code></pre>
<p>📌 面试推荐 <strong>迭代版</strong>，因为无额外栈空间。</p>
<hr />
<h2><strong>九、常见翻车点（非常重要）</strong></h2>
<h3><strong>❌ 错误 1：交换顺序写错</strong></h3>
<p>指针顺序一错就断链 / 成环。</p>
<hr />
<h3><strong>❌ 错误 2：prev 移动错</strong></h3>
<pre><code>prev = second  # ❌ 会死循环
</code></pre>
<hr />
<h3><strong>❌ 错误 3：交换 val（违规）</strong></h3>
<pre><code>a.val, b.val = b.val, a.val  # ❌
</code></pre>
<hr />
<h2><strong>十、面试标准回答模板</strong></h2>
<blockquote>
<p>通过引入一个虚拟头节点来统一处理头节点的交换问题，在遍历过程中每次将相邻的两个节点重新连接，并将指针移动到下一组，整个过程只修改 next 指针，不修改节点值，时间复杂度是 O(n)。</p>
</blockquote>
<hr />
<p>看到题目出现：</p>
<ul>
<li>链表</li>
<li>成对 / 成组操作</li>
<li>不能改值</li>
</ul>
<p>👉 <strong>dummy + 成组指针重连</strong></p>
<hr />
]]></content><link href="https://github.com/lihe/MyLeetcode/issues/13"/><category term="链表"/><published>2025-12-29T07:56:19+00:00</published></entry><entry><id>https://github.com/lihe/MyLeetcode/issues/12</id><title>LeetCode 707：设计链表</title><updated>2026-01-06T13:48:47.473786+00:00</updated><content type="html"><![CDATA[<p><a href="https://leetcode.cn/problems/design-linked-list/description/">https://leetcode.cn/problems/design-linked-list/description/</a></p>
<h1><strong>一、设计选择</strong></h1>
<p>👉 <strong>选择：单链表 + 虚拟头节点（dummy） + 记录长度</strong></p>
<p>为什么？</p>
<table>
<thead>
<tr>
<th><strong>设计</strong></th>
<th><strong>好处</strong></th>
</tr>
</thead>
<tbody>
<tr>
<td>单链表</td>
<td>结构简单、好写</td>
</tr>
<tr>
<td>dummy</td>
<td>所有操作不用特判 index=0</td>
</tr>
<tr>
<td>size</td>
<td>快速判断 index 合法性</td>
</tr>
</tbody></table><p>这套设计是 <strong>最稳、最不容易出 bug 的</strong>。</p>
<hr />
<h1><strong>二、链表节点定义</strong></h1>
<pre><code class="language-python">class ListNode:
    def __init__(self, val=0, next=None):
        self.val = val
        self.next = next
</code></pre>
<hr />
<h1><strong>三、MyLinkedList 完整实现</strong></h1>
<pre><code class="language-python">class MyLinkedList:

    def __init__(self):
        self.dummy = ListNode(0)
        self.size = 0

    def get(self, index):
        if index &lt; 0 or index &gt;= self.size:
            return -1

        cur = self.dummy.next
        for _ in range(index):
            cur = cur.next
        return cur.val

    def addAtHead(self, val):
        self.addAtIndex(0, val)

    def addAtTail(self, val):
        self.addAtIndex(self.size, val)

    def addAtIndex(self, index, val):
        if index &lt; 0 or index &gt; self.size:
            return

        prev = self.dummy
        for _ in range(index):
            prev = prev.next

        node = ListNode(val)
        node.next = prev.next
        prev.next = node
        self.size += 1

    def deleteAtIndex(self, index):
        if index &lt; 0 or index &gt;= self.size:
            return

        prev = self.dummy
        for _ in range(index):
            prev = prev.next

        prev.next = prev.next.next
        self.size -= 1
</code></pre>
<hr />
<h1><strong>四、逐个方法讲清楚“为什么这样写”</strong></h1>
<h2><strong>1️⃣</strong></h2>
<h2><strong>get(index)</strong></h2>
<pre><code>if index &lt; 0 or index &gt;= self.size:
    return -1
</code></pre>
<ul>
<li>链表 <strong>不能随机访问</strong></li>
<li>必须从头走 index 步</li>
</ul>
<p>👉 时间复杂度：O(index)</p>
<hr />
<h2><strong>2️⃣</strong></h2>
<h2><strong>addAtHead(val)</strong></h2>
<pre><code>self.addAtIndex(0, val)
</code></pre>
<p>🔥 <strong>关键点</strong>：</p>
<p><strong>所有插入统一走 addAtIndex</strong></p>
<hr />
<h2><strong>3️⃣</strong></h2>
<h2><strong>addAtTail(val)</strong></h2>
<pre><code>self.addAtIndex(self.size, val)
</code></pre>
<p>当 index == size：</p>
<p>👉 插入到尾部（题目明确允许）</p>
<hr />
<h2><strong>4️⃣</strong></h2>
<h2><strong>addAtIndex(index, val)</strong></h2>
<h3><strong>核心逻辑</strong></h3>
<pre><code>prev = self.dummy
for _ in range(index):
    prev = prev.next
</code></pre>
<ul>
<li>找到 <strong>index 前一个节点</strong></li>
<li>插入操作永远是：</li>
</ul>
<pre><code>prev → new → prev.next
</code></pre>
<h3><strong>插入代码</strong></h3>
<pre><code>node.next = prev.next
prev.next = node
</code></pre>
<hr />
<h2><strong>5️⃣</strong></h2>
<h2><strong>deleteAtIndex(index)</strong></h2>
<h3><strong>删除的本质</strong></h3>
<p>删除 index 节点</p>
<p>👉 <strong>找到 index-1 的节点</strong></p>
<pre><code>prev.next = prev.next.next
</code></pre>
<p>dummy 的存在让：</p>
<ul>
<li>删除头节点</li>
<li>删除中间节点</li>
</ul>
<p><strong>写法完全一样</strong></p>
<hr />
<h1><strong>五、为什么 dummy 是“王炸设计”？</strong></h1>
<p>如果不用 dummy：</p>
<ul>
<li>index == 0 要特判</li>
<li>删除头节点要改 head</li>
<li>插入头节点要写两套逻辑</li>
</ul>
<p>用了 dummy：</p>
<blockquote>
<p><strong>所有操作都当“中间节点”处理</strong></p>
</blockquote>
<p>这是链表题的<strong>工程级思维</strong>。</p>
<hr />
<h1><strong>六、复杂度分析</strong></h1>
<table>
<thead>
<tr>
<th><strong>操作</strong></th>
<th><strong>时间复杂度</strong></th>
</tr>
</thead>
<tbody>
<tr>
<td>get</td>
<td>O(n)</td>
</tr>
<tr>
<td>addAtHead</td>
<td>O(1)</td>
</tr>
<tr>
<td>addAtTail</td>
<td>O(n)</td>
</tr>
<tr>
<td>addAtIndex</td>
<td>O(n)</td>
</tr>
<tr>
<td>deleteAtIndex</td>
<td>O(n)</td>
</tr>
</tbody></table><p>空间复杂度：O(1)（不算节点本身）</p>
<hr />
<h1><strong>七、常见翻车点</strong></h1>
<h3><strong>❌ 忘记更新 size</strong></h3>
<pre><code>self.size += 1
self.size -= 1
</code></pre>
<hr />
<h3><strong>❌ index 合法范围写错</strong></h3>
<ul>
<li>插入：index &gt; size ❌</li>
<li>删除：index &gt;= size ❌</li>
</ul>
<hr />
<h3><strong>❌ 删除后还移动指针</strong></h3>
<pre><code>prev = prev.next  # ❌ 删除时不能动
</code></pre>
<hr />
<h1><strong>一句话终极总结</strong></h1>
<blockquote>
<p>通过虚拟头节点和长度记录，可以统一处理所有边界情况，使链表的增删查操作逻辑简洁且稳定。</p>
</blockquote>
<hr />
<p>你选一个，我继续。</p>
]]></content><link href="https://github.com/lihe/MyLeetcode/issues/12"/><category term="链表"/><published>2025-12-28T13:43:27+00:00</published></entry><entry><id>https://github.com/lihe/MyLeetcode/issues/11</id><title>LeetCode 203：移除链表元素</title><updated>2026-01-06T13:48:47.685957+00:00</updated><content type="html"><![CDATA[<p><a href="https://leetcode.cn/problems/remove-linked-list-elements/">https://leetcode.cn/problems/remove-linked-list-elements/</a></p>
<h2><strong>一、这道题的“真正难点”是什么？</strong></h2>
<p>不是删中间节点，而是：</p>
<blockquote>
<p><strong>当要删除的节点可能是头节点时，如何统一处理？</strong></p>
</blockquote>
<p>例如：</p>
<ul>
<li>[6,1,2] 删除 6</li>
<li>[6,6,6] 删除 6</li>
<li>[] 空链表</li>
</ul>
<p>👉 如果你单独处理头节点，代码会变得又臭又长。</p>
<hr />
<h2><strong>二、最稳妥的解法：</strong></h2>
<h2><strong>虚拟头节点（dummy node）</strong></h2>
<h3><strong>一句话思路</strong></h3>
<blockquote>
<p>在原链表前加一个虚拟头节点，用它统一处理所有删除逻辑。</p>
</blockquote>
<hr />
<h2><strong>三、标准解法（强烈推荐）</strong></h2>
<pre><code class="language-python">class Solution:
    def removeElements(self, head, val):
        dummy = ListNode(0)
        dummy.next = head

        cur = dummy
        while cur.next:
            if cur.next.val == val:
                cur.next = cur.next.next
            else:
                cur = cur.next

        return dummy.next
</code></pre>
<hr />
<h2><strong>四、为什么要用 dummy</strong></h2>
<h3><strong>如果不用 dummy，会发生什么？</strong></h3>
<p>你必须写很多特判：</p>
<pre><code>while head and head.val == val:
    head = head.next
</code></pre>
<p>然后再处理后面的节点……</p>
<p>👉 <strong>逻辑分裂，容易漏情况</strong></p>
<hr />
<h3><strong>dummy 的好处</strong></h3>
<pre><code>dummy → head → ...
</code></pre>
<ul>
<li>dummy 永远不删</li>
<li>所有删除操作都变成：</li>
</ul>
<pre><code>cur.next = cur.next.next
</code></pre>
<p>👉 <strong>删除头节点和删除中间节点，写法完全一致</strong></p>
<hr />
<h2><strong>五、手动推演一遍</strong></h2>
<h3><strong>示例 1</strong></h3>
<pre><code>head = [1,2,6,3,4,5,6], val = 6
</code></pre>
<p>加 dummy：</p>
<pre><code>0 → 1 → 2 → 6 → 3 → 4 → 5 → 6
↑
cur
</code></pre>
<p>过程关键点：</p>
<ul>
<li>cur.next = 6 → 删除</li>
<li>cur 不动（因为可能连续是 6）</li>
<li>最终链表：</li>
</ul>
<pre><code>0 → 1 → 2 → 3 → 4 → 5
</code></pre>
<p>返回：</p>
<pre><code>dummy.next → [1,2,3,4,5]
</code></pre>
<hr />
<h3><strong>示例 3（全删）</strong></h3>
<pre><code>head = [7,7,7,7], val = 7
</code></pre>
<p>结果：</p>
<pre><code>dummy → None
</code></pre>
<p>返回 None，正确。</p>
<hr />
<h2><strong>六、为什么是 while cur.next，而不是 while cur？</strong></h2>
<p>因为我们判断的是：</p>
<pre><code>cur.next.val == val
</code></pre>
<p>如果 cur.next 是 None，就不能访问 .val 了。</p>
<p>👉 <strong>这是链表题里非常经典的写法</strong></p>
<hr />
<h2><strong>七、时间 &amp; 空间复杂度</strong></h2>
<ul>
<li>时间复杂度：O(n)</li>
<li>空间复杂度：O(1)（dummy 是常数级）</li>
</ul>
<hr />
<h2><strong>八、常见错误（高频翻车）</strong></h2>
<h3><strong>❌ 错误 1：不处理头节点</strong></h3>
<pre><code>cur = head
while cur.next:
    ...
</code></pre>
<p>👉 头节点如果等于 val，会漏删</p>
<hr />
<h3><strong>❌ 错误 2：删除后还移动 cur</strong></h3>
<pre><code>cur.next = cur.next.next
cur = cur.next   # ❌
</code></pre>
<p>👉 会跳过连续的 val</p>
<hr />
<h3><strong>❌ 错误 3：空链表没处理</strong></h3>
<p>dummy 写法自动解决这个问题。</p>
<h2><strong>九、面试标准回答模板</strong></h2>
<blockquote>
<p>我通过引入一个虚拟头节点来统一处理删除逻辑，从而避免对头节点进行特殊判断。遍历过程中，如果当前节点的下一个节点值等于目标值，就跳过该节点，否则继续向后移动，最终返回虚拟头节点的下一个节点。</p>
</blockquote>
<hr />
<p>看到题目出现：</p>
<ul>
<li>删除链表节点</li>
<li>可能删除头节点</li>
<li>删除条件是某个值</li>
</ul>
<p>👉 <strong>第一反应：dummy 虚拟头节点</strong></p>
]]></content><link href="https://github.com/lihe/MyLeetcode/issues/11"/><category term="链表"/><published>2025-12-28T07:03:29+00:00</published></entry><entry><id>https://github.com/lihe/MyLeetcode/issues/10</id><title>LeetCode 18：四数之和（4Sum）</title><updated>2026-01-06T13:48:47.892998+00:00</updated><content type="html"><![CDATA[<p><a href="https://leetcode.cn/problems/4sum/">
LeetCode 18：四数之和（4Sum）</a></p>
<h2><strong>一、核心思路</strong></h2>
<blockquote>
<p><strong>先排序，然后固定前两个数，把问题转化为一个有序数组上的 Two Sum，用双指针解决，并在每一层做去重。</strong></p>
</blockquote>
<hr />
<h2><strong>二、为什么一定要排序？</strong></h2>
<p>排序后可以：</p>
<ol>
<li>使用 <strong>双指针</strong>（单调性）</li>
<li><strong>天然去重</strong>（相同元素相邻）</li>
<li>做 <strong>剪枝优化</strong>（提前 break / continue）</li>
</ol>
<hr />
<h2><strong>三、标准解法（O(n³)，面试推荐）</strong></h2>
<pre><code class="language-python">class Solution:
    def fourSum(self, nums, target):
        nums.sort()
        n = len(nums)
        res = []

        for i in range(n - 3):
            # 1️⃣ 第一层去重
            if i &gt; 0 and nums[i] == nums[i - 1]:
                continue

            # 剪枝：最小和都大于 target
            if nums[i] + nums[i+1] + nums[i+2] + nums[i+3] &gt; target:
                break
            # 剪枝：最大和都小于 target
            if nums[i] + nums[n-1] + nums[n-2] + nums[n-3] &lt; target:
                continue

            for j in range(i + 1, n - 2):
                # 2️⃣ 第二层去重
                if j &gt; i + 1 and nums[j] == nums[j - 1]:
                    continue

                # 第二层剪枝
                if nums[i] + nums[j] + nums[j+1] + nums[j+2] &gt; target:
                    break
                if nums[i] + nums[j] + nums[n-1] + nums[n-2] &lt; target:
                    continue

                left, right = j + 1, n - 1

                while left &lt; right:
                    total = nums[i] + nums[j] + nums[left] + nums[right]

                    if total == target:
                        res.append([nums[i], nums[j], nums[left], nums[right]])

                        # 3️⃣ 第三层去重（left）
                        while left &lt; right and nums[left] == nums[left + 1]:
                            left += 1
                        # 4️⃣ 第四层去重（right）
                        while left &lt; right and nums[right] == nums[right - 1]:
                            right -= 1

                        left += 1
                        right -= 1

                    elif total &lt; target:
                        left += 1
                    else:
                        right -= 1

        return res
</code></pre>
<hr />
<h2><strong>四、去重逻辑（这是这题的灵魂）</strong></h2>
<h3><strong>一共</strong></h3>
<h3><strong>4 个位置</strong></h3>
<h3><strong>需要考虑去重：</strong></h3>
<table>
<thead>
<tr>
<th><strong>层级</strong></th>
<th><strong>变量</strong></th>
<th><strong>去重方式</strong></th>
</tr>
</thead>
<tbody>
<tr>
<td>第 1 个数</td>
<td>i</td>
<td>nums[i] == nums[i-1]</td>
</tr>
<tr>
<td>第 2 个数</td>
<td>j</td>
<td>nums[j] == nums[j-1]</td>
</tr>
<tr>
<td>第 3 个数</td>
<td>left</td>
<td>while 跳过</td>
</tr>
<tr>
<td>第 4 个数</td>
<td>right</td>
<td>while 跳过</td>
</tr>
</tbody></table><p>👉 <strong>任何一层不去重，都会产生重复四元组</strong></p>
<hr />
<h2><strong>五、手动推演</strong></h2>
<pre><code>nums = [1,0,-1,0,-2,2]
target = 0
</code></pre>
<p>排序后：</p>
<pre><code>[-2, -1, 0, 0, 1, 2]
</code></pre>
<h3><strong>i = -2, j = -1</strong></h3>
<ul>
<li>left = 0, right = 2</li>
<li>sum = -1 → left++</li>
<li>sum = 0 → ✅ [-2, -1, 1, 2]</li>
</ul>
<h3><strong>i = -2, j = 0</strong></h3>
<ul>
<li>sum = 0 → ✅ [-2, 0, 0, 2]</li>
</ul>
<h3><strong>i = -1, j = 0</strong></h3>
<ul>
<li>sum = 0 → ✅ [-1, 0, 0, 1]</li>
</ul>
<hr />
<h2><strong>六、时间 &amp; 空间复杂度</strong></h2>
<ul>
<li>
<p>时间复杂度：</p>
<ul>
<li>排序：O(n log n)</li>
<li>两层循环 + 双指针：O(n³)</li>
</ul>
</li>
<li>
<p>空间复杂度：</p>
<ul>
<li>排序栈空间：O(log n)</li>
<li>输出不计入额外空间</li>
</ul>
</li>
</ul>
<hr />
<h2><strong>七、常见错误（高频翻车点）</strong></h2>
<h3><strong>❌ 错误 1：去重只做一层</strong></h3>
<p>👉 结果中会出现重复四元组</p>
<hr />
<h3><strong>❌ 错误 2：忘记排序</strong></h3>
<p>👉 双指针和去重全部失效</p>
<hr />
<h3><strong>❌ 错误 3：剪枝条件写反</strong></h3>
<pre><code>if min_sum &gt; target: continue  # ❌ 应该 break
</code></pre>
<hr />
<h2><strong>八、从 4Sum 到 kSum（重要升华）</strong></h2>
<h3><strong>本质关系</strong></h3>
<table>
<thead>
<tr>
<th><strong>题目</strong></th>
<th><strong>本质</strong></th>
</tr>
</thead>
<tbody>
<tr>
<td>Two Sum</td>
<td>哈希 / 双指针</td>
</tr>
<tr>
<td>Three Sum</td>
<td>固定 1 个 + Two Sum</td>
</tr>
<tr>
<td>Four Sum</td>
<td>固定 2 个 + Two Sum</td>
</tr>
<tr>
<td>kSum</td>
<td>固定 k-2 个 + Two Sum</td>
</tr>
</tbody></table><hr />
<h3><strong>kSum 通用思想（一句话）</strong></h3>
<blockquote>
<p><strong>排序 + 递归固定前 k-2 个数，底层用双指针做 Two Sum，并在每一层去重。</strong></p>
</blockquote>
<hr />
<h2><strong>九、面试标准回答模板（直接背）</strong></h2>
<blockquote>
<p>我先对数组排序，通过两层循环固定前两个数，再在剩余区间内使用双指针查找另外两个数，使四数之和等于 target。通过在每一层跳过重复元素来保证结果不重复，整体时间复杂度是 O(n³)。</p>
</blockquote>
]]></content><link href="https://github.com/lihe/MyLeetcode/issues/10"/><category term="数组"/><published>2025-12-28T06:37:15+00:00</published></entry><entry><id>https://github.com/lihe/MyLeetcode/issues/9</id><title>LeetCode 15：三数之和（3Sum）</title><updated>2026-01-06T13:48:48.129900+00:00</updated><content type="html"><![CDATA[<p><a href="https://leetcode.cn/problems/3sum/">https://leetcode.cn/problems/3sum/</a></p>
<h2><strong>一、这道题的关键难点是什么？</strong></h2>
<p>不是“能不能找到三个数”，而是：</p>
<blockquote>
<p><strong>如何在不重复的前提下，找全所有和为 0 的三元组</strong></p>
</blockquote>
<p>核心难点只有两个：</p>
<ol>
<li><strong>去重</strong></li>
<li><strong>把暴力 O(n³) 降到 O(n²)</strong></li>
</ol>
<hr />
<h2><strong>二、核心思路</strong></h2>
<h3><strong>一句话版本</strong></h3>
<blockquote>
<p><strong>先排序，然后固定一个数，用双指针在右侧区间内找另外两个数。</strong></p>
</blockquote>
<hr />
<h2><strong>三、为什么要排序？</strong></h2>
<p>排序后可以：</p>
<ol>
<li>使用双指针（利用单调性）</li>
<li>轻松去重（相同元素会相邻）</li>
</ol>
<hr />
<h2><strong>四、标准解法（O(n²)，强烈推荐）</strong></h2>
<pre><code class="language-python">class Solution:
    def threeSum(self, nums):
        nums.sort()
        res = []
        n = len(nums)

        for i in range(n):
            # 1️⃣ 剪枝：最小值已经 &gt; 0，不可能再凑出 0
            if nums[i] &gt; 0:
                break

            # 2️⃣ 去重：跳过重复的 nums[i]
            if i &gt; 0 and nums[i] == nums[i - 1]:
                continue

            left, right = i + 1, n - 1

            while left &lt; right:
                total = nums[i] + nums[left] + nums[right]

                if total == 0:
                    res.append([nums[i], nums[left], nums[right]])

                    # 3️⃣ 去重：跳过重复的 left
                    while left &lt; right and nums[left] == nums[left + 1]:
                        left += 1
                    # 4️⃣ 去重：跳过重复的 right
                    while left &lt; right and nums[right] == nums[right - 1]:
                        right -= 1

                    left += 1
                    right -= 1

                elif total &lt; 0:
                    left += 1
                else:
                    right -= 1

        return res
</code></pre>
<hr />
<h2><strong>五、手动走一遍</strong></h2>
<h3><strong>示例</strong></h3>
<pre><code>nums = [-1,0,1,2,-1,-4]
</code></pre>
<p>排序后：</p>
<pre><code>[-4, -1, -1, 0, 1, 2]
</code></pre>
<h3><strong>i = 1（nums[i] = -1）</strong></h3>
<ul>
<li>left = 2（-1）</li>
<li>right = 5（2）</li>
<li>sum = 0 → 记录 [-1, -1, 2]</li>
</ul>
<p>继续：</p>
<ul>
<li>left = 3（0）</li>
<li>right = 4（1）</li>
<li>sum = 0 → 记录 [-1, 0, 1]</li>
</ul>
<p>完成。</p>
<hr />
<h2><strong>六、为什么这样能避免重复？</strong></h2>
<h3><strong>去重一共</strong></h3>
<h3><strong>三层</strong></h3>
<h4><strong>1️⃣ 固定 i 时去重</strong></h4>
<pre><code>if i &gt; 0 and nums[i] == nums[i - 1]:
    continue
</code></pre>
<p>避免同一个数作为第一个元素重复出现。</p>
<hr />
<h4><strong>2️⃣ left 指针去重</strong></h4>
<pre><code>while left &lt; right and nums[left] == nums[left + 1]:
    left += 1
</code></pre>
<hr />
<h4><strong>3️⃣ right 指针去重</strong></h4>
<pre><code>while left &lt; right and nums[right] == nums[right - 1]:
    right -= 1
</code></pre>
<hr />
<h2><strong>七、时间 &amp; 空间复杂度</strong></h2>
<ul>
<li>
<p>时间复杂度：</p>
<ul>
<li>排序：O(n log n)</li>
<li>双指针：O(n²)</li>
<li>总体：<strong>O(n²)</strong></li>
</ul>
</li>
<li>
<p>空间复杂度：</p>
<ul>
<li>排序：O(log n)（Python 内部）</li>
<li>结果集不算额外空间</li>
</ul>
</li>
</ul>
<hr />
<h2><strong>八、常见错误</strong></h2>
<h3><strong>❌ 错误 1：忘记排序</strong></h3>
<p>👉 双指针失效 + 无法去重</p>
<hr />
<h3><strong>❌ 错误 2：去重写错位置</strong></h3>
<pre><code>if nums[left] == nums[left + 1]: left += 1  # ❌ 只跳一次
</code></pre>
<p>👉 必须用 while</p>
<hr />
<h3><strong>❌ 错误 3：没做剪枝</strong></h3>
<pre><code>if nums[i] &gt; 0: break
</code></pre>
<p>👉 会做很多无效计算</p>
<hr />
<h2><strong>九、面试标准回答模板</strong></h2>
<blockquote>
<p>我先对数组进行排序，然后固定一个数，使用双指针在其右侧查找另外两个数，使三数之和为 0。通过在固定元素和双指针移动过程中跳过重复值，保证结果中不包含重复三元组，时间复杂度为 O(n²)。</p>
</blockquote>
<hr />
<p>看到题目出现：</p>
<ul>
<li>三数之和 / 四数之和</li>
<li>去重</li>
<li>不要求下标，只要组合</li>
</ul>
<p>👉 <strong>排序 + 双指针</strong></p>
]]></content><link href="https://github.com/lihe/MyLeetcode/issues/9"/><category term="数组"/><published>2025-12-28T06:25:58+00:00</published></entry><entry><id>https://github.com/lihe/MyLeetcode/issues/8</id><title>LeetCode 59：螺旋矩阵 II</title><updated>2026-01-06T13:48:48.337186+00:00</updated><content type="html"><![CDATA[<p><a href="https://leetcode.cn/problems/spiral-matrix-ii/">https://leetcode.cn/problems/spiral-matrix-ii/</a></p>
<h2><strong>一、核心思路</strong></h2>
<blockquote>
<p>用 <strong>四个边界</strong> 控制方向：</p>
</blockquote>
<blockquote>
<p>上 → 右 → 下 → 左</p>
</blockquote>
<blockquote>
<p>每走完一条边，就把对应边界往里收缩一圈。</p>
</blockquote>
<hr />
<h2><strong>二、为什么这是最稳的解法？</strong></h2>
<ul>
<li>n ≤ 20，规模很小</li>
<li>只需要 <strong>模拟填数</strong></li>
<li>不需要复杂数据结构</li>
<li>时间复杂度 <strong>O(n²)</strong>（刚好填满矩阵）</li>
</ul>
<hr />
<h2><strong>三、标准解法</strong></h2>
<pre><code class="language-python">class Solution:
    def generateMatrix(self, n):
        matrix = [[0] * n for _ in range(n)]

        top, bottom = 0, n - 1
        left, right = 0, n - 1
        num = 1

        while top &lt;= bottom and left &lt;= right:
            # 1. 从左到右
            for col in range(left, right + 1):
                matrix[top][col] = num
                num += 1
            top += 1

            # 2. 从上到下
            for row in range(top, bottom + 1):
                matrix[row][right] = num
                num += 1
            right -= 1

            # 3. 从右到左
            if top &lt;= bottom:
                for col in range(right, left - 1, -1):
                    matrix[bottom][col] = num
                    num += 1
                bottom -= 1

            # 4. 从下到上
            if left &lt;= right:
                for row in range(bottom, top - 1, -1):
                    matrix[row][left] = num
                    num += 1
                left += 1

        return matrix
</code></pre>
<hr />
<h2><strong>四、手动推演</strong></h2>
<h3><strong>示例：n = 3</strong></h3>
<p>初始：</p>
<pre><code>top=0 bottom=2 left=0 right=2
num=1
</code></pre>
<h3><strong>第一圈：</strong></h3>
<p>1️⃣ 左 → 右（top=0）</p>
<pre><code>[1, 2, 3]
[0, 0, 0]
[0, 0, 0]
</code></pre>
<p>2️⃣ 上 → 下（right=2）</p>
<pre><code>[1, 2, 3]
[0, 0, 4]
[0, 0, 5]
</code></pre>
<p>3️⃣ 右 → 左（bottom=2）</p>
<pre><code>[1, 2, 3]
[0, 0, 4]
[7, 6, 5]
</code></pre>
<p>4️⃣ 下 → 上（left=0）</p>
<pre><code>[1, 2, 3]
[8, 0, 4]
[7, 6, 5]
</code></pre>
<hr />
<h3><strong>第二圈：</strong></h3>
<pre><code>top=1 bottom=1 left=1 right=1
</code></pre>
<p>填中间：</p>
<pre><code>[1, 2, 3]
[8, 9, 4]
[7, 6, 5]
</code></pre>
<hr />
<h2><strong>五、边界判断为什么不能省？</strong></h2>
<p>这两句是<strong>防止重复填 / 越界</strong>的关键：</p>
<pre><code>if top &lt;= bottom:
if left &lt;= right:
</code></pre>
<h3><strong>否则会发生什么？</strong></h3>
<ul>
<li>n 为奇数时</li>
<li>中心点会被重复填</li>
<li>或方向走“反了”</li>
</ul>
<p>👉 <strong>这是本题最常见的翻车点</strong></p>
<hr />
<h2><strong>六、时间 &amp; 空间复杂度</strong></h2>
<ul>
<li>时间复杂度：O(n²)</li>
<li>空间复杂度：O(n²)（输出矩阵）</li>
</ul>
<p>这是 <strong>最优 &amp; 不可避免的</strong>。</p>
<hr />
<h2><strong>七、另一种写法（方向数组法，了解即可）</strong></h2>
<p>思路：</p>
<p>用方向数组 [(0,1),(1,0),(0,-1),(-1,0)] 控制方向</p>
<p>碰壁或已访问就转向</p>
<p>⚠️ 写法更灵活，但更容易出 bug</p>
<p>👉 <strong>面试不如边界法稳</strong></p>
<hr />
<h2><strong>八、常见错误总结</strong></h2>
<h3><strong>❌ 错误 1：忘记边界判断</strong></h3>
<pre><code>for col in range(right, left - 1, -1):  # 没判断
</code></pre>
<hr />
<h3><strong>❌ 错误 2：边界更新顺序错</strong></h3>
<ul>
<li>top += 1、right -= 1 写错位置</li>
<li>会导致覆盖 / 漏填</li>
</ul>
<hr />
<h3><strong>❌ 错误 3：while 条件写错</strong></h3>
<pre><code>while top &lt; bottom and left &lt; right:  # ❌
</code></pre>
<p>👉 会漏掉中间一圈</p>
<hr />
<h2><strong>九、面试标准回答模板</strong></h2>
<blockquote>
<p>我通过维护上下左右四个边界来模拟螺旋填充，每次按照右、下、左、上的顺序填数，并在填完一条边后收缩对应边界，直到所有元素填完。</p>
</blockquote>
<hr />
]]></content><link href="https://github.com/lihe/MyLeetcode/issues/8"/><category term="数组"/><published>2025-12-28T05:57:46+00:00</published></entry><entry><id>https://github.com/lihe/MyLeetcode/issues/7</id><title>LeetCode 209：长度最小的子数组</title><updated>2026-01-06T13:48:48.543586+00:00</updated><content type="html"><![CDATA[<p><a href="https://leetcode.cn/problems/minimum-size-subarray-sum/description/">https://leetcode.cn/problems/minimum-size-subarray-sum/description/</a></p>
<hr />
<h2><strong>一、这题为什么能用 O(n)？</strong></h2>
<p>题目里有一个<strong>隐藏但决定性的条件</strong>：</p>
<blockquote>
<p><strong>nums 中全是正整数</strong></p>
</blockquote>
<p>这意味着：</p>
<ul>
<li>窗口右扩 → 和 <strong>只会变大</strong></li>
<li>窗口左缩 → 和 <strong>只会变小</strong></li>
</ul>
<p>👉 <strong>窗口具有单调性</strong></p>
<p>👉 <strong>可以用滑动窗口，而不是暴力枚举</strong></p>
<hr />
<h2><strong>二、O(n) 解法：滑动窗口</strong></h2>
<h3><strong>核心思想</strong></h3>
<blockquote>
<p>用两个指针维护一个窗口，右指针负责扩展直到满足条件，左指针负责收缩以缩短长度。</p>
</blockquote>
<hr />
<h2><strong>三、标准代码</strong></h2>
<pre><code class="language-python">class Solution:
    def minSubArrayLen(self, target, nums):
        left = 0
        curr_sum = 0
        res = float(&#x27;inf&#x27;)

        for right in range(len(nums)):
            curr_sum += nums[right]

            while curr_sum &gt;= target:
                res = min(res, right - left + 1)
                curr_sum -= nums[left]
                left += 1

        return 0 if res == float(&#x27;inf&#x27;) else res
</code></pre>
<hr />
<h2><strong>四、手动走一遍</strong></h2>
<h3><strong>示例 1</strong></h3>
<pre><code>target = 7
nums = [2,3,1,2,4,3]
</code></pre>
<p>初始化：</p>
<pre><code>left=0, curr_sum=0, res=∞
</code></pre>
<h3><strong>过程关键片段：</strong></h3>
<p>1️⃣ right=3 → curr_sum = 8</p>
<p>满足条件，开始收缩：</p>
<ul>
<li>res = 4</li>
<li>去掉 nums[0]=2 → sum=6（不满足）</li>
</ul>
<p>2️⃣ right=4 → curr_sum = 10</p>
<p>收缩：</p>
<ul>
<li>res = 4</li>
<li>去掉 nums[1]=3 → sum=7</li>
<li>res = 3</li>
<li>去掉 nums[2]=1 → sum=6（停止）</li>
</ul>
<p>3️⃣ right=5 → curr_sum = 9</p>
<p>收缩：</p>
<ul>
<li>res = 3</li>
<li>去掉 nums[3]=2 → sum=7</li>
<li>res = 2</li>
<li>去掉 nums[4]=4 → sum=3</li>
</ul>
<p>最终：</p>
<pre><code>res = 2
</code></pre>
<hr />
<h2><strong>五、为什么时间复杂度是 O(n)？</strong></h2>
<p>虽然看起来有两层循环：</p>
<pre><code>for right in ...
    while curr_sum &gt;= target
</code></pre>
<p>但注意：</p>
<ul>
<li>right 只走一遍</li>
<li>left 也只走一遍</li>
</ul>
<p>👉 <strong>每个元素最多被加一次、减一次</strong></p>
<p>所以是：</p>
<pre><code>O(n)
</code></pre>
<hr />
<h2><strong>六、常见错误</strong></h2>
<h3><strong>❌ 错误 1：用 if 而不是 while</strong></h3>
<pre><code>if curr_sum &gt;= target:   # ❌
</code></pre>
<p>👉 你会错过更短的子数组</p>
<hr />
<h3><strong>❌ 错误 2：没意识到“正整数”是前提</strong></h3>
<p>如果 nums 里有负数：</p>
<ul>
<li>滑动窗口 ❌ 不成立</li>
<li>要换方法（前缀和 + 二分 / 哈希）</li>
</ul>
<hr />
<h3><strong>❌ 错误 3：忘记返回 0 的情况</strong></h3>
<hr />
<h2><strong>七、进阶：O(n log n) 解法</strong></h2>
<h3><strong>思路</strong></h3>
<ul>
<li>用 <strong>前缀和</strong></li>
<li>对每个起点 i</li>
<li>二分找最小的 j，使：</li>
</ul>
<p>$prefix[j] - prefix[i] \ge target$</p>
<hr />
<h3><strong>代码</strong></h3>
<pre><code class="language-python">import bisect

class Solution:
    def minSubArrayLen(self, target, nums):
        n = len(nums)
        prefix = [0] * (n + 1)
        for i in range(n):
            prefix[i + 1] = prefix[i] + nums[i]

        res = float(&#x27;inf&#x27;)
        for i in range(n):
            need = target + prefix[i]
            j = bisect.bisect_left(prefix, need)
            if j &lt;= n:
                res = min(res, j - i)

        return 0 if res == float(&#x27;inf&#x27;) else res
</code></pre>
<hr />
<h3><strong>复杂度</strong></h3>
<ul>
<li>构建前缀和：O(n)</li>
<li>每次二分：O(log n)</li>
<li>总体：O(n log n)</li>
</ul>
<hr />
<h2><strong>八、O(n) vs O(n log n) 怎么选？</strong></h2>
<blockquote>
<p>因为数组元素全为正数，滑动窗口具有单调性，所以可以用 O(n) 的双指针解法。</p>
</blockquote>
<blockquote>
<p>如果不满足单调性，或者需要支持更复杂查询，可以用前缀和加二分。</p>
</blockquote>
<hr />
<h2><strong>九、这道题在“滑动窗口体系”中的地位</strong></h2>
<p>这是 <strong>最标准的滑动窗口模型</strong>：</p>
<ul>
<li>固定条件：sum &gt;= target</li>
<li>优化目标：<strong>窗口长度最小</strong></li>
</ul>
<p>你后面会遇到大量同构题：</p>
<ul>
<li>最长 / 最短子数组</li>
<li>满足条件的子串</li>
<li>至多 / 至少 K 个元素</li>
</ul>
<p>👉 <strong>模板一模一样</strong></p>
<p>看到题目出现：</p>
<ul>
<li>连续子数组</li>
<li>全是正数</li>
<li>求最短 / 最长</li>
</ul>
<p><strong>第一反应：滑动窗口</strong></p>
]]></content><link href="https://github.com/lihe/MyLeetcode/issues/7"/><category term="数组"/><published>2025-12-28T04:42:13+00:00</published></entry><entry><id>https://github.com/lihe/MyLeetcode/issues/6</id><title>LeetCode 704：二分查找（Binary Search）</title><updated>2026-01-06T13:48:48.761463+00:00</updated><content type="html"><![CDATA[<p><a href="https://leetcode.cn/problems/binary-search/">https://leetcode.cn/problems/binary-search/</a></p>
<h2><strong>一、核心结论</strong></h2>
<blockquote>
<p><strong>有序数组 + O(log n)</strong></p>
</blockquote>
<blockquote>
<p>👉 <strong>只能用二分查找</strong></p>
</blockquote>
<hr />
<h2><strong>二、标准二分查找（推荐写法）</strong></h2>
<h3><strong>✅ Python 正确实现（闭区间写法）</strong></h3>
<pre><code class="language-python">class Solution:
    def search(self, nums, target):
        left, right = 0, len(nums) - 1

        while left &lt;= right:
            mid = (left + right) // 2

            if nums[mid] == target:
                return mid
            elif nums[mid] &lt; target:
                left = mid + 1
            else:
                right = mid - 1

        return -1
</code></pre>
<hr />
<h2><strong>三、用示例完整走一遍</strong></h2>
<h3><strong>示例 1</strong></h3>
<pre><code>nums = [-1,0,3,5,9,12]
target = 9
</code></pre>
<table>
<thead>
<tr>
<th><strong>left</strong></th>
<th><strong>right</strong></th>
<th><strong>mid</strong></th>
<th><strong>nums[mid]</strong></th>
<th><strong>操作</strong></th>
</tr>
</thead>
<tbody>
<tr>
<td>0</td>
<td>5</td>
<td>2</td>
<td>3</td>
<td>target &gt; 3 → left=3</td>
</tr>
<tr>
<td>3</td>
<td>5</td>
<td>4</td>
<td>9</td>
<td>命中 → return 4</td>
</tr>
</tbody></table><hr />
<h3><strong>示例 2</strong></h3>
<pre><code>nums = [-1,0,3,5,9,12]
target = 2
</code></pre>
<table>
<thead>
<tr>
<th><strong>left</strong></th>
<th><strong>right</strong></th>
<th><strong>mid</strong></th>
<th><strong>nums[mid]</strong></th>
<th><strong>操作</strong></th>
</tr>
</thead>
<tbody>
<tr>
<td>0</td>
<td>5</td>
<td>2</td>
<td>3</td>
<td>target &lt; 3 → right=1</td>
</tr>
<tr>
<td>0</td>
<td>1</td>
<td>0</td>
<td>-1</td>
<td>target &gt; -1 → left=1</td>
</tr>
<tr>
<td>1</td>
<td>1</td>
<td>1</td>
<td>0</td>
<td>target &gt; 0 → left=2</td>
</tr>
<tr>
<td>2</td>
<td>1</td>
<td>结束</td>
<td></td>
<td>return -1</td>
</tr>
</tbody></table><hr />
<h2><strong>四、为什么时间复杂度是 O(log n)？</strong></h2>
<h3><strong>每一步都在干什么？</strong></h3>
<ul>
<li>把搜索区间 <strong>缩小一半</strong></li>
<li>n → n/2 → n/4 → …</li>
</ul>
<p>查找次数：</p>
<p>$\log_2 n$</p>
<p>👉 所以是 <strong>O(log n)</strong></p>
<hr />
<h2><strong>五、二分查找的三个“铁律”</strong></h2>
<h3><strong>1️⃣ 数组必须有序</strong></h3>
<p>（本题已保证）</p>
<hr />
<h3><strong>2️⃣ 区间定义要统一</strong></h3>
<p>你这版用的是：</p>
<pre><code>[left, right]  闭区间
</code></pre>
<p>所以：</p>
<ul>
<li>while 用 &lt;=</li>
<li>左边收缩：left = mid + 1</li>
<li>右边收缩：right = mid - 1</li>
</ul>
<hr />
<h3><strong>3️⃣ mid 一定要这样算</strong></h3>
<pre><code>mid = (left + right) // 2
</code></pre>
<p>（Python 不会溢出，但这是标准写法）</p>
<hr />
<h2><strong>六、常见错误（90% 翻车点）</strong></h2>
<h3><strong>❌ 错误 1：while 写成</strong></h3>
<h3><strong>&lt;</strong></h3>
<pre><code>while left &lt; right:   # ❌
</code></pre>
<p>👉 会漏掉最后一个元素</p>
<hr />
<h3><strong>❌ 错误 2：区间没对齐</strong></h3>
<pre><code>right = mid      # ❌
</code></pre>
<p>👉 可能死循环</p>
<hr />
<h3><strong>❌ 错误 3：忘记 return -1</strong></h3>
<hr />
<h2><strong>七、另一种写法（左闭右开区间，了解即可）</strong></h2>
<pre><code class="language-python">class Solution:
    def search(self, nums, target):
        left, right = 0, len(nums)

        while left &lt; right:
            mid = (left + right) // 2
            if nums[mid] == target:
                return mid
            elif nums[mid] &lt; target:
                left = mid + 1
            else:
                right = mid

        return -1
</code></pre>
<hr />
<h2><strong>八、面试标准回答模板</strong></h2>
<blockquote>
<p>这是一个典型的二分查找问题。</p>
</blockquote>
<blockquote>
<p>因为数组是有序的，并且要求 O(log n) 的时间复杂度，我使用二分查找，在每一步把搜索区间缩小一半，最终找到目标值或返回 -1。</p>
</blockquote>
<hr />
<h1>进阶</h1>
<h2><strong>一、问题</strong></h2>
<blockquote>
<p><strong>数组有重复元素</strong></p>
</blockquote>
<blockquote>
<p>要求：<strong>返回 target 第一次出现的位置（最左边）</strong></p>
</blockquote>
<p>这是经典问题，通常叫：</p>
<ul>
<li><strong>Binary Search – First Occurrence</strong></li>
<li>或 <strong>lower_bound 的等值版本</strong></li>
</ul>
<hr />
<h2><strong>二、思路</strong></h2>
<blockquote>
<p>在搜索到 target 之后 <strong>不立即返回</strong>，</p>
</blockquote>
<blockquote>
<p><strong>记录 mid</strong>，然后把 right 向左移动一位继续找</p>
</blockquote>
<p>但要注意两点细节：</p>
<ol>
<li><strong>right 不是简单减 1</strong></li>
<li>而是 <strong>right = mid - 1</strong></li>
</ol>
<hr />
<h2><strong>三、正确的“找第一个 target”的二分逻辑</strong></h2>
<h3><strong>关键思想（一句话）</strong></h3>
<blockquote>
<p><strong>当 nums[mid] == target 时，说明答案在左侧（包括 mid），</strong></p>
</blockquote>
<blockquote>
<p><strong>先记下 mid，再继续向左收缩区间。</strong></p>
</blockquote>
<hr />
<h2><strong>四、标准代码</strong></h2>
<pre><code class="language-python">class Solution:
    def searchFirst(self, nums, target):
        left, right = 0, len(nums) - 1
        ans = -1  # 用来记录答案

        while left &lt;= right:
            mid = (left + right) // 2

            if nums[mid] == target:
                ans = mid              # 记录当前命中位置
                right = mid - 1        # 继续向左找
            elif nums[mid] &lt; target:
                left = mid + 1
            else:
                right = mid - 1

        return ans
</code></pre>
<hr />
<h2><strong>五、手动走一遍（你一定要能走）</strong></h2>
<h3><strong>示例</strong></h3>
<pre><code>nums = [1,2,2,2,3,4]
target = 2
</code></pre>
<table>
<thead>
<tr>
<th><strong>left</strong></th>
<th><strong>right</strong></th>
<th><strong>mid</strong></th>
<th><strong>nums[mid]</strong></th>
<th><strong>操作</strong></th>
<th><strong>ans</strong></th>
</tr>
</thead>
<tbody>
<tr>
<td>0</td>
<td>5</td>
<td>2</td>
<td>2</td>
<td>记录，right=1</td>
<td>2</td>
</tr>
<tr>
<td>0</td>
<td>1</td>
<td>0</td>
<td>1</td>
<td>left=1</td>
<td>2</td>
</tr>
<tr>
<td>1</td>
<td>1</td>
<td>1</td>
<td>2</td>
<td>记录，right=0</td>
<td>1</td>
</tr>
<tr>
<td>1</td>
<td>0</td>
<td>结束</td>
<td></td>
<td></td>
<td>1</td>
</tr>
</tbody></table><p>✅ 返回 1（第一个 2）</p>
<hr />
<h2><strong>六、和“普通二分”的本质区别</strong></h2>
<h3><strong>普通二分（无重复）</strong></h3>
<pre><code>if nums[mid] == target:
    return mid
</code></pre>
<hr />
<h3><strong>找第一个（有重复）</strong></h3>
<pre><code>if nums[mid] == target:
    ans = mid
    right = mid - 1
</code></pre>
<p>📌 <strong>区别只在这一点</strong></p>
<hr />
<h2><strong>七、面试官标准听法</strong></h2>
<blockquote>
<p>当数组中存在重复元素时，如果要求返回第一个 target，在二分查找到 target 后不能立即返回，而是记录当前位置，并继续在左半区间查找，最终得到最左侧的 target 下标。</p>
</blockquote>
<p>这句话 <strong>非常标准</strong>。</p>
<hr />
<h2><strong>八、顺手给一个“对称版本”（找最后一个）</strong></h2>
<h3><strong>找最后一个 target（最右）</strong></h3>
<pre><code class="language-python">class Solution:
    def searchLast(self, nums, target):
        left, right = 0, len(nums) - 1
        ans = -1

        while left &lt;= right:
            mid = (left + right) // 2

            if nums[mid] == target:
                ans = mid
                left = mid + 1     # 向右找
            elif nums[mid] &lt; target:
                left = mid + 1
            else:
                right = mid - 1

        return ans
</code></pre>
<hr />
<h2><strong>九、二分查找“进阶口诀”</strong></h2>
<blockquote>
<p><strong>找第一个：命中后右边界左移</strong></p>
</blockquote>
<blockquote>
<p><strong>找最后一个：命中后左边界右移</strong></p>
</blockquote>
<hr />
]]></content><link href="https://github.com/lihe/MyLeetcode/issues/6"/><category term="数组"/><published>2025-12-27T05:54:00+00:00</published></entry><entry><id>https://github.com/lihe/MyLeetcode/issues/5</id><title>LeetCode 215. 数组中的第K个最大元素</title><updated>2026-01-06T13:48:48.991414+00:00</updated><content type="html"><![CDATA[<p><a href="https://leetcode.cn/problems/kth-largest-element-in-an-array/solutions/821137/ji-yu-kuai-pai-de-suo-you-topkwen-ti-jia-ylsd/">https://leetcode.cn/problems/kth-largest-element-in-an-array/solutions/821137/ji-yu-kuai-pai-de-suo-you-topkwen-ti-jia-ylsd/</a></p>
<hr />
<h2><strong>一、TopK 问题的本质</strong></h2>
<blockquote>
<p><strong>TopK = Partition（切分） + 选择（不完全排序）</strong></p>
</blockquote>
<p>不是排序问题，</p>
<p>而是 <strong>“把第 k 个元素放到它最终该在的位置”</strong>。</p>
<hr />
<h2><strong>二、面试中 TopK 的 5 种标准问法</strong></h2>
<table>
<thead>
<tr>
<th><strong>问法</strong></th>
<th><strong>本质</strong></th>
</tr>
</thead>
<tbody>
<tr>
<td>前 k 小的数</td>
<td>切到索引 k-1</td>
</tr>
<tr>
<td>第 k 小的数</td>
<td>找索引 k-1</td>
</tr>
<tr>
<td>前 k 大的数</td>
<td>切到索引 n-k</td>
</tr>
<tr>
<td>第 k 大的数</td>
<td>找索引 n-k</td>
</tr>
<tr>
<td>只排序前 / 后 k 个</td>
<td>Quickselect + 局部排序</td>
</tr>
</tbody></table><p>👉 <strong>90% 的 TopK 都能映射到这 5 个</strong></p>
<hr />
<h2><strong>三、两种主流解法选型</strong></h2>
<h3><strong>1️⃣ 快选（Quickselect）—— 面试首选</strong></h3>
<ul>
<li>平均时间复杂度：O(n)</li>
<li>空间复杂度：O(1)</li>
<li>原地</li>
<li>不需要完全排序</li>
</ul>
<p>📌 <strong>当 k 不接近 n，或强调效率 → 一定选它</strong></p>
<hr />
<h3><strong>2️⃣ 堆（Heap）—— 稳定保底方案</strong></h3>
<ul>
<li>时间复杂度：O(n log k)</li>
<li>适合 <strong>流式数据 / 超大数据 / 在线 TopK</strong></li>
</ul>
<p>📌 面试官追问时你可以说：</p>
<blockquote>
<p>如果是数据流或内存受限，我会用小根堆 / 大根堆。</p>
</blockquote>
<hr />
<h2><strong>四、Partition（核心中的核心）</strong></h2>
<h3><strong>标准挖坑法</strong></h3>
<pre><code class="language-python">def partition(nums, left, right):
    pivot = nums[left]
    i, j = left, right
    while i &lt; j:
        while i &lt; j and nums[j] &gt;= pivot:
            j -= 1
        nums[i] = nums[j]
        while i &lt; j and nums[i] &lt;= pivot:
            i += 1
        nums[j] = nums[i]
    nums[i] = pivot
    return i
</code></pre>
<p>📌 面试官可能追问：</p>
<ul>
<li>
<p>这是 Hoare 还是 Lomuto？</p>
<p>👉 <strong>Hoare 变体（挖坑法）</strong></p>
</li>
</ul>
<hr />
<h2><strong>五、Quickselect 模板</strong></h2>
<blockquote>
<p><strong>k 一定是 0-based 索引</strong></p>
</blockquote>
<pre><code class="language-python">def quickselect(nums, k, left, right):
    if left &lt; right:
        index = partition(nums, left, right)
        if index == k:
            return
        elif index &lt; k:
            quickselect(nums, k, index + 1, right)
        else:
            quickselect(nums, k, left, index - 1)
</code></pre>
<hr />
<h2><strong>六、TopK 全家桶</strong></h2>
<h3><strong>1️⃣ 前 k 小的数</strong></h3>
<pre><code class="language-python">def topk_smalls(nums, k):
    quickselect(nums, k - 1, 0, len(nums) - 1)
    return nums[:k]
</code></pre>
<hr />
<h3><strong>2️⃣ 第 k 小的数</strong></h3>
<pre><code class="language-python">def kth_small(nums, k):
    quickselect(nums, k - 1, 0, len(nums) - 1)
    return nums[k - 1]
</code></pre>
<hr />
<h3><strong>3️⃣ 前 k 大的数</strong></h3>
<pre><code class="language-python">def topk_larges(nums, k):
    n = len(nums)
    quickselect(nums, n - k, 0, n - 1)
    return nums[n - k:]
</code></pre>
<hr />
<h3><strong>4️⃣ 第 k 大的数</strong></h3>
<pre><code class="language-python">def kth_large(nums, k):
    n = len(nums)
    quickselect(nums, n - k, 0, n - 1)
    return nums[n - k]
</code></pre>
<hr />
<h3><strong>5️⃣ 只排序前 k 个小的数</strong></h3>
<pre><code class="language-python">def topk_sort_left(nums, k):
    quickselect(nums, k - 1, 0, len(nums) - 1)
    quicksort(nums, 0, k - 1)
    return nums
</code></pre>
<hr />
<h3><strong>6️⃣ 只排序后 k 个大的数</strong></h3>
<pre><code class="language-python">def topk_sort_right(nums, k):
    n = len(nums)
    quickselect(nums, n - k, 0, n - 1)
    quicksort(nums, n - k, n - 1)
    return nums
</code></pre>
<hr />
<h2><strong>七、k 的语义速查表</strong></h2>
<table>
<thead>
<tr>
<th><strong>人类语义</strong></th>
<th><strong>Quickselect 用的 k</strong></th>
</tr>
</thead>
<tbody>
<tr>
<td>第 k 小</td>
<td>k-1</td>
</tr>
<tr>
<td>前 k 小</td>
<td>k-1</td>
</tr>
<tr>
<td>第 k 大</td>
<td>n-k</td>
</tr>
<tr>
<td>前 k 大</td>
<td>n-k</td>
</tr>
</tbody></table><p>📌 <strong>90% TopK bug 来自这里</strong></p>
<hr />
<h2><strong>八、面试官高频追问 &amp; 标准回答</strong></h2>
<h3><strong>Q1：为什么不用快速排序？</strong></h3>
<blockquote>
<p>因为 TopK 不需要全局有序，只需要第 k 个元素就位。</p>
</blockquote>
<blockquote>
<p>Quickselect 平均时间复杂度是 O(n)，优于排序的 O(n log n)。</p>
</blockquote>
<hr />
<h3><strong>Q2：Quickselect 最坏情况？</strong></h3>
<blockquote>
<p>最坏是 O(n^2)，可以通过随机 pivot 或三数取中优化。</p>
</blockquote>
<hr />
<h3><strong>Q3：什么时候用堆？</strong></h3>
<blockquote>
<p>数据流、k 很小、或者无法一次性加载全部数据时。</p>
</blockquote>
<hr />
<h3><strong>Q4：TopK 会稳定吗？</strong></h3>
<blockquote>
<p>不稳定。Quickselect 和 Heap 都不保证稳定性。</p>
</blockquote>
<hr />
<h2><strong>九、30 秒面试答题心法</strong></h2>
<blockquote>
<p>TopK 本质是选择问题而不是排序问题。</p>
</blockquote>
<blockquote>
<p>我会使用基于 Partition 的 Quickselect，在平均 O(n) 时间内把第 k 个元素放到正确位置。</p>
</blockquote>
<blockquote>
<p>如果需要排序前 / 后 k 个元素，只在局部再做一次排序即可。</p>
</blockquote>
<hr />
<h1><strong>进阶：无序数组找中位数（字节高频题）</strong></h1>
<hr />
<h2><strong>一、题目描述（面试原型）</strong></h2>
<blockquote>
<p>给定一个<strong>无序数组</strong>，在 <strong>不排序整个数组</strong> 的前提下，找到数组的 <strong>中位数</strong>，并分析时间复杂度。</p>
</blockquote>
<hr />
<h2><strong>二、「一句话答案」</strong></h2>
<blockquote>
<p>无序数组找中位数是一个<strong>选择问题</strong>，</p>
</blockquote>
<blockquote>
<p>可以用 <strong>Quickselect（基于 Partition）</strong>，</p>
</blockquote>
<blockquote>
<p>平均时间复杂度是 <strong>O(n)</strong>，空间复杂度 <strong>O(1)</strong>。</p>
</blockquote>
<hr />
<h2><strong>三、中位数 = 第 k 小元素（关键映射）</strong></h2>
<p>设数组长度为 n（0-based 索引）：</p>
<h3><strong>1️⃣ n 为奇数</strong></h3>
<pre><code>中位数 = 第 (n//2 + 1) 小
索引 = n//2
</code></pre>
<h3><strong>2️⃣ n 为偶数（两种情况）</strong></h3>
<table>
<thead>
<tr>
<th><strong>面试要求</strong></th>
<th><strong>处理方式</strong></th>
</tr>
</thead>
<tbody>
<tr>
<td>任意一个中位数</td>
<td>找第 n//2 小</td>
</tr>
<tr>
<td>数学中位数</td>
<td>找第 n//2 和 n//2 - 1，取平均</td>
</tr>
</tbody></table><hr />
<h2><strong>四、标准解法：Quickselect（核心）</strong></h2>
<h3><strong>1️⃣ 为什么不用排序？</strong></h3>
<blockquote>
<p>排序是 O(n log n)，</p>
</blockquote>
<blockquote>
<p>但中位数只需要一个元素的位置，不需要全局有序。</p>
</blockquote>
<p>这是面试官<strong>一定会追问</strong>的点。</p>
<hr />
<h3><strong>2️⃣ Quickselect 思想</strong></h3>
<blockquote>
<p>每一次 partition，都能确定一个元素的最终位置</p>
</blockquote>
<blockquote>
<p>如果这个位置正好是中位数索引，直接返回</p>
</blockquote>
<blockquote>
<p>否则只递归一边</p>
</blockquote>
<hr />
<h2><strong>五、代码模板</strong></h2>
<h3><strong>Partition（挖坑法）</strong></h3>
<pre><code class="language-python">def partition(nums, left, right):
    pivot = nums[left]
    i, j = left, right
    while i &lt; j:
        while i &lt; j and nums[j] &gt;= pivot:
            j -= 1
        nums[i] = nums[j]
        while i &lt; j and nums[i] &lt;= pivot:
            i += 1
        nums[j] = nums[i]
    nums[i] = pivot
    return i
</code></pre>
<hr />
<h3><strong>Quickselect</strong></h3>
<pre><code class="language-python">def quickselect(nums, k, left, right):
    if left &lt; right:
        index = partition(nums, left, right)
        if index == k:
            return
        elif index &lt; k:
            quickselect(nums, k, index + 1, right)
        else:
            quickselect(nums, k, left, index - 1)
</code></pre>
<hr />
<h3><strong>找中位数（奇数长度）</strong></h3>
<pre><code class="language-python">def find_median(nums):
    n = len(nums)
    k = n // 2
    quickselect(nums, k, 0, n - 1)
    return nums[k]
</code></pre>
<hr />
<h2><strong>六、时间复杂度分析</strong></h2>
<h3><strong>平均时间复杂度：✅ O(n)</strong></h3>
<ul>
<li>每次 partition 是 O(n)</li>
<li>但只递归一半</li>
<li>所以是线性期望</li>
</ul>
<hr />
<h3><strong>最坏时间复杂度：⚠️ O(n²)</strong></h3>
<ul>
<li>pivot 每次都选到极端值（如已排序数组）</li>
</ul>
<hr />
<h3><strong>优化一句话</strong></h3>
<blockquote>
<p>可以通过 <strong>随机选 pivot</strong> 或 <strong>三数取中</strong>，</p>
</blockquote>
<blockquote>
<p>将最坏情况概率极大降低。</p>
</blockquote>
<hr />
<h2><strong>七、面试官可能的追问 &amp; 标准回答</strong></h2>
<h3><strong>Q1：如果是数据流，怎么找中位数？</strong></h3>
<blockquote>
<p>使用两个堆：</p>
</blockquote>
<ul>
<li>
<blockquote>
<p>一个大根堆存左半部分</p>
</blockquote>
</li>
<li>
<blockquote>
<p>一个小根堆存右半部分</p>
</blockquote>
<blockquote>
<p>动态维护平衡</p>
</blockquote>
</li>
</ul>
<hr />
<h3><strong>Q2：为什么 Quickselect 不稳定？</strong></h3>
<blockquote>
<p>因为 Partition 会交换相等元素的相对顺序，</p>
</blockquote>
<blockquote>
<p>不保证稳定性。</p>
</blockquote>
<hr />
<h3><strong>Q3：你会不会用堆来做？</strong></h3>
<blockquote>
<p>会，但堆是 O(n log n) 或 O(n log k)，在静态数组中不如 Quickselect 高效。</p>
</blockquote>
<hr />
<h2><strong>八、30 秒「字节标准回答模板」（可直接背）</strong></h2>
<blockquote>
<p>无序数组找中位数可以转化为选择问题。</p>
</blockquote>
<blockquote>
<p>我会使用基于 Partition 的 Quickselect 算法，在平均 O(n) 时间内把中位数放到正确位置。</p>
</blockquote>
<blockquote>
<p>相比排序的 O(n log n)，效率更高。</p>
</blockquote>
<blockquote>
<p>最坏情况是 O(n²)，可以通过随机 pivot 优化。</p>
</blockquote>
<hr />
]]></content><link href="https://github.com/lihe/MyLeetcode/issues/5"/><category term="数组"/><published>2025-12-25T14:54:28+00:00</published></entry><entry><id>https://github.com/lihe/MyLeetcode/issues/1</id><title>LeetCode 206 反转链表</title><updated>2026-01-06T13:48:49.216699+00:00</updated><content type="html"><![CDATA[<p><a href="https://leetcode.cn/problems/reverse-linked-list/description/">https://leetcode.cn/problems/reverse-linked-list/description/</a></p>
<h2><strong>一、题目回顾</strong></h2>
<p><strong>问题</strong></p>
<p>给定单链表头节点 head，反转链表并返回反转后的头节点。</p>
<hr />
<h2><strong>二、两种标准解法概览</strong></h2>
<h3><strong>1️⃣ 迭代法（首插法 / 三指针）</strong></h3>
<pre><code class="language-python">class Solution:
    def reverseList(self, head: Optional[ListNode]) -&gt; Optional[ListNode]:
        pre = None
        cur = head
        while cur:
            nxt = cur.next
            cur.next = pre
            pre = cur
            cur = nxt
        return pre
</code></pre>
<p><strong>核心思想</strong></p>
<p>不断把当前节点 cur 插到新链表头部 pre 前。</p>
<hr />
<h3><strong>2️⃣ 递归法（重点）</strong></h3>
<pre><code class="language-python">class Solution:
    def reverseList(self, head: Optional[ListNode]) -&gt; Optional[ListNode]:
        if head is None or head.next is None:
            return head
        
        rev_head = self.reverseList(head.next)
        tail = head.next
        tail.next = head
        head.next = None
        return rev_head
</code></pre>
<hr />
<h2><strong>三、递归反转链表的本质理解</strong></h2>
<h3><strong>递归一句话定义</strong></h3>
<blockquote>
<p><strong>我不反转整个链表，只负责把“当前节点”接到“已经反转好的后半段”后面。</strong></p>
</blockquote>
<hr />
<h2><strong>四、递归执行的完整过程（示例）</strong></h2>
<p>链表：</p>
<pre><code>1 → 2 → 3 → None
</code></pre>
<p>调用：</p>
<pre><code>reverseList(1)
</code></pre>
<hr />
<h3><strong>1️⃣ 递归下潜（只做调用，不反转）</strong></h3>
<pre><code>reverse(1)
 → reverse(2)
   → reverse(3)
</code></pre>
<p>在 head = 3 时触发 base case：</p>
<pre><code>if head.next is None:
    return head
</code></pre>
<p>返回节点 3</p>
<hr />
<h3><strong>2️⃣ 回溯阶段（真正发生反转）</strong></h3>
<h4><strong>回到head = 2</strong></h4>
<pre><code>tail = head.next      # 3
tail.next = head      # 3 → 2
head.next = None      # 2 → None
</code></pre>
<p>链表变为：</p>
<pre><code>3 → 2 → None
</code></pre>
<p>返回 rev_head = 3</p>
<hr />
<h4><strong>回到head = 1</strong></h4>
<pre><code>tail = head.next      # 2
tail.next = head      # 2 → 1
head.next = None      # 1 → None
</code></pre>
<p>最终链表：</p>
<pre><code>3 → 2 → 1 → None
</code></pre>
<hr />
<h2><strong>五、为什么始终返回 rev_head？</strong></h2>
<ul>
<li>rev_head <strong>永远指向反转后链表的头节点</strong></li>
<li>它来自递归最深处（原链表尾部）</li>
<li>后续每一层只是在“接节点”，<strong>不改变头指针</strong></li>
</ul>
<p>❌ 错误理解：</p>
<p>“当前 head 是新的头” —— 这是错的</p>
<hr />
<h2><strong>六、为什么必须写 head.next = None？</strong></h2>
<p>如果省略这一句：</p>
<pre><code>tail.next = head
# 没有 head.next = None
</code></pre>
<p>会发生：</p>
<pre><code>1 ↔ 2 ↔ 3
</code></pre>
<p>→ <strong>形成环</strong></p>
<p>✅ 正确作用：</p>
<blockquote>
<p>断开原有正向指针，避免产生环</p>
</blockquote>
<hr />
<h2><strong>七、递归反转链表的「通用模板」</strong></h2>
<p>遇到链表递归题，直接套：</p>
<p>1️⃣ <strong>Base Case</strong></p>
<p>空链表 / 单节点直接返回</p>
<p>2️⃣ <strong>递归调用</strong></p>
<p>先处理 head.next</p>
<p>3️⃣ <strong>局部操作</strong></p>
<p>修改当前节点与后继节点的指针关系</p>
<p>4️⃣ <strong>返回结果</strong></p>
<p>始终返回“最深层”的头节点</p>
<hr />
<h2><strong>八、迭代 vs 递归（建立直觉）</strong></h2>
<table>
<thead>
<tr>
<th><strong>方法</strong></th>
<th><strong>思维方式</strong></th>
</tr>
</thead>
<tbody>
<tr>
<td>迭代</td>
<td>我一步一步往前推</td>
</tr>
<tr>
<td>递归</td>
<td>我相信后面已经反转好了</td>
</tr>
</tbody></table><hr />
<h2><strong>九、🔥 高阶考点：带环链表会发生什么？</strong></h2>
<h3><strong>输入：带环链表</strong></h3>
<pre><code>A → B → C → D → E
        ↑       ↓
        └───────┘
</code></pre>
<hr />
<h3><strong>✅ 结论（面试官标准答案）</strong></h3>
<p><strong>结论 1：不会死循环</strong></p>
<ul>
<li>不会无限递归</li>
<li>不会栈溢出</li>
</ul>
<p><strong>结论 2：最终结果是</strong></p>
<blockquote>
<p><strong>环内被反转，环外保持原样</strong></p>
</blockquote>
<hr />
<h2><strong>十、为什么不会死循环？</strong></h2>
<p>关键不在「有没有环」，而在这里：</p>
<pre><code>head.next = None
</code></pre>
<h3><strong>关键机制</strong></h3>
<ul>
<li>递归下潜时：<strong>不会修改指针</strong></li>
<li>第一次回溯时：</li>
</ul>
<pre><code>head.next = None
</code></pre>
<ul>
<li>👉 <strong>直接剪断环</strong></li>
</ul>
<p>一旦环被破坏：</p>
<ul>
<li>后续递归不会再绕回</li>
<li>递归自然结束</li>
</ul>
<p>📌 加分表述：</p>
<blockquote>
<p>虽然输入是带环链表，但递归反转在回溯阶段会主动破坏环结构，因此不会死循环。</p>
</blockquote>
<hr />
<h2><strong>十一、为什么“环内反转，环外不动”？</strong></h2>
<p>原因只有一句话：</p>
<blockquote>
<p><strong>只有参与回溯的节点，才会执行反转逻辑</strong></p>
</blockquote>
<ul>
<li>递归深入到环内后，才开始回溯</li>
<li>回溯只覆盖环内节点</li>
<li>环外节点的 next 从未被置为 None</li>
<li>因此顺序保持不变</li>
</ul>
<hr />
<h3><strong>最终结构示意</strong></h3>
<p>原始：</p>
<pre><code>A → B → C → D → E
        ↑       ↓
        └───────┘
</code></pre>
<p>结果：</p>
<pre><code>A → B → C ← D ← E
        |
       None
</code></pre>
<hr />
<h2><strong>十二、面试满分回答模板（可直接背）</strong></h2>
<blockquote>
<p>对于带环链表，这个递归反转函数不会死循环。</p>
</blockquote>
<blockquote>
<p>因为在回溯阶段会执行 head.next = None，第一次回溯就会破坏环结构，使递归能够正常终止。</p>
</blockquote>
<blockquote>
<p>最终结果是：环内节点被反转，环外节点保持原有顺序不变。</p>
</blockquote>
]]></content><link href="https://github.com/lihe/MyLeetcode/issues/1"/><category term="链表"/><published>2025-12-24T12:54:45+00:00</published></entry></feed>